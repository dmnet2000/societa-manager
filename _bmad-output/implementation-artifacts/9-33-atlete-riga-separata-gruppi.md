---
baseline_commit: 67d9cb7583d42d9066fd2e23c5b5c92cff3e2575
---

# Story 9.33: Atlete su riga separata in `/gruppi` (elenco orizzontale)

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Admin o Dirigente che gestisce l'elenco Gruppi,
I want vedere le Atlete assegnate a un Gruppo su una riga distinta e compatta invece che in una colonna verticale,
so that l'elenco Gruppi resti leggibile anche per rose numerose, senza dover scrollare a lungo per passare da un Gruppo al successivo.

## Acceptance Criteria

1. **Given** un Admin/Dirigente/Allenatore su `/gruppi` **When** vede un Gruppo **Then** le Atlete assegnate non sono più nella colonna della riga principale (Nome/Categoria) ma su una riga di tabella distinta a piena larghezza, sotto la riga principale — stesso trattamento già applicato agli Allenatori.
2. ~~**And** l'elenco delle Atlete in quella riga è disposto in orizzontale (wrap automatico), non più una per riga verticale — stesso pattern di `AllenatoreAssegnato.tsx`/`.listaAssegnatiInline` già in uso per Allenatori.~~ — **ribaltato dall'estensione 2026-08-06**: su richiesta esplicita dell'utente, l'elenco Atlete è ora una tabella (Nome | Rimuovi nel round 2, **Nome | Certificato | Rimuovi dal round 3**, vedi AC #3), non più chip orizzontali. Gli Allenatori restano invece a chip orizzontali (non menzionati nella richiesta di estensione). — **Review fix (round 3, Acceptance Auditor)**: questo testo era rimasto disallineato a "2 colonne" dopo che il round 3 aveva già portato la tabella a 3 colonne, corretto qui.
3. **And** il form "Assegna Atleta" e il pulsante/pannello "Nuovo Atleta" si spostano nella stessa nuova riga, invariati nella funzionalità (nessuna modifica a `assegnaAtleta`/`creaEAssegnaAtleta`). **Precisato dall'estensione 2026-08-06 (round 3)**: "stessa nuova riga" resta vero a livello di `<tr>`/cella (`rigaAtlete`), ma su richiesta esplicita dell'utente il form "Assegna Atleta" e il pulsante/pannello "Nuovo Atleta" occupano ora ciascuno una propria riga visiva impilata (non più affiancati/wrap in un unico blocco), seguiti dalla tabella Atlete su una terza riga visiva ancora. Aggiunto anche un requisito non presente negli AC originali: la tabella Atlete guadagna una terza colonna "Certificato" (badge solo se scaduto o in scadenza). **Ulteriore estensione 2026-08-07 (round 4)**: la tabella Atlete guadagna altre due colonne, "Iscrizione" e "Tesseramento" (badge verde/rosso — "Iscritta"/"Non iscritta", "Tesserata"/"Non tesserata"), per un totale di 5 colonne: Nome | Certificato | Iscrizione | Tesseramento | Rimuovi, sempre sulla stessa riga per Atleta.
4. **And** l'header della tabella (`page.tsx`) riflette la nuova struttura a 2 colonne (Nome, Categoria) per la riga principale — "Atlete" non è più un'intestazione di colonna.
5. **And** nessuna regressione su `assegnaAtleta`/`rimuoviAtleta`/`creaEAssegnaAtleta` né sulla riga Allenatori già esistente (`rigaAllenatori`) — solo riposizionamento/restyling, nessuna Server Action toccata.

## Studio di usabilità (svolto in fase di analisi, 2026-08-06)

**Problema**: la colonna Atlete della riga principale impila verticalmente elenco (una per riga) + form "Assegna Atleta" + pulsante/pannello "Nuovo Atleta" — per un Gruppo con rosa numerosa (10-20 Atlete) la colonna diventa altissima, rendendo l'intero elenco Gruppi difficile da scorrere.

**Opzioni valutate con l'utente**:

- **(a) Riga separata, elenco sempre visibile in orizzontale** — mirror diretto di quanto già fatto per Allenatori: le Atlete escono dalla colonna e diventano una riga a piena larghezza, nomi disposti in orizzontale con wrap automatico invece che uno sotto l'altro. Nessun click aggiuntivo, stesso identico pattern di interazione già in produzione per Allenatori (coerenza visiva, nessuna nuova interazione da imparare per l'utente).
- **(b) Riga separata e richiudibile** — come (a), ma la riga parte collassata mostrando solo un riassunto ("Atlete: N assegnate") con un toggle per espandere l'elenco completo. Più compatta per rose molto numerose, ma richiede un click in più ad ogni consultazione e introduce un pattern di interazione (collassa/espandi) non ancora presente altrove in questa tabella.

**Scelta confermata dall'utente: opzione (a).** Motivazione: coerenza con il trattamento già applicato agli Allenatori nella stessa pagina (stessa riga, stesso pattern `.listaAssegnatiInline`), nessuna interazione aggiuntiva da imparare. Il collassamento (opzione b) resta un'estensione possibile in una story futura se una rosa particolarmente numerosa dovesse rendere anche l'elenco orizzontale troppo esteso — non richiesto ora, non assumere che vada implementato in questa storia.

## Tasks / Subtasks

- [x] Task 1: Spostare le Atlete dalla colonna della riga principale a una nuova riga distinta (AC: #1, #2, #3, #4)
  - [x] `GruppoRow.tsx`: rimuovere il terzo `<td>` della `rigaPrincipale` (righe 92-222 nella versione corrente, contiene l'intero blocco Atlete: elenco, form "Assegna Atleta", toggle "Nuovo Atleta") — la `rigaPrincipale` resta con solo `<td>{gruppo.nome}</td>` e `<td>{gruppo.categoria}</td>`.
  - [x] Nuova `<tr className={styles.rigaAtlete}>` (mirror di `rigaAllenatori`, riga 224 in poi) con un singolo `<td colSpan={2}>` (non più 3 — la riga principale ha ora 2 colonne, non 3) contenente, nell'ordine: etichetta "Atlete:", `<ul className={styles.listaAssegnatiInline}>` con `AtletaAssegnata` invece di `AllenatoreAssegnato`, il form "Assegna Atleta" (con `styles.formInline` aggiunto, mirror del form Allenatori), il toggle "Nuovo Atleta"/pannello (Task 2/3 del codice già in produzione, spostato invariato).
  - [x] `rigaAllenatori` esistente: `colSpan` da aggiornare da `3` a `2` (la riga principale ha ora 2 colonne, non più 3) — **attenzione**, se dimenticato la tabella si disallinea silenziosamente (nessun errore a runtime, solo un layout visivamente sbagliato).
  - [x] Ordine delle due righe extra (Atlete/Allenatori) da confermare in sviluppo — nessuna preferenza esplicita dell'utente, mantenere l'ordine attuale (Allenatori) o mettere Atlete prima (probabilmente più naturale, dato che la colonna Atlete era la più a destra/ultima prima) — decisione libera dello sviluppatore, non bloccante.
- [x] Task 2: Aggiornare l'header della tabella (AC: #4)
  - [x] `page.tsx`: rimuovere `<th>Atlete</th>` dal `<thead>` — resta solo `<th>Nome</th>`/`<th>Categoria</th>`.
- [x] Task 3: CSS (AC: #2)
  - [x] `gruppi.module.css`: riusare `.listaAssegnatiInline`/`.formInline`/`.azioniCompatto` già esistenti (introdotti per Allenatori, generici — non serve duplicarli). Aggiungere solo `.rigaAtlete`/`.etichettaAtlete` se serve una regola dedicata diversa da `.rigaAllenatori`/`.etichettaAllenatori` (probabilmente identiche, valutare se riusare le stesse classi generiche invece di duplicare — es. rinominare `.etichettaAllenatori` in qualcosa di più generico se usata identica per entrambe).
- [x] Task 4: Verifica
  - [x] `npx vitest run`, `npx tsc --noEmit`, `npm run lint`, `npm run build` puliti — nessun test di rendering esiste per `GruppoRow.tsx` (convenzione già accettata), nessuna modifica alle Server Action quindi nessuna modifica attesa a `actions.test.ts`.
  - [x] Verifica dal vivo (aspetto visivo reale) probabilmente non eseguibile in questo sandbox per il limite già noto (motore Prisma WASM non caricabile da Node sotto `next dev`) — se il limite persiste, documentarlo come per le storie precedenti invece di darlo per scontato.

## Dev Notes

### Pattern da riusare (non reinventare)

- **Mirror esatto della riga Allenatori** (già in produzione, `GruppoRow.tsx` righe 224-268): stessa struttura `<tr><td colSpan={N}>etichetta + <ul> inline + form inline</td></tr>`. Questa storia applica lo stesso trattamento alle Atlete — non progettare un pattern nuovo.
- **`AtletaAssegnata.tsx` non richiede modifiche**: già renderizza un `<li className={styles.atletaAssegnata}>` internamente flessibile (nome + badge + pulsante Rimuovi) — il cambio da verticale a orizzontale è puramente una questione del contenitore `<ul>` (`.listaAssegnati` → `.listaAssegnatiInline`), non del componente riga stesso. Stesso principio già verificato per `AllenatoreAssegnato.tsx`.
- **Il toggle "Nuovo Atleta"** (pulsante + pannello collassabile, codice già in produzione senza story formale dedicata) si sposta invariato nella nuova riga — nessuna modifica alla sua logica (`mostraNuovaAtleta`, pattern "adjust state during render").

### Attenzione al `colSpan`

Sia `rigaAtlete` (nuova) sia `rigaAllenatori` (esistente) devono avere `colSpan={2}`, non `{3}` — la tabella ha 2 colonne nella riga principale dopo questa storia (Nome, Categoria), non più 3. `rigaAllenatori` ha oggi `colSpan={3}` (corretto quando la riga principale aveva ancora 3 colonne, incluse le Atlete) — va aggiornato a `2` **in questa storia**, non solo la nuova riga Atlete, altrimenti la tabella si disallinea silenziosamente (nessun errore, solo un layout visivamente rotto).

### Riferimenti

- [Source: app/(gruppi-allenatori)/gruppi/GruppoRow.tsx] — struttura attuale completa, incluso il mirror da riusare (`rigaAllenatori`).
- [Source: app/(gruppi-allenatori)/gruppi/AtletaAssegnata.tsx] — componente riga, nessuna modifica prevista.
- [Source: app/(gruppi-allenatori)/gruppi/gruppi.module.css] — classi `.listaAssegnatiInline`/`.formInline`/`.rigaAllenatori`/`.etichettaAllenatori` già esistenti da riusare/mirrorare.
- [Source: app/(gruppi-allenatori)/gruppi/page.tsx] — header tabella da aggiornare.

### Project Structure Notes

- Nessun nuovo file, nessuna nuova migrazione, nessuna Server Action toccata — solo `GruppoRow.tsx`/`page.tsx`/`gruppi.module.css`.
- Modifica puramente di presentazione, stesso principio già dichiarato per la story diretta degli Allenatori.

## Review Findings

Code review con 3 layer adversariali paralleli (Blind Hunter, Edge Case Hunter, Acceptance Auditor) sul diff completo della story.

**Acceptance Auditor**: nessuna violazione degli AC — tutti e 5 verificati indipendentemente, incluso il punto specifico sul `colSpan` (entrambe le righe estese confermate a `{2}`, non solo una delle due) e la conferma che nessuna forma di collassamento (opzione (b) scartata nello studio di usabilità) sia stata implementata per errore.

- [x] [Review][Patch] Il pannello "Nuova Atleta" (paragrafo separatore + form a 6 campi), quando espanso, era un Fragment con due figli diretti dentro un `<td>` che ora è `display:flex; flex-wrap:wrap` (`.rigaAtlete td`) — prima di questa storia quel `<td>` era a layout di blocco normale, quindi i due figli si impilavano correttamente; sarebbero diventati due elementi flessibili indipendenti nella stessa riga di "Atlete:"/elenco/form-assegna/pulsante, perdendo il raggruppamento visivo del pannello. Trovato dall'Edge Case Hunter. Corretto: nuovo contenitore `.pannelloNuovaAtleta` (un solo elemento della riga flessibile esterna). [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx:144-225]
- [x] [Review][Patch] Commento di intestazione di `gruppi.module.css` (righe 1-7) citava ancora `.listaAssegnati` tra le classi disponibili, ma questa storia l'ha rimossa come dead code. Trovato dall'Acceptance Auditor. Corretto. [app/(gruppi-allenatori)/gruppi/gruppi.module.css:1-7]

- [x] [Review][Defer] Il form "Assegna Atleta" (e allo stesso modo "Assegna Allenatore") usa `.formInline` (riga, nessun `flex-wrap`) — un messaggio di errore di validazione lungo non andrebbe a capo su una riga propria, si affiderebbe solo allo scroll orizzontale del contenitore esterno. Trovato dall'Edge Case Hunter. Deferred: stesso identico gap già presente nella riga Allenatori già in produzione (spedita prima di questa storia) — questa storia lo eredita invariato mirrorando lo stesso pattern, non lo introduce né lo aggrava specificamente per le Atlete. [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx, gruppi.module.css]
- [x] [Review][Defer] L'errore inline per-riga di `AtletaAssegnata.tsx` (quando `rimuoviAtleta` fallisce su una singola Atleta) è ora un figlio di un `<li>` che è un elemento flessibile dentro `.listaAssegnatiInline` — un `<li>` più alto per via dell'errore potrebbe spostare/far andare a capo i chip vicini sulla stessa riga, cosa che non accadeva col vecchio elenco verticale. Trovato dal Blind Hunter. Deferred: caso raro (solo su un fallimento di rimozione), cosmetico e transitorio (sparisce al retry/dismiss), nessun impatto sui dati. [app/(gruppi-allenatori)/gruppi/AtletaAssegnata.tsx, gruppi.module.css]
- [x] [Review][Defer] Il commento sopra `.rigaPrincipale td, .rigaAtlete td` afferma che il bordo separatore vive solo sull'ultima delle tre righe (`rigaAllenatori`) — vero oggi, ma fragile: l'ordine Atlete-poi-Allenatori è una scelta arbitraria dello sviluppatore (nessuna preferenza esplicita dell'utente, come dichiarato nella story stessa) e un futuro scambio d'ordine invaliderebbe silenziosamente il commento e la regola CSS. Trovato dal Blind Hunter. Deferred: nessun impatto attuale, nessun test possibile per questo tipo di invariante visivo in questo progetto. [app/(gruppi-allenatori)/gruppi/gruppi.module.css]
- [x] [Review][Defer] Le etichette "Atlete:"/"Allenatori:" sono `<span>` semplici, non associate semanticamente (`aria-labelledby`/`<label>`) all'elenco/form che seguono — utenti di screen reader ricevono una cella con lista e form non etichettati. Trovato dal Blind Hunter. Deferred: gap già pre-esistente per Allenatori (spedito prima di questa storia), questa storia lo replica invariato per le Atlete invece di introdurlo — un miglioramento di accessibilità consolidato per entrambe le righe è più adatto a una story dedicata. [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx]

### Review Findings — estensione round 3 (2026-08-06)

Code review con 3 layer adversariali paralleli (Blind Hunter, Edge Case Hunter, Acceptance Auditor) sul diff non committato del round 3 (`git diff HEAD`).

- [x] [Review][Decision] Asimmetria silenziosa tra Ruoli: Admin/Dirigente su `/gruppi` ora vedono il badge "Certificato scaduto" per singola Atleta, mentre l'Allenatore su `/i-miei-gruppi` (stesso dato, `certificatoScaduto` propagato anche al suo tipo `Atleta`) continua a vedere solo "in scadenza". Trovato dal Blind Hunter. **Risolto con l'utente**: resta solo su `/gruppi`, deliberatamente fuori scope per `/i-miei-gruppi` — nessuna richiesta dell'utente ha mai menzionato quella pagina in questo round.

- [x] [Review][Patch] Il nuovo badge "Certificato scaduto" in `/gruppi` usa la variante colore **danger**, violando la regola esplicitamente segnata "**non negoziabile**" in DESIGN.md (`Componenti` → Badge di stato): il badge di stato per singola Atleta deve usare la variante **warning**, non danger — l'unica eccezione documentata è `/conferma-certificati` (Story 9.23), con nota esplicita "non estendere questa eccezione ad altre pagine senza una nuova decisione esplicita". Trovato dal Blind Hunter. Corretto: rimossa `.badgeScaduto` (danger), "Certificato scaduto" ora riusa `.badge` (warning), stessa variante di "in scadenza". [app/(gruppi-allenatori)/gruppi/gruppi.module.css, AtletaTabellaRiga.tsx]
- [x] [Review][Patch] Il nuovo `<th></th>` vuoto per la colonna "Rimuovi" nella tabella Atlete non ha nome accessibile per screen reader. Trovato indipendentemente da Blind Hunter ed Edge Case Hunter. Corretto: testo visibile "Rimuovi" invece di header vuoto. [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx:237]
- [x] [Review][Patch] Nessun test copre il confine esatto giorni=0 (scadenza proprio oggi) tra SCADUTO e IN_SCADENZA per `certificatoScaduto`/`certificatoInScadenza`. Trovato dall'Edge Case Hunter. Corretto: nuovo test aggiunto. [lib/certificato-in-scadenza-per-atleta.test.ts]
- [x] [Review][Patch] AC #2 di questa story resta testualmente "tabella a 2 colonne (Nome | Rimuovi)" anche dopo che il round 3 l'ha estesa a 3 colonne (AC #3 lo documenta, AC #2 no) — incoerenza interna del documento. Trovato dall'Acceptance Auditor. Corretto: AC #2 aggiornato. [story file, AC #2]
- [x] [Review][Patch] Il commento in `AtletaTabellaRiga.tsx` dichiara "Nome Cognome" come ordine della colonna, ma il campo `atleta.nome` memorizza in realtà "Cognome Nome" (convenzione pre-esistente dell'anagrafica Atleta, non introdotta da questo round) — commento fuorviante. Trovato dall'Acceptance Auditor. Corretto: commento riformulato, dato sottostante non toccato (fuori scope). [app/(gruppi-allenatori)/gruppi/AtletaTabellaRiga.tsx]

- [x] [Review][Defer] Duplicazione del type predicate `{id, nome, certificatoInScadenza, certificatoScaduto}` tra `gruppi/page.tsx` e `i-miei-gruppi/page.tsx` — pattern di duplicazione già pre-esistente (i due predicate erano già duplicati identici prima di questo round), questo round lo estende con un terzo campo invece di introdurlo. Trovato dal Blind Hunter. Deferred: refactor verso un tipo condiviso (`ReturnType<typeof calcolaAtleteConCertificatoInScadenza>[number]`) più adatto a una story dedicata che tocchi entrambi i file per intero, non un fix isolato in un'estensione già di per sé ampia. — deferred, pre-existing. [app/(gruppi-allenatori)/gruppi/page.tsx, app/(gruppi-allenatori)/i-miei-gruppi/page.tsx]

**Dismessi come rumore/convenzioni già accettate/decisioni già prese (6)**: nessun test di rendering copre la logica del badge/ternario in `AtletaTabellaRiga.tsx` o l'allineamento thead/tbody — convenzione "nessun test di rendering per componenti-riga" già accettata in tutto il progetto (Blind Hunter); nessun `scope="col"` sui nuovi `<th>` — gap trasversale pre-esistente in ogni tabella del progetto, nessuna tabella esistente lo usa, introdurlo solo qui creerebbe un'inconsistenza stilistica isolata invece di un miglioramento sistemico (Blind Hunter, stesso trattamento già dato a gap di accessibilità analoghi nelle review precedenti di questa storia); classe CSS badge duplicata una terza volta — diventato non applicabile dopo il patch che rimuove la variante danger appena introdotta (Blind Hunter); differenza di formattazione (`window.confirm` su una riga in `AtletaTabellaRiga.tsx` vs multi-riga in `AtletaAssegnata.tsx`) — puramente cosmetico, nessun impatto funzionale (Blind Hunter); `i-miei-gruppi/page.tsx` calcola `certificatoScaduto` senza consumarlo — già documentato esplicitamente come scelta deliberata di scope nel File List/Change Log di questa stessa estensione (Blind Hunter); changelog che riporta "969/969 test passati" senza specificare che i 3 nuovi test coprono solo il livello helper — verrà comunque riformulato nel changelog di chiusura di questo round di review (Blind Hunter).

**Dismessi come rumore/convenzioni già accettate/decisioni già prese (7)**: riordino del paragrafo di errore nel form "Assegna Atleta" (dopo il pulsante invece che prima) non esplicitamente dichiarato nei Task — in realtà intenzionale, allinea l'ordine a quello già esistente del form Allenatori (Dev Notes: "mirror esatto della riga Allenatori"), nessun impatto funzionale, osservazione indipendente anche dell'Acceptance Auditor come "benigna"; mismatch di scala nel riuso del pattern a chip per rose numerose (10-20 Atlete) — già discusso e deliberato nello Studio di usabilità della story stessa, l'utente ha scelto esplicitamente l'opzione senza collassamento sapendo che le rose possono essere numerose; nessuna copertura automatica/test di rendering per `GruppoRow.tsx` — convenzione già accettata in tutto il progetto per componenti-riga analoghi; mismatch semantico thead/tbody (2 header, 3 tipi di riga) — gap già pre-esistente per Allenatori, non introdotto da questa storia; story segnata "review" nonostante un AC visivo non verificabile dal vivo in questo sandbox — limite già dichiarato esplicitamente nelle Completion Notes, stesso trattamento trasparente già usato per ogni storia di questa sessione con lo stesso limite; commento che cita "Story 9.33" per una parte e "senza story formale" per l'altra nello stesso blocco — chiarezza sufficiente, non un'ambiguità reale; nessun guard strutturale sul rischio `colSpan` oltre alla disciplina dello sviluppatore — stesso limite di "nessun test di rendering" già accettato sopra, lo stesso rischio esiste già per `rigaAllenatori` da prima di questa storia.

### Review Findings — estensione round 4 (2026-08-07)

Code review con 3 layer adversariali paralleli (Blind Hunter, Edge Case Hunter, Acceptance Auditor) sul diff non committato del round 4 (`git diff HEAD`).

- [x] [Review][Patch] La scelta di usare la variante **danger** per i badge "Non iscritta"/"Non tesserata" — già confermata esplicitamente con l'utente prima dell'implementazione — non è mai stata scritta in DESIGN.md, che resta l'unica fonte di verità del design system: il commento nel codice cita una "regola non negoziabile" e ne rivendica l'esenzione, ma senza una voce in DESIGN.md un futuro contributor che legga solo quel documento (in particolare la tabella "Cose da fare e da evitare") leggerebbe questo come una violazione, esattamente come già avvenuto una volta in questo stesso round (round 3, prima del fix). Trovato indipendentemente da Blind Hunter e Acceptance Auditor — convergenza tra due layer. Corretto: aggiunta una "Seconda eccezione esplicita" in `Componenti → Badge di stato` (mirror della Story 9.23) e aggiornata la tabella "Cose da fare e da evitare". [DESIGN.md]
- [x] [Review][Patch] `.badgeSuccesso`/`.badgeDanger` duplicano quasi verbatim le proprietà di `.badge` (stesso `flex-shrink`/`font-size`/`font-weight`/`padding`/`border-radius`/`white-space`, cambia solo `background`/`color`) — a differenza della duplicazione *cross-modulo* già accettata come convenzione del progetto, qui sono 3 blocchi quasi identici nello **stesso file**. Trovato dal Blind Hunter. Corretto: proprietà comuni consolidate in un unico selettore raggruppato, solo `background`/`color` restano per variante — stesso comportamento visivo. [app/(gruppi-allenatori)/gruppi/gruppi.module.css]

- [x] [Review][Defer] Nessun test copre la nuova logica di composizione `idAtleteIscritte`/`idAtleteTesserate` (due `Set` di `atletaId` mappati su `atleteGruppo`) in `page.tsx`. Trovato dal Blind Hunter. Deferred: coerente con la convenzione già accettata in tutto il progetto di non testare la logica di composizione dati inline in un Server Component pagina (stesso limite già presente per `atleteGruppo`/`gruppoAtleteRows` prima di questo round) — non una funzione pura estraibile come `calcolaAtleteConCertificatoInScadenza`. — deferred, consistente con convenzione esistente. [app/(gruppi-allenatori)/gruppi/page.tsx]
- [x] [Review][Defer] `Tesseramento` non ha un campo `attiva`/di esclusione (a differenza di `Iscrizione`) — una volta confermato, non esiste un modo nell'app per "detesserare" un'Atleta se il Tesseramento è stato confermato per errore, se non intervenendo direttamente sul DB. Trovato dal Blind Hunter. Deferred: gap del modello dati pre-esistente (schema.prisma, non toccato da questo round), la nuova colonna Tesseramento lo rende solo più visibile in UI, non lo introduce — fuori scope per un round di sola presentazione. — deferred, pre-existing. [prisma/schema.prisma]

**Dismessi come rumore/decisioni già deliberate col mockup approvato (8)**: badge rossi che potrebbero colorare gran parte della tabella durante l'onboarding stagionale (prima che Iscrizioni/Tesseramenti siano processati per la maggior parte delle Atlete) — il mockup mostrava esplicitamente questa combinazione (riga "Neri Elena" con entrambi i badge rossi) ed è stato approvato così com'è (Blind Hunter); il round 4 riallarga la tabella Atlete che la story originale voleva restringere, spingendo di nuovo verso lo scroll orizzontale — esito esplicito e visibile nel mockup approvato, l'utente ha visto e scelto questo layout consapevolmente (Blind Hunter); incoerenza visiva tra Certificato (badge condizionale, solo se problema) e Iscrizione/Tesseramento (badge sempre presente) nella stessa riga — combinazione mostrata esplicitamente nel mockup e approvata (Blind Hunter); incoerenza cross-pagina con `/conferma-iscrizioni`/`/conferma-tesseramenti` (testo semplice, non badge) per lo stesso dato — segnalata esplicitamente all'utente nel mockup stesso ("introduce però una nuova convenzione... testo semplice, mai badge") e scelta consapevolmente (Blind Hunter); presunto aggravamento della duplicazione del type predicate tra `gruppi/page.tsx`/`i-miei-gruppi/page.tsx` già deferita nel round 3 — verificato non accurato: `iscritta`/`tesserata` sono aggiunte con un `.map()` **dopo** quel predicate, che resta invariato in questo round esattamente per non ripetere il problema (Blind Hunter); una nuova Atleta creata dal pannello "Nuovo Atleta" mostra subito due badge rossi — comportamento accurato e atteso, stessa combinazione già mostrata nel mockup approvato, nessuna richiesta di attenuarlo (Blind Hunter); combinazione `tesserata=true`+`iscritta=false` mostrata senza segnalarla come "incoerente" — verificato in `conferma-tesseramenti/actions.ts` che la dipendenza Iscrizione→Tesseramento è stata rimossa in una storia precedente, quindi è una combinazione normale e raggiungibile, non un bug: i due stati sono richiesti come colonne indipendenti, non come un'unica colonna combinata (Edge Case Hunter); lettura concorrente di Iscrizioni (RLS/Supabase) e Tesseramenti (Prisma) via `Promise.all` senza uno snapshot transazionale unico — stesso pattern già pre-esistente e accettato in tutto il resto della pagina (gruppi/allenatori/atlete/certificati) e mutuato identico da `conferma-tesseramenti/page.tsx`, non introdotto da questo round (Edge Case Hunter).

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5

### Debug Log References

Nessuno — nessuna migrazione, nessun accesso DB necessario, nessuna Server Action toccata.

### Completion Notes List

- Implementate tutte le 4 Task/5 AC della story: `rigaPrincipale` ridotta a Nome/Categoria, nuova `rigaAtlete` (mirror di `rigaAllenatori`, elenco orizzontale via `.listaAssegnatiInline`, form "Assegna Atleta" in linea, toggle "Nuovo Atleta" spostato invariato), `colSpan` di entrambe le righe estese aggiornato a `2`, header tabella aggiornato.
- Ordine delle righe scelto: Atlete prima di Allenatori (nessuna preferenza esplicita dell'utente, lasciata allo sviluppatore per la story).
- CSS: rinominata `.etichettaAllenatori` in `.etichettaRigaEstesa` (riusata da entrambe le righe invece di duplicare la regola), rimossa `.listaAssegnati`/`.listaAssegnati li` da `gruppi.module.css` (dead code dopo questa storia — nessun altro consumer nello stesso modulo, verificato; `i-miei-gruppi.module.css` ha una propria definizione indipendente, non impattata).
- Verifica dal vivo (aspetto visivo reale) non eseguibile in questo sandbox — stesso limite già incontrato più volte in questa sessione (motore Prisma WASM non caricabile da Node sotto `next dev`). Verificato tutto il resto: 966/966 test Vitest passati (invariato, nessun test di rendering per `GruppoRow.tsx`), `eslint`/`tsc --noEmit` puliti, `npm run build` riuscita.

### File List

**Nuovi (estensione 2026-08-06):**
- `app/(gruppi-allenatori)/gruppi/AtletaTabellaRiga.tsx`

**Modificati:**
- `app/(gruppi-allenatori)/gruppi/GruppoRow.tsx` (Atlete spostate dalla colonna a nuova `rigaAtlete`, `colSpan` di `rigaAllenatori` aggiornato, pannello "Nuova Atleta" raggruppato in `.pannelloNuovaAtleta`; estensione: elenco Atlete da `<ul>`/chip a `<table>`/`AtletaTabellaRiga`; estensione round 3: form Assegna Atleta / pulsante Nuovo Atleta / tabella Atlete impilati su righe visive separate, `<thead>` aggiunto alla tabella Atlete — 3° `<th>` con testo visibile "Rimuovi" dopo il review fix, non vuoto)
- `app/(gruppi-allenatori)/gruppi/page.tsx` (header tabella: rimossa colonna "Atlete"; estensione round 3: type predicate esteso con `certificatoScaduto`)
- `app/(gruppi-allenatori)/gruppi/gruppi.module.css` (`.etichettaAllenatori` → `.etichettaRigaEstesa`, `.rigaAtlete` aggiunta, `.listaAssegnati`/`.listaAssegnati li` rimosse, commento intestazione corretto; estensione: `.tabellaAtlete`/`.tabellaAtlete td` aggiunte; estensione round 3: `.rigaAtlete td` separata da `.rigaAllenatori td` e passata a `flex-direction:column`, `.tabellaAtlete th` aggiunta; review fix round 3: `.badgeScaduto` (danger) rimossa dopo il fix — "Certificato scaduto" riusa `.badge` esistente, warning, per non violare la regola non negoziabile di DESIGN.md)

**Modificati (estensione 2026-08-06, round 3 — nuova colonna Certificato):**
- `lib/certificato-in-scadenza-per-atleta.ts` (aggiunto `certificatoScaduto: boolean` al ritorno, stesso `categorizzaStatoCertificato` già in uso, nessun nuovo calcolo)
- `lib/certificato-in-scadenza-per-atleta.test.ts` (3 nuovi test per `certificatoScaduto`; review fix: +1 test di confine su giorni=0)
- `app/(gruppi-allenatori)/gruppi/AtletaAssegnata.tsx` (tipo `Atleta` esteso con `certificatoScaduto: boolean`; rendering del componente invariato — mostra ancora solo il badge "in scadenza", usato anche da `/i-miei-gruppi` fuori scope, confermato con l'utente in code review)
- `app/(gruppi-allenatori)/gruppi/AtletaTabellaRiga.tsx` (da 2 a 3 colonne: Nome / Certificato (badge, stessa variante warning per scaduto e in scadenza dopo il review fix) / Rimuovi)
- `app/(gruppi-allenatori)/i-miei-gruppi/page.tsx` (type predicate esteso con `certificatoScaduto` per compatibilità col tipo `Atleta` condiviso, nessun cambiamento di comportamento — quella pagina non consuma il nuovo campo)

**Modificati (estensione 2026-08-07, round 4 — colonne Iscrizione/Tesseramento, precedute da un mockup HTML/CSS approvato dall'utente prima dell'implementazione):**
- `app/(gruppi-allenatori)/gruppi/AtletaTabellaRiga.tsx` (nuovo tipo esportato `AtletaConStato = Atleta & { iscritta: boolean; tesserata: boolean }` — deliberatamente **non** aggiunto al tipo `Atleta` condiviso di `AtletaAssegnata.tsx`, per non ripetere la duplicazione di tipo già segnalata come defer nella code review del round 3; da 3 a 5 colonne: Nome / Certificato / Iscrizione / Tesseramento / Rimuovi, badge verde/rosso su Iscrizione/Tesseramento, sempre presente — non condizionale come il Certificato)
- `app/(gruppi-allenatori)/gruppi/GruppoRow.tsx` (tipo `Gruppo.atlete` da `Atleta[]` a `AtletaConStato[]`; `<thead>` della tabella Atlete esteso con `Iscrizione`/`Tesseramento`)
- `app/(gruppi-allenatori)/gruppi/page.tsx` (nuove letture in `Promise.all`: `elencaIscrizioniPerAnno` — RLS, stesso helper già usato da `conferma-tesseramenti/page.tsx` — e `prisma.tesseramento.findMany` diretto, non RLS-protetta per AD-9, stesso pattern di quella stessa pagina; `atleteGruppo` arricchita con `iscritta`/`tesserata` via due `Set` di `atletaId`)
- `app/(gruppi-allenatori)/gruppi/gruppi.module.css` (nuove classi `.badgeSuccesso`/`.badgeDanger` — non riusa `.badge`, che resta la sola variante warning per il Certificato; danger su singola Atleta qui è stato confermato esplicitamente dall'utente e non riguarda la regola non negoziabile di DESIGN.md, specifica del badge "Certificato scaduto")
- `AtletaAssegnata.tsx`, `i-miei-gruppi/page.tsx`, `lib/db-rls/iscrizione.ts`: **nessuna modifica** — Iscrizione/Tesseramento restano fuori scope per `/i-miei-gruppi`, `elencaIscrizioniPerAnno` riusata invariata
- `_bmad-output/planning-artifacts/ux-designs/ux-societa-manager-2026-07-22/DESIGN.md` (review fix: aggiunta "Seconda eccezione esplicita" in `Componenti → Badge di stato`, mirror della Story 9.23, per registrare la scelta danger su Iscrizione/Tesseramento; tabella "Cose da fare e da evitare" aggiornata)

## Change Log

- 2026-08-06: File di story creato (con studio di usabilità già svolto e decisione confermata dall'utente), stato ready-for-dev.
- 2026-08-06: Story implementata (Task 1-4 completi). Atlete spostate dalla colonna della riga principale a una nuova riga distinta a piena larghezza con elenco orizzontale, mirror esatto del trattamento già in produzione per Allenatori. `colSpan` di entrambe le righe estese aggiornato da 3 a 2 (riga principale ora a 2 colonne). CSS consolidato (`.etichettaRigaEstesa` condivisa, `.listaAssegnati` rimossa come dead code). 966/966 test Vitest passati, 0 errori tsc/eslint, build produzione riuscita. Status: review, in attesa di code review adversariale.
- 2026-08-06: Code review completata (3 layer adversariali paralleli: Blind Hunter, Edge Case Hunter, Acceptance Auditor). Acceptance Auditor: nessuna violazione degli AC (verificato indipendentemente anche il punto specifico sul `colSpan` di entrambe le righe estese, e che nessuna forma di collassamento scartata nello studio di usabilità sia stata implementata per errore). 2 patch applicati: il pannello "Nuova Atleta" (separatore + form) sarebbe diventato due elementi flessibili slegati invece di un blocco raggruppato una volta che il `<td>` contenitore è diventato `display:flex` — corretto con un nuovo contenitore `.pannelloNuovaAtleta`; commento di intestazione CSS obsoleto (citava ancora `.listaAssegnati`, rimossa come dead code dalla stessa storia) — corretto. 4 defer (form "Assegna" senza `flex-wrap` per errori lunghi — gap già pre-esistente per Allenatori; errore inline per-riga di `AtletaAssegnata.tsx` che potrebbe spostare i chip vicini — raro e cosmetico; commento CSS fragile legato a un ordine di righe arbitrario; etichette "Atlete:"/"Allenatori:" senza associazione semantica/aria — gap già pre-esistente per Allenatori) — tutti coerenti con limiti/decisioni già accettati in questo progetto o già deliberati nello Studio di usabilità della story stessa, vedi `deferred-work.md`. 7 dismessi come rumore/convenzioni già accettate/decisioni già prese. 966/966 test Vitest passati, 0 errori tsc/eslint dopo i fix, build produzione riuscita. Status: done.
- 2026-08-06: **Estensione post-done**, richiesta esplicita dell'utente dopo aver rivisto mentalmente il risultato dell'AC #2 originale (elenco Atlete in orizzontale a chip): "non mi piace la visualizzazione delle atlete... vorrei vederle a forma tabellare con nome cognome su una colonna e il pulsante rimuovi sull'altra". AC #2 di fatto ribaltato **solo per le Atlete** (Allenatori restano invariati, ancora a chip orizzontali — non menzionati nella richiesta). Nuovo componente `AtletaTabellaRiga.tsx` (`<tr>`-based, 2 `<td>`: Nome+badge scadenza / form Rimuovi) — **non** riusato/modificato `AtletaAssegnata.tsx` esistente (rimasto `<li>`-based, invariato) perché condiviso con `MioGruppoCard.tsx` (`/i-miei-gruppi`, fuori scope di questa richiesta) - un `<tr>` dentro il suo `<ul>` avrebbe rotto l'HTML. `GruppoRow.tsx`: l'elenco Atlete ora è una `<table>` nidificata (`.tabellaAtlete`, resa solo se `gruppo.atlete.length > 0`) invece di `<ul className={styles.listaAssegnatiInline}>`. CSS: nuove regole `.tabellaAtlete`/`.tabellaAtlete td` (dichiarate dopo `.tabella td` per vincere a parità di specificità, altrimenti il bordo/padding della tabella Gruppi esterna si applicherebbe anche a queste celle nidificate). 966/966 test Vitest passati (invariato, nessun test di rendering), 0 errori tsc/eslint, build produzione riuscita. Verifica dal vivo non eseguibile in questo sandbox (stesso limite noto). Status invariato: done.
- 2026-08-06: **Estensione post-done (round 3)**, richiesta esplicita dell'utente dopo aver rivisto il risultato del round precedente: "non ci siamo ancora. voglio nome campionato e gruppo in testa come è adesso, poi riga di assegna atlete con dropdown + pulsante assegna a fianco. riga successiva pulsante nuovo atleta. righe successive, lista atlete dove per ogni riga voglio 'nome cognome' colonna certificati in caso sia scaduto o in scadeza e colonna con pulsante rimuovi." Interpretato "campionato" come refuso per "categoria" (la riga principale di `/gruppi` mostra Nome/Categoria, non Campionato — nessuna colonna Campionato esiste in questa pagina), lasciata invariata come richiesto. Implementato letteralmente: (1) form "Assegna Atleta" e (2) pulsante/pannello "Nuovo Atleta" ora impilati ciascuno sulla propria riga visiva (`.rigaAtlete td` passata da `flex-wrap` a `flex-direction:column`, separata dalla regola condivisa con `.rigaAllenatori td` che resta invece a chip orizzontali), seguiti da (3) la tabella Atlete con una nuova terza colonna "Certificato" (badge "Certificato scaduto" con priorità sul badge "Certificato in scadenza" esistente, nessun badge per gli altri stati). Nuovo campo `certificatoScaduto: boolean` aggiunto a `lib/certificato-in-scadenza-per-atleta.ts` (riusa `categorizzaStatoCertificato` già esistente, nessun nuovo calcolo di soglia) e propagato al tipo condiviso `Atleta` (`AtletaAssegnata.tsx`) e ai type predicate di `gruppi/page.tsx`/`i-miei-gruppi/page.tsx` — `i-miei-gruppi`/`MioGruppoCard.tsx` ricevono il campo per compatibilità di tipo ma non lo consumano (fuori scope, non richiesto, confermato con l'utente in code review). `<thead>` aggiunto alla tabella Atlete nidificata (Nome/Certificato/Rimuovi), assente nel round precedente. 969/969 test Vitest passati (3 nuovi per `certificatoScaduto`), 0 errori tsc/eslint, build produzione riuscita. Verifica dal vivo non eseguibile in questo sandbox (stesso limite noto, disclosure ripetuta). Status: review, in attesa di code review adversariale.
- 2026-08-06: Code review completata sul round 3 (3 layer adversariali paralleli: Blind Hunter, Edge Case Hunter, Acceptance Auditor). 1 decision risolta con l'utente: l'asimmetria per cui `/gruppi` mostra il badge "scaduto" e `/i-miei-gruppi` no, confermata deliberatamente fuori scope. 5 patch applicati: (1) **violazione reale di una regola "non negoziabile" di DESIGN.md** — il badge "Certificato scaduto" era stato introdotto con la variante danger, ma DESIGN.md riserva danger a singola Atleta solo a `/conferma-certificati` (Story 9.23) e vieta esplicitamente di estendere l'eccezione altrove senza una nuova decisione — corretto riusando la variante warning esistente (`.badge`), rimossa la classe `.badgeScaduto`; (2) `<th></th>` vuoto per la colonna Rimuovi senza nome accessibile — corretto con testo visibile "Rimuovi"; (3) test mancante sul confine esatto giorni=0 tra SCADUTO/IN_SCADENZA — aggiunto; (4) AC #2 rimasto disallineato a "2 colonne" dopo che il round 3 era già passato a 3 — corretto; (5) commento che dichiarava erroneamente l'ordine "Nome Cognome" (il dato è in realtà "Cognome Nome", convenzione pre-esistente non toccata) — riformulato. 1 defer (duplicazione del type predicate tra `gruppi/page.tsx`/`i-miei-gruppi/page.tsx`, pattern pre-esistente esteso non introdotto da questo round). 6 dismessi come rumore/convenzioni già accettate. 970/970 test Vitest passati (era 969, +1 test di confine), 0 errori tsc/eslint dopo i fix, build produzione riuscita. Status invariato: done.
- 2026-08-07: **Estensione post-done (round 4)**, richiesta esplicita dell'utente: "sempre stessa funzionalità vedo gli atleti su righe ma anche lo stato e i pulsanti voglio avere la riga composta da atleta, stato certificato, stato iscrizione, stato tesseramento, pulsante rimuovi a destra ma tutto sulla stessa riga. prima di finalizzare fammi vedere un esempio." Prima di scrivere codice, pubblicato un mockup HTML/CSS statico (fedele a token/colori/font reali di `gruppi.module.css`/`globals.css`) come Artifact per farlo approvare — l'utente ha chiesto badge colorati anche per Iscrizione/Tesseramento (non solo testo semplice come nelle pagine dedicate `/conferma-iscrizioni`/`/conferma-tesseramenti`), confermando esplicitamente il colore danger per "Non iscritta"/"Non tesserata" su singola Atleta (non viola la regola non negoziabile di DESIGN.md, specifica del badge "Certificato scaduto"). Il primo mockup aveva un difetto (le 5 informazioni della riga apparivano spezzate su più righe invece che affiancate) causato dal reset CSS iniettato dall'ambiente artifact su una vera `<table>` — risolto ricostruendo la sezione con CSS grid (`display:contents` sulle righe) invece di `<table>`/`<tr>`/`<td>`, un problema del solo mockup, non del codice reale (che continua a usare una vera `<table>`, come tutto il resto del progetto). Implementazione: tabella Atlete da 3 a 5 colonne (Nome | Certificato | Iscrizione | Tesseramento | Rimuovi). Nuovo tipo `AtletaConStato` (in `AtletaTabellaRiga.tsx`, non nel tipo `Atleta` condiviso) per evitare di ripetere la duplicazione di tipo già segnalata come defer nel round 3. `page.tsx`: nuove letture Iscrizione (RLS, `elencaIscrizioniPerAnno`, stesso helper già usato da `conferma-tesseramenti/page.tsx`) e Tesseramento (Prisma diretto, non RLS-protetta per AD-9, stesso pattern di quella pagina), arricchimento `atleteGruppo` con `iscritta`/`tesserata` via due `Set`. Nuove classi CSS `.badgeSuccesso`/`.badgeDanger` (non riusa `.badge`, riservata al solo Certificato). Nessuna Server Action toccata, nessuna migrazione. 970/970 test Vitest passati (invariato, nessun nuovo test — nessuna nuova logica pura da testare, solo lettura/composizione dati e presentazione), 0 errori tsc/eslint, build produzione riuscita. Verifica dal vivo non eseguibile in questo sandbox (stesso limite noto). Status: review, in attesa di code review adversariale.
- 2026-08-07: Code review completata sul round 4 (3 layer adversariali paralleli: Blind Hunter, Edge Case Hunter, Acceptance Auditor). Nessun `decision-needed` (la scelta danger era già stata confermata con l'utente prima dell'implementazione, via mockup). 2 patch applicati, **convergenza indipendente di 2 layer su 3** sullo stesso punto: la scelta danger per Iscrizione/Tesseramento non era mai stata scritta in DESIGN.md, che resta l'unica fonte di verità — corretto aggiungendo una "Seconda eccezione esplicita" in `Componenti → Badge di stato` (mirror della Story 9.23) e aggiornando la tabella "Cose da fare e da evitare"; `.badgeSuccesso`/`.badgeDanger` duplicavano quasi verbatim `.badge` nello stesso file — consolidate in un unico selettore raggruppato, solo `background`/`color` restano per variante. 2 defer (nessun test per la nuova logica di composizione `idAtleteIscritte`/`idAtleteTesserate` — coerente con la convenzione già accettata di non testare composizione dati inline in un Server Component pagina; `Tesseramento` senza campo `attiva`/esclusione, nessun modo in-app per "detesserare" un'Atleta — gap del modello dati pre-esistente, non introdotto da questo round). 8 dismessi: quasi tutti riconducibili allo stesso motivo — erano scelte di design già mostrate esplicitamente nel mockup e approvate consapevolmente dall'utente (righe rosse durante l'onboarding, riallargamento della tabella, incoerenza tra badge condizionale/sempre-presente nella stessa riga, incoerenza cross-pagina con testo semplice), più 2 letture d'edge-case rivelatesi comportamento corretto/pattern pre-esistente non aggravato da questo round. 970/970 test Vitest passati (invariato), 0 errori tsc/eslint dopo i fix, build produzione riuscita. Status invariato: done.
