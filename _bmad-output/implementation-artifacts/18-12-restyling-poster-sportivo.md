---
baseline_commit: a957215
---

# Story 18.12: Applicazione del registro visivo "Poster Sportivo" alle pagine pubbliche esistenti

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Visitatore senza account,
I want che l'intero sito pubblico (Home e Squadre) abbia un registro visivo coerente ed energico,
so that percepisca l'identità del Settore Volley fin dal primo sguardo, non solo un layout funzionale spoglio.

## Acceptance Criteria

1. **Given** un Visitatore **When** visita qualunque pagina pubblica esistente (`/`, `/squadre`) **Then** vede i colori, la tipografia e le forme definiti in `DESIGN.md` (bianco/azzurro prevalenti, blocchi quasi-neri solo su header/hero/match-card/footer, tagli diagonali `clip-path`, tipografia condensata peso 900 su titoli/nav/pulsanti).
2. **And** ogni sezione condizionale della home (Sponsor, Partite della settimana, Foto squadra, Post social) mantiene esattamente le stesse regole di visibilità/assenza già validate nelle rispettive story (18.2 AC#2, 18.3 AC#2, 18.4 AC#3, 18.5 AC#3) — il restyling non introduce né rimuove alcuna condizione.
3. **And** la navigazione pubblica resta un elenco orizzontale con wrap su schermi stretti, senza alcun drawer/hamburger — solo colori/tipografia aggiornati (`EXPERIENCE.md` → Responsive & Piattaforma, nota critica).
4. **And** ogni elemento interattivo (voci di nav, pulsante primario, azioni del banner cookie, icone social, link Accedi) ha un'area di tocco minima 44×44px e un contorno di focus visibile alla navigazione da tastiera, secondo i token `focus-*` di `DESIGN.md`.
5. **And** le foto di squadra non ancora caricate (Story 18.4) mostrano il trattamento placeholder intenzionale (`DESIGN.md` → `placeholder-foto`), non un'area vuota o un'icona rotta.
6. **And** tutte le coppie di contrasto testo/sfondo usate nel restyling rispettano i valori calcolati in `DESIGN.md → Colori` (nessuna combinazione nuova introdotta senza verifica).
7. **And** nessun test esistente si rompe per il solo cambio di classi/stili CSS (i test che verificano contenuto/comportamento restano validi, quelli che verificano dettagli implementativi CSS vanno aggiornati di conseguenza).

## Tasks / Subtasks

- [ ] Task 1: Header/Nav — restyle a blocco `{colors.nero}` (AC: #1, #3, #4, #6)
  - [ ] `app/HeaderPubblico.module.css`: `.header` da `background: var(--color-surface)` (bianco) a `background: #0B0E14` (`{colors.nero}` — nessun corrispondente in `globals.css`, hex letterale commentato). `border-bottom` da `1px solid var(--color-border)` a `4px solid var(--color-primary)` (`{colors.azzurro}` `#00A3E0`, **eccezione**: stesso valore esadecimale già in `globals.css`, riusare il custom property esistente, non ripetere l'hex — vedi Dev Notes "Convenzione CSS"). Aggiungere `height: 76px` (`{spacing.header-height}`, nessun custom property equivalente, letterale).
  - [ ] `.nomeSettore`: colore da `var(--color-text-primary)` a `#FFFFFF` (`{colors.bianco}` — **eccezione**, stesso valore di `var(--color-surface)`, riusabile). Font-family/size/weight secondo `{typography.wordmark}` (`'Arial Black',sans-serif; 20px; 900; line-height:1`). Nessun cambio al markup (nessuna evidenziazione automatica della parola "VOLLEY" — `nomeSettore` è testo libero configurabile da Admin, non affidabile per un match di sottostringa; trattamento opzionale, non richiesto da alcun AC, vedi Dev Notes).
  - [ ] `.accedi`: **non** è il pulsante primario (`button-primary`) — è il trattamento distinto `header-nav.accedi-link-*` di `DESIGN.md`: rimuovere `background: var(--color-button-bg)` (riempimento pieno attuale) e sostituire con bordo `1px solid #333B47` (`{colors.bordo-scuro-forte}`, letterale), colore testo `#8891A0` (`{colors.testo-chiaro-debole}`, letterale, contrasto su nero verificato 6.08:1 in `DESIGN.md`), `border-radius: 2px` (`{rounded.sm}` — **attenzione**: `var(--radius-sm)` esistente vale `6px`, valore diverso, non riusabile, letterale). Hover: colore bianco, bordo azzurro (`var(--color-primary)`). Mantenere invariati `display:inline-flex; align-items:center; min-height:44px` (AC #4, nessuna regressione sul touch target).
  - [ ] `app/NavPubblica.module.css`: `.voce` colore da `var(--color-text-secondary)` a `#FFFFFF` (`{colors.bianco}`, eccezione riusabile `var(--color-surface)`), font-family/size/weight/letter-spacing secondo `{typography.nav-item}` (`'Arial Narrow',Arial,'Helvetica Neue',sans-serif; 14px; 900; letter-spacing:1px`). **Non** toccare `display:inline-flex; align-items:center; min-height:44px` (righe 29-31) — è la lezione già pagata nel progetto (memoria: "Touch target 44px richiede align-self:stretch"/min-height su un elemento non-flex non basta) e il commento sorgente lo dichiara esplicitamente; un restyle "solo colori" non deve toccare `display`/`min-height`.
  - [ ] `.voceAttiva`: sostituire `text-decoration: underline; text-underline-offset: 4px` con la sottolineatura persistente `{colors.azzurro}` 3px descritta in `DESIGN.md` (`border-bottom: 3px solid var(--color-primary)` è un'alternativa equivalente a `text-decoration` con controllo preciso dello spessore — usare `border-bottom`, non `text-decoration-thickness`, per lo spessore esatto 3px). **Non rimuovere** la regola `.voceAttiva:hover { color: var(--color-primary) }` (o l'hex equivalente) — è il review fix Story 18.7 che impedisce a `.voce:hover` (più specifico) di sovrascrivere lo stato attivo; va mantenuta con i nuovi colori, non eliminata.
  - [ ] Micro-animazione hover (`DESIGN.md` → Componenti, header-nav): sottolineatura che si espande da 0 a 100% larghezza, 250ms — implementabile con `::after` posizionato e `transition: width .25s ease` (stesso pattern del mockup, righe 94-102), oppure con `transform: scaleX()`. Solo su `.voce:hover`, non su `.voceAttiva` (che ha già la sottolineatura sempre visibile, nessuna animazione da applicare li').
  - [ ] Focus: sia `.accedi:focus-visible` (`HeaderPubblico.module.css`) sia `.voce:focus-visible` (`NavPubblica.module.css`) usano oggi `outline: 2px solid var(--color-focus-ring)` (`#006da6`) — **non riusabile**, `{colors.focus-ring}` di `DESIGN.md` vale `#0072A3`, valore diverso nonostante il nome identico (falso amico, vedi Dev Notes "Convenzione CSS"). Su sfondo `{colors.nero}` il token corretto è `{colors.focus-ring-chiaro}` (`#FFFFFF`, letterale) — sostituire entrambi.

- [ ] Task 2: Hero — restyle + CTA primario + placeholder foto (AC: #1, #4, #6)
  - [ ] `app/home-pubblica.module.css` `.hero`: `background` da `linear-gradient(135deg, var(--color-navy), var(--color-button-bg))` a `#0B0E14` (`{colors.nero}`, letterale) con `clip-path: polygon(0 0,100% 0,100% 92%,0 100%)` (taglio diagonale sul bordo inferiore). Aggiungere un livello `.heroDiagonale` (nuovo `<div>` posizionato assolutamente dentro `.hero`, dietro il contenuto testuale) con `background: linear-gradient(100deg, transparent 0%, rgba(0,163,224,0.85) 35%, rgba(49,38,130,0.9) 100%)` (innesto azzurro→navy, valori RGBA già equivalenti agli hex `{colors.azzurro}`/`{colors.navy}` con l'opacità specificata in `DESIGN.md`) e `clip-path: polygon(30% 0,100% 0,100% 100%,0% 100%)`, `right: -10%; width: 60%`.
  - [ ] `<h1>` (`app/page.tsx`, invariato nel testo "Benvenuti nel {nomeVisualizzato}"): tipografia `{typography.display-hero}` desktop (`'Arial Black','Arial Narrow',Impact,sans-serif; 68px; 900; line-height:0.92`), `{typography.display-hero-mobile}` sotto i 900px (44px), colore bianco (`var(--color-surface)`, eccezione riusabile), `text-shadow: 0 4px 24px rgba(0,0,0,0.5)` (pensata per restare leggibile sopra una foto reale futura, vedi `DESIGN.md` nota UX aperta — non bloccante, applicarla comunque per coerenza col token).
  - [ ] Padding: `{spacing.hero-padding-desktop}` (`64px 48px 88px`) / `{spacing.hero-padding-mobile}` (`24px 20px`) — nessun custom property equivalente, letterali (sostituiscono l'attuale `padding: var(--space-8) var(--space-4); min-height:60vh`).
  - [ ] **CTA primario nell'hero** (nuovo elemento, non presente oggi — vedi Dev Notes "Elementi nuovi richiesti da AC #4, non solo restyle"): un `<Link href="/squadre">` con classe `.heroCta`, testo "Scopri le squadre" (`EXPERIENCE.md` → Pattern dei Componenti, Hero), stile `button-primary` (vedi Task successivo per i token esatti, riusati identici). Richiede `import Link from "next/link"` in `app/page.tsx` (non ancora importato lì). Min-height 44px, focus-visible con `{colors.focus-ring-chiaro}` (sfondo azzurro pieno → per la combinazione specifica pulsante-su-hero-nero l'anello corretto è comunque `focus-ring-chiaro` bianco, il pulsante stesso è un blocco chiaro su sfondo nero).
  - [ ] **Foto placeholder nell'hero** (nuovo elemento — vedi `EXPERIENCE.md` → Fotografia Placeholder, "Hero: placeholder presumibilmente di lunga durata"): un `<div>` posizionato a piena area dietro il testo, `background: repeating-linear-gradient(45deg, #0B0E14, #0B0E14 14px, #1C2433 14px, #1C2433 28px)` (`{colors.nero}` alternato a `{colors.placeholder-hatch-alt}`, entrambi letterali), con una didascalia centrata (`::before` o `<span>`) `"[FOTO AZIONE]"` in maiuscolo, `{typography.label-tag}`, colore `rgba(255,255,255,0.5-0.55)`. Non bloccante per nessun AC numerato di questa storia (nessun AC lo richiede esplicitamente) ma coerente con AC #1 ("forme"/blocchi quasi-neri) — implementarlo per coerenza col registro, ometterlo non è un difetto bloccante se il tempo stringe.
  - [ ] Eyebrow badge (es. "Stagione 2026/27") e paragrafo lead evocativo (`{typography.body-lead}`, es. "Ogni punto è una famiglia che tifa", `EXPERIENCE.md` → Voce e Tono): **opzionali, fuori scope stretto** — sono nuovo copy testuale, non solo restyle di markup esistente, e nessun AC li richiede. Il badge stagione richiederebbe derivare un'etichetta da `AnnoAgonistico.dataInizio`/`dataFine` (nessun campo `nome`/etichetta esiste sul modello, verificato in `prisma/schema.prisma` riga 180) — se il dev agent li aggiunge, derivarli così, altrimenti ometterli senza che sia un difetto.

- [ ] Task 3: Sezione Sponsor — restyle `sponsor-strip` (AC: #1, #2, #6)
  - [ ] `.sezioneSponsor`/`.titoloSezione` (quando usata per "I nostri sponsor"/"Convenzioni"): `DESIGN.md` `components.sponsor-strip` definisce un'intestazione dedicata più piccola del resto (non `display-section`): `background: #FFFFFF` (`{colors.bianco}`, eccezione `var(--color-surface)`), `border-top`/`border-bottom: 1px solid #E5E9EE` (`{colors.bordo-chiaro}`, letterale — diverso da `var(--color-border)` `#bfe4f5`, non riusabile), intestazione `{typography.label-tag}` con `letter-spacing: 3px` (override esplicito, non il default 1.5px del token) e `color: #5B6472` (`{colors.grigio}`, letterale).
  - [ ] `.schedaSponsor`: nessun componente "sponsor-card" dedicato in `DESIGN.md` oltre al box-placeholder tratteggiato (per sponsor senza logo — stato che non esiste nel codice attuale, `SponsorPubblicoCard.tsx` renderizza sempre un `<img>`, nessun AC di questa storia introduce quello stato) — mantenere la card bianca con bordo sottile esistente, aggiornando solo `border-color` a `#E5E9EE` e `border-radius` a `0` (`{rounded.none}`, coerente con "Forme" di `DESIGN.md`: tagli diagonali/nessun radius tranne le 2 eccezioni esplicite — Accedi e icone social — che non includono le card).
  - [ ] Non introdurre il box tratteggiato "SPONSOR 0X" — nessun AC lo richiede e non esiste uno stato "sponsor senza logo" da rappresentare nel modello dati attuale.

- [ ] Task 4: Sezione Partite della settimana — restyle a `match-card` (AC: #1, #2, #6)
  - [ ] `.schedaPartita` → diventa una `match-card`: `background: #0B0E14` (`{colors.nero}`), `color: #FFFFFF` (`{colors.bianco}`), `clip-path: polygon(0 0,100% 0,100% 100%,4% 100%)` (taglio diagonale asimmetrico angolo in basso a sinistra), `border-radius: 0`. Triangolo d'accento azzurro (`::before`, `clip-path` triangolare, `opacity:0.15`, angolo in alto a destra) — dettaglio decorativo minore, implementarlo se rapido, altrimenti omettere senza che sia bloccante (stessa nota già usata in Story 18.9 per lo stesso componente).
  - [ ] `.gruppoPartita` (etichetta categoria/girone): `{typography.label-tag}` (`Arial,sans-serif; 11px; 700; letter-spacing:1.5px`), colore `var(--color-primary)` (`{colors.azzurro}`, eccezione). **Nota**: oggi questa etichetta ha uno sfondo pieno `var(--color-surface-alt)` (chip) — `DESIGN.md` per `match-card.category-tag` non prevede uno sfondo chip, solo testo colorato azzurro su sfondo nero della card; rimuovere lo sfondo chip per coerenza con `DESIGN.md` (nessun AC lo impone esplicitamente ma è la stessa etichetta semantica, "categoria/girone", del componente `match-card`).
  - [ ] `.squadrePartita`: `{typography.display-card}` (`'Arial Black','Arial Narrow',Impact,sans-serif; 22px; 900; line-height:1.15; letter-spacing:0.5px`), colore bianco.
  - [ ] `.luogoPartita`/`.dataPartita`: `{typography.meta}` (`Arial,sans-serif; 13px; 400; line-height:1.4`), colore `#AEB6C2` (`{colors.testo-chiaro-muto}`, letterale — nessun corrispondente in `globals.css`).
  - [ ] `.linkNaviga`: colore `var(--color-primary)` (verificato 6.73:1 su `#0B0E14`, stesso calcolo `DESIGN.md` → Colori) — mantenere `:focus-visible` ma con `outline: 2px solid #FFFFFF` (`{colors.focus-ring-chiaro}`, non `var(--color-focus-ring)` — stesso "falso amico" del Task 1).
  - [ ] `.listaPartite`: sostituire la griglia `auto-fill` attuale con `match-grid` a colonne fisse (`DESIGN.md` → Layout: "griglie CSS a colonne fisse... 2 colonne match"): `grid-template-columns: repeat(2, 1fr)` sopra i 900px, `1fr` (colonna singola) sotto — unico breakpoint esplicito di `EXPERIENCE.md` → Responsive & Piattaforma. `gap: 28px` (`{spacing.card-gap}`, letterale, non un multiplo pulito di 4px).
  - [ ] `.sezionePartite`/`.titoloSezione` ("Partite della settimana"): a differenza della sezione Sponsor (che ha un token dedicato più piccolo), nessun componente "sezione home" generico è definito esplicitamente in `DESIGN.md` per questo `<h2>` — **inferenza dello story-writer** (stesso principio già usato in Story 18.9 per `<h1>Calendario</h1>`): applicare `{typography.display-section}` (40px/900 desktop, `display-section-mobile` 22px/900 sotto i 900px), coerente col token esplicitamente assegnato a `social-embed.heading-typography`/`staff-list.heading-typography`/`contact-block.heading-typography` (tutti `display-section`) — stesso ruolo semantico di intestazione di sezione a piena pagina.

- [ ] Task 5: Sezione Foto squadra (teaser in home) — restyle, **nessun placeholder qui** (AC: #1, #2, #5, #6)
  - [ ] `.sezioneFotoSquadra`/`.titoloSezione`: stesso trattamento `{typography.display-section}` del Task 4 (stessa inferenza, stessa famiglia di sezioni home).
  - [ ] `.immagineFotoSquadra`: `clip-path` diagonale sul bordo inferiore (mirror del trattamento `team-card.photo` di `DESIGN.md`: `polygon(0 0,100% 0,100% 85%,0 100%)`), `border-radius: 0` al posto di `var(--radius-md)`.
  - [ ] `.nomeGruppoFoto`: `{typography.label-tag}` o `{typography.body}`, colore `var(--color-text-primary)` → verificare che sia sostituito con un token del nuovo sistema con contrasto verificato su sfondo bianco (es. `{colors.nero}` per testo su superficie chiara, valore diverso da `var(--color-text-primary)` `#101820` — letterale `#0B0E14`).
  - [ ] **Non aggiungere alcun placeholder per i Gruppi senza foto in questa sezione** — a differenza del Task 7 (pagina `/squadre`), questa è la "galleria parziale" descritta esplicitamente in `EXPERIENCE.md` → Pattern dei Componenti ("in Home, la sezione foto-squadra mostra **solo** i Gruppi che hanno caricato una foto — non è un elenco completo con placeholder per gli assenti"): la variabile `mostraFotoSquadra`/il filtro `gruppiConFoto` (`app/page.tsx` righe 231-234) **non va toccato**, resta identico. Vedi Dev Notes "Le due regole del placeholder-foto non vanno confuse" per la distinzione completa con AC #5.

- [ ] Task 6: Sezione Post social — restyle `social-embed` (AC: #1, #2, #6)
  - [ ] `.sezioneSocial`: `background: #F2F5F7` (`{colors.grigio-chiaro}`, letterale — non corrisponde a `var(--color-surface-alt)` `#e4f5fd`, non riusabile), `padding: {spacing.6}` (`24px` = `var(--space-6)`, riusabile).
  - [ ] `.titoloSezione` (qui, "Ultimi post"): `{typography.display-section}`/`display-section-mobile` — **esplicitamente citato in `DESIGN.md.components.social-embed.heading-typography`**, non un'inferenza qui (a differenza del Task 4/5).
  - [ ] `.iframeSocial`: nessun cambio dimensionale (width/height fissi già corretti in code review Story 18.5, non toccare) — solo eventuale `border`/contenitore esterno secondo il nuovo sfondo grigio-chiaro. Il `title` dell'iframe (già presente, "Ultimi post dalla nostra Pagina Facebook") resta invariato — soddisfa già il requisito `EXPERIENCE.md` → Soglia di Accessibilità, nessuna modifica necessaria.

- [ ] Task 7: Pagina Squadre — restyle `team-card` + placeholder foto per Gruppo senza foto (AC: #1, #5, #6)
  - [ ] `app/squadre/squadre.module.css` `.titolo` ("Squadre"): `{typography.display-section}`. `.main`: `background: #F2F5F7` (`{colors.grigio-chiaro}` — `DESIGN.md` → Colori: "sezione Squadre" ha sfondo grigio-chiaro dedicato, non bianco).
  - [ ] `.listaGruppi`: da griglia `auto-fill` a `team-grid` a colonne fisse — `repeat(3, 1fr)` sopra i 900px, colonna singola sotto (`DESIGN.md` → Layout: "3 colonne team"), `gap: 28px` (`{spacing.card-gap}`).
  - [ ] `.schedaGruppo` → `team-card`: `background: #FFFFFF` (bianco, eccezione `var(--color-surface)`), **nessun `border-radius`** (rimuovere `var(--radius-md)`), `box-shadow: 0 8px 24px rgba(11,14,20,0.08)` (unica ombra ammessa nel sistema, riservata alle superfici chiare — `DESIGN.md` → Elevazione), `transition: transform .25s ease` + `:hover { transform: translateY(-6px) }` (micro-animazione, non decorativa).
  - [ ] `.immagineGruppo` (quando `fotoInfo !== undefined`): altezza `260px` desktop / `140px` mobile (`{team-card.photo-height-desktop/-mobile}`), `clip-path: polygon(0 0,100% 0,100% 85%,0 100%)` (taglio diagonale sul bordo inferiore dell'area foto), `object-fit: cover` (già presente, non toccare).
  - [ ] `.nomeGruppo`: `{typography.display-card}` (22px/900). `.categoriaGruppo`: `{typography.label-tag}`, colore `#0072A3` (`{colors.azzurro-scuro}` — nessun corrispondente esatto in `globals.css`, `var(--color-primary-hover)` è `#0086bd`, valore diverso, non riusabile, letterale). `.listaAllenatori`: `{typography.body}` (`Arial,sans-serif; 14px; 400; line-height:1.8`).
  - [ ] **Placeholder foto per Gruppo senza foto caricata (AC #5, cambio di comportamento visivo intenzionale rispetto a oggi)**: in `app/squadre/page.tsx` (righe 97-103), oggi quando `fotoInfo === undefined` **nessun elemento immagine viene renderizzato affatto** (l'`<img>` è dentro `{fotoInfo !== undefined && (...)}`, nessun `else`). Questo va cambiato: aggiungere un ramo `else` che renderizza un `<div className={styles.placeholderFoto}>` con la stessa altezza/`clip-path` dell'immagine reale, `background: repeating-linear-gradient(-45deg, #0B0E14, #0B0E14 10px, #1C2433 10px, #1C2433 20px)` (`{colors.nero}`/`{colors.placeholder-hatch-alt}`, bande 10px) e una didascalia centrata `"[FOTO SQUADRA]"` (`{typography.label-tag}`, `rgba(255,255,255,0.5-0.55)`). **Questo è coerente con `DESIGN.md`/`EXPERIENCE.md`, non con Story 18.4 AC #3**: `EXPERIENCE.md` → Fotografia Placeholder è esplicito — il placeholder team-card è "**solo in fase di lancio**", scoped a `/squadre` (dove ogni Gruppo ha comunque una card, Story 18.8 AC #3), **distinto** dalla sezione "galleria" della home (Task 5 sopra, dove l'assenza di placeholder resta la regola permanente, Story 18.4 AC #3 letterale — quell'AC parla esplicitamente de "la home pubblica", non di `/squadre`). Nessun AC di Story 18.8 vieta un placeholder su `/squadre` — il comportamento attuale (nessuna immagine) era solo una scelta implementativa della Story 18.8, non un vincolo di un AC, quindi è legittimo cambiarlo qui.

- [ ] Task 8: Footer — restyle a blocco `{colors.nero}` + icona social opzionale (AC: #1, #4, #6)
  - [ ] `app/FooterPubblico.module.css` `.footer`: `background: #0B0E14` (`{colors.nero}`), `color: #B8C0CC` (`{colors.testo-chiaro-footer}`, letterale — diverso da `var(--color-text-secondary)` `#46586b` attuale). Tipografia paragrafo `{typography.body}`. Copyright: `{typography.meta}`, colore `#7B8697` (`{colors.testo-scuro-muto}`, letterale, contrasto verificato 5.24:1 in `DESIGN.md`).
  - [ ] `.footerConCookieBanner` (padding-bottom di sicurezza per il pulsante fisso "Preferenze cookie"): **non toccare la logica**, solo eventuali valori di colore ereditati — il calc `44px + var(--space-4) * 2` resta invariato (nessun AC lo riguarda).
  - [ ] **Icona social nel footer (elemento nuovo, richiesto letteralmente da AC #4)**: `DESIGN.md.components.footer` definisce icone social con hit-area 44px/focus dedicato, ed `EXPERIENCE.md` le elenca esplicitamente tra gli elementi da portare a 44px in questa storia — ma **nessuna icona social esiste oggi nel footer pubblico** (il footer attuale ha solo un paragrafo di copyright). Per soddisfare AC #4 senza introdurre nuovi dati/Server Action: `FooterPubblico.tsx` legge `leggiUrlPaginaFacebook()` (già esistente, `@/lib/configurazione-applicazione`, stessa funzione già usata da `app/page.tsx` per la sezione Post social, Story 18.5) e, se configurato, renderizza un link `<a href={urlPaginaFacebook} target="_blank" rel="noopener noreferrer">` come cerchio (`{rounded.full}`) con un'iniziale testuale (es. "F", nessuna libreria di icone — coerente con "nessun webfont" di `DESIGN.md`), `background: #1C2433` (`{colors.placeholder-hatch-alt}`) che diventa `var(--color-primary)` all'hover, **area cliccabile reale ≥44×44px anche se la resa visiva è 38px** (hit-area via padding, non l'icona stessa — stessa lezione già pagata nel progetto per il touch target, applicata qui a un elemento nuovo). Se `urlPaginaFacebook` non è configurato, nessuna icona compare (stesso principio fail-soft di ogni altro elemento condizionale pubblico) — non è un'area vuota "rotta", semplicemente non c'è nulla da mostrare. Questo non introduce alcuna nuova lettura DB/Server Action (riuso diretto di una funzione già esistente) né alcun nuovo comportamento funzionale sostanziale (è un link in uscita verso una URL già configurata e già mostrata altrove in home) — resta coerente con "restyling puro" della nota epica.
  - [ ] Non è richiesto (e non è nello scope) replicare l'intero `footer-grid` a 3 colonne di `DESIGN.md` (contatti+social, menu, società) — quel layout presuppone contenuti (indirizzo/telefono, menu duplicato) che oggi non esistono nel footer pubblico e che arriveranno con Story 18.11 (Contatti, backlog). Qui il footer resta strutturalmente quello di oggi (paragrafo di copyright), solo ricolorato/ritipografato, con l'aggiunta minima dell'icona social sopra descritta.

- [ ] Task 9: Banner cookie — restyle `cookie-banner` (AC: #1, #4, #6)
  - [ ] `app/CookieBanner.module.css` `.banner`: `background: #FFFFFF` (bianco — **già corretto oggi**, `var(--color-surface)` è già bianco, nessun cambio di sfondo necessario). Sostituire `border: 1px solid var(--color-border)` con `border-top: 2px solid #0B0E14` (`{colors.nero}`, letterale — **nota**: `DESIGN.md` specifica solo `border-top`, non un bordo su tutti i lati come oggi; rimuovere gli altri lati). `border-radius: 0` (rimuovere `var(--radius-sm)` — falso amico, `{rounded.none}` non `{rounded.sm}`). `box-shadow` da `0 1px 3px rgba(16,24,32,0.08)` a `0 -4px 16px rgba(11,14,20,0.12)` (ombra verso l'alto, valore già specificato in `DESIGN.md` → Elevazione per questo componente specifico).
  - [ ] `.testo`: colore `#0B0E14` (`{colors.nero}`), tipografia `{typography.body}`.
  - [ ] `.bottone` (Accetta, CTA primaria compatta): stessi token `button-primary` del Task 2 (`background: var(--color-primary)`, `color: #0B0E14`, `border-radius: 0`, `{typography.button-label}` maiuscolo). **Non riusare `var(--color-button-bg)`** (colore diverso, sistema vecchio).
  - [ ] `.bottoneSecondario` (Rifiuta): `{typography.button-label-secondary}` (12px/700, non maiuscolo forzato come il primario), colore `#5B6472` (`{colors.grigio}`) su sfondo bianco (~5.46:1, valore verificato in `DESIGN.md` in accessibility review) — **stesso peso visivo del pulsante Accetta** (nessun dark pattern, `EXPERIENCE.md` → Voce e Tono): non ridurre dimensione/opacità di "Rifiuta" rispetto ad "Accetta" oltre a quanto già specificato dal token.
  - [ ] `.preferenze` (pulsante fisso per riaprire il banner): stesso trattamento cta-secondaria, focus `{colors.focus-ring}` (`#0072A3`, letterale — **non** `var(--color-focus-ring)` `#006da6`, falso amico).
  - [ ] `:focus-visible` su `.bottone`/`.bottoneSecondario`/`.preferenze`: `{colors.focus-ring}` `#0072A3` (banner su sfondo bianco, non nero — token diverso dal Task 1/4, dove il contesto è scuro).
  - [ ] **Non toccare `z-index: 30`, `position: fixed`, la logica `visibile`/`valoreCorrente` in `CookieBanner.tsx`, né `router.refresh()`** — nessuno di questi è visivo, tutti sono comportamento già validato (Story 18.6/18.5 AC).

- [ ] Task 10: Verifica AC #2 — nessuna regressione sulla visibilità condizionale delle sezioni home
  - [ ] Confermare con una lettura riga-per-riga di `app/page.tsx` dopo le modifiche dei Task 3-6 che le quattro condizioni sono **testualmente identiche** a prima del restyling: `mostraSponsor = banner.length > 0 || convenzioni.length > 0` (riga 217); `mostraPartite = partiteSettimana.length > 0` (riga 226, derivato da `raggruppaPerSettimana`); `mostraFotoSquadra = gruppiConFoto.length > 0` (riga 234, dove `gruppiConFoto` resta filtrato su `fotoPerGruppo.has(g.id)`, invariato); `urlPaginaFacebook && consentitoSocial` (riga 387, invariato). Nessuna di queste va sostituita, rinominata, o avvolta in una nuova condizione — il restyling tocca solo il markup/CSS **dentro** ogni blocco condizionale, mai la condizione stessa.
  - [ ] Verificare manualmente (o con `npx tsc --noEmit`) che nessun `{condizione && (...)}` sia stato accidentalmente cambiato in un blocco sempre renderizzato durante la sostituzione di classi CSS (rischio concreto quando si riscrive molto JSX in un'unica passata).

- [ ] Task 11: Verifica AC #3 — nav resta wrap orizzontale, mai hamburger
  - [ ] Confermare che `app/NavPubblica.tsx` non è stato toccato nella struttura (nessun nuovo stato `useState` per aprire/chiudere un drawer, nessun bottone hamburger aggiunto, nessun `<button aria-expanded>`) — solo `NavPubblica.module.css` deve cambiare (Task 1). `.lista` deve mantenere `display:flex; flex-wrap:wrap` (righe 17-18 di `NavPubblica.module.css`, invariate). `.header` in `HeaderPubblico.module.css` deve mantenere `flex-wrap: wrap` (riga 12, invariata, commento riga 21-25 spiega perché).
  - [ ] Testare visivamente (o via resize della viewport) a 375px (frame mobile del mockup) che le 5 voci + Accedi vadano a capo su più righe invece di essere tagliate o nascoste dietro un menu — nessuna voce deve sparire dal DOM sotto alcuna larghezza.

- [ ] Task 12: Verifica AC #4 — area di tocco 44×44px e focus visibile su ogni elemento interattivo
  - [ ] Elenco completo da verificare uno per uno (nessuno escluso): voci di nav (`.voce`, già 44px, non toccato), pulsante primario nuovo nell'hero (`.heroCta`, Task 2), pulsanti Accetta/Rifiuta/Preferenze del banner cookie (Task 9, già 44px esistenti, verificare che il restyle non abbia rimosso `min-height:44px`), icona social nel footer (Task 8, nuova — hit-area 44px anche con resa visiva 38px), link "Accedi" (Task 1, già 44px esistenti). Per ognuno: area cliccabile reale (non solo visiva) ≥44×44px **e** un `:focus-visible` con outline visibile nel colore corretto per il contesto (`focus-ring-chiaro` bianco su sfondi scuri: header/nav/hero/footer/match-card; `focus-ring` `#0072A3` su sfondi chiari: cookie banner; `focus-ring-su-azzurro` `#0B0E14` per elementi su sfondo azzurro pieno, se presenti).
  - [ ] Navigare l'intera Home e `/squadre` solo da tastiera (Tab) e confermare che ogni elemento interattivo mostra un contorno visibile all'atto del focus, in un colore che non si confonde con lo sfondo circostante.

- [ ] Task 13: Verifica AC #5 — placeholder foto per Gruppo senza foto, solo su `/squadre`
  - [ ] Confermare che `/squadre` mostra il pattern hatch (Task 7) per ogni Gruppo della stagione corrente **senza** foto caricata, e che la home (`mostraFotoSquadra`, Task 5) continua a mostrare **solo** i Gruppi con foto reale, senza placeholder aggiunto lì. Le due pagine devono avere comportamenti visibilmente diversi per lo stesso dato mancante — non è un'incoerenza, è il comportamento specificato (`EXPERIENCE.md` → Fotografia Placeholder).
  - [ ] Caso di test manuale: una stagione con almeno un Gruppo senza foto caricata (Story 18.4) — verificare che su `/squadre` quella card mostri il placeholder (non un'icona rotta, non un'area vuota), e che quello stesso Gruppo semplicemente non compaia nella sezione "Foto di squadra" della home.

- [ ] Task 14: Verifica AC #6 — coppie di contrasto testo/sfondo
  - [ ] Per ogni combinazione colore introdotta nei Task 1-9, verificare che corrisponda esattamente a una coppia già calcolata in `DESIGN.md` → Colori (nessun valore arbitrario, nessuna interpolazione "simile"): bianco/nero su azzurro (6.73:1), testo chiaro su nero nelle quattro varianti (lead 14.26:1, muto 9.44:1, footer 10.53:1, copyright/scuro-muto 5.24:1), link Accedi su nero (6.08:1), magenta su nero per il "vs" del match-card se implementato (4.28:1, **solo** a dimensione `display-card`, mai più piccolo), Rifiuta grigio su bianco (~5.46:1), focus ring su bianco (5.33:1) e su azzurro (6.73:1). **Non** usare magenta per alcun elemento su sfondo azzurro (scenderebbe a 1.57:1, tabella "Cose da fare e da evitare" di `DESIGN.md`).
  - [ ] Se il dev agent introduce un accoppiamento colore non presente in questa lista (es. per un dettaglio decorativo non specificato), calcolarne il contrasto prima di usarlo o ometterlo — non introdurre "a occhio".

- [ ] Task 15: Verifica AC #7 — suite di test invariata
  - [ ] `npx vitest run` — nessuna delle modifiche di questa storia tocca file con test diretti: `HeaderPubblico.tsx`, `FooterPubblico.tsx`, `NavPubblica.tsx`, `CookieBanner.tsx`, `app/page.tsx`, `app/squadre/page.tsx`, `SponsorPubblicoCard.tsx` non hanno test dedicati oggi (verificato: nessun file `*.test.{ts,tsx}` nel repo importa nessuno di questi componenti, convenzione già consolidata "nessun test diretto su componenti di rendering pubblici", stessa nota già in Story 18.9/18.10/18.11). L'unico test che menziona le rotte pubbliche (`lib/auth/route-decision.test.ts`, righe 69-70, verifica `/squadre`/`/calendario`/`/staff`/`/contatti` in `PUBLIC_ROUTES`) non dipende da markup/CSS — deve restare verde senza modifiche.
  - [ ] `npx tsc --noEmit`, `npm run lint`, `npm run build` puliti — build produzione deve continuare a includere `/` e `/squadre` nell'output.

## Dev Notes

### Cosa NON cambia in questa storia (regressioni da non introdurre)

Questa è una storia di **restyling puro**: nessuna nuova Server Action, nessuna migrazione DB, nessuna nuova rotta, nessun nuovo modello dati (eccetto il riuso letto-solo di `leggiUrlPaginaFacebook()`, già esistente, per l'icona social del footer — Task 8). Ogni comportamento funzionale già validato da un AC di una story precedente (18.1-18.8, tutte `done`) deve restare **testualmente identico**:

- **`app/page.tsx`**: `mostraSponsor`/`mostraPartite`/`mostraFotoSquadra`/`urlPaginaFacebook && consentitoSocial` (righe 217, 226, 234, 387) — le quattro condizioni di visibilità delle sezioni. Vedi Task 10.
- **`app/NavPubblica.tsx`**: elenco orizzontale con wrap, `usePathname()` per lo stato attivo, confronto di uguaglianza esatta — nessun drawer/hamburger va introdotto (memoria di progetto: il mockup di riferimento mostra un hamburger *solo* a scopo illustrativo, la decisione reale di Story 18.7 è wrap orizzontale, `EXPERIENCE.md` → Responsive & Piattaforma lo ribadisce esplicitamente come "nota critica"). Vedi Task 11.
- **`.voce { display:inline-flex; align-items:center; min-height:44px }`** (`NavPubblica.module.css` righe 29-31) — touch target 44px sul link stesso, non su un contenitore. Questa è la stessa lezione già pagata nel progetto e salvata in memoria ("Touch target 44px richiede align-self:stretch" / min-height su un elemento non-flex non basta se il figlio cliccabile non riempie l'altezza dichiarata): un restyle "solo colori" che tocchi per errore `display`/`min-height` per allinearli a un nuovo valore di padding romperebbe silenziosamente l'area di tocco senza che nessun AC visivo lo segnali a occhio. Stesso principio per `.accedi` in `HeaderPubblico.module.css` (già `inline-flex`/`min-height:44px`) e per i pulsanti del `CookieBanner.module.css`.
- **`app/CookieBanner.tsx`**: `visibile`/`valoreCorrente`/`impostaConsenso`/`router.refresh()` — tutta la logica React resta invariata, solo `CookieBanner.module.css` cambia (Task 9). Il banner resta montato **solo sulla home** (`app/page.tsx`), non su `/squadre` — decisione già presa in Story 18.6, non riaperta qui.
- **`app/squadre/page.tsx`**: query Prisma (`select`, mai `include`), `trovaAnnoAgonisticoCorrente()` (mai `risolviAnnoAgonisticoCorrente`, side-effect di scrittura non ammissibile in una pagina GET), messaggio esplicito quando `gruppi.length === 0`, un Gruppo senza Allenatori che compare comunque senza elenco staff — nessuno di questi cambia. **L'unica modifica di comportamento visivo intenzionale in questa pagina è il placeholder per Gruppo senza foto (Task 7/AC #5)**, esplicitamente richiesta da questa storia e non in conflitto con alcun AC di Story 18.8.

### Le due regole del placeholder-foto non vanno confuse (il punto più delicato di questa storia)

`EXPERIENCE.md` → Fotografia Placeholder distingue esplicitamente due contesti con regole opposte per lo stesso dato mancante (Gruppo senza foto caricata):

1. **Home, sezione "Foto di squadra" (`mostraFotoSquadra`, Task 5)**: galleria **parziale** — mostra solo i Gruppi che hanno effettivamente una foto. Nessun placeholder, mai. Questa è la regola letterale di Story 18.4 AC #3 ("nessun placeholder per i Gruppi senza foto"), che parla esplicitamente della home pubblica — **non va toccata da questa storia**.
2. **`/squadre` (`team-card`, Task 7)**: elenco **completo** — ogni Gruppo della stagione corrente ha una card, con o senza Allenatori (Story 18.8 AC #3) e ora, con questa storia, con o senza foto: se manca la foto, la card mostra il pattern hatch placeholder invece di nessuna immagine. Questo è nuovo per questa storia (AC #5 di 18.12), non vietato da alcun AC di 18.8.

Un dev agent che applichi lo stesso trattamento a entrambe le pagine (es. aggiungendo il placeholder anche alla home, o togliendolo da `/squadre`) introdurrebbe una regressione su un AC già validato (Story 18.4 AC #3) o mancherebbe l'AC #5 di questa storia.

### Elementi nuovi richiesti da AC #4/#5, non solo restyle di markup esistente

Nonostante la nota dell'epica dica "restyling puro", **due AC di questa storia menzionano letteralmente elementi che non esistono ancora nel markup pubblico**:

- **AC #4** elenca "pulsante primario" e "icone social" tra gli elementi che devono avere area di tocco 44px e focus visibile. Oggi l'hero non ha alcun pulsante (Story 18.5 ha rimosso l'ultimo paragrafo residuo, nessun CTA è mai stato aggiunto) e il footer non ha alcuna icona. `DESIGN.md.components.hero.cta` e `EXPERIENCE.md` → Pattern dei Componenti ("Hero: Un solo CTA primario 'Scopri le squadre' → `/squadre`") li richiedono esplicitamente come parte del registro visivo che questa storia applica.
- **AC #5** richiede il placeholder foto su `/squadre`, che oggi non esiste (vedi sopra).

Questi non violano "nessun cambio di comportamento funzionale già validato" perché non introducono nuovi dati, nuove query, o nuove Server Action — solo nuovo markup/link verso risorse già esistenti (CTA → `/squadre`, icona social → `urlPaginaFacebook` già letto da Story 18.5, placeholder → puro CSS). Il dev agent deve trattarli come **richiesti dagli AC numerati**, non come "fuori scope perché è solo restyling" — la nota dell'epica descrive l'intento generale, gli AC numerati sono la fonte di verità puntuale e prevalgono.

### Convenzione CSS: hex letterali commentati, quattro sole eccezioni riusabili (verificate esattamente in questa sessione)

Nessun custom property per la palette "Poster Sportivo" esiste in `app/globals.css` (che contiene solo i token del `DESIGN.md` 2026-07-22 del portale interno `/app`, un sistema di design diverso — stesso principio già stabilito e applicato in Story 18.9/18.10/18.11 per `/calendario`/`/staff`/`/contatti`). Scrivere ogni valore come hex/px letterale in un commento che cita il nome esatto del token `DESIGN.md` — **non** aggiungere nuovi custom property a `globals.css` (condiviso con `/app`, mescolerebbe due vocabolari di token).

Verificato riga per riga in `app/globals.css` (righe 7-40) contro `DESIGN.md` → Colori: **solo quattro valori coincidono esattamente** e sono gli unici riusabili come custom property esistente invece di un hex letterale:

| Token `DESIGN.md` | Valore | Custom property esistente | Coincide? |
|---|---|---|---|
| `{colors.azzurro}` | `#00A3E0` | `var(--color-primary)` (`#00a3e0`) | Sì — riusare |
| `{colors.navy}` | `#312682` | `var(--color-navy)` (`#312682`) | Sì — riusare |
| `{colors.magenta}` | `#E6007C` | `var(--color-magenta)` (`#e6007c`) | Sì — riusare |
| `{colors.bianco}` | `#FFFFFF` | `var(--color-surface)` (`#ffffff`) | Sì — riusare |

**Falsi amici da non usare per il nome, solo per coincidenza apparente** (verificati diversi, valori non intercambiabili):
- `{colors.nero}` (`#0B0E14`) **≠** `var(--color-text-primary)` (`#101820`) — letterale.
- `{colors.focus-ring}` (`#0072A3`) **≠** `var(--color-focus-ring)` (`#006da6`) — nonostante il nome identico, valori diversi. Letterale.
- `{rounded.sm}` (`2px`) **≠** `var(--radius-sm)` (`6px`) — letterale.
- `{colors.grigio-chiaro}` (`#F2F5F7`) **≠** `var(--color-surface-alt)` (`#e4f5fd`) — letterale.
- `{colors.azzurro-scuro}` (`#0072A3`) **≠** `var(--color-primary-hover)` (`#0086bd`) — letterale.
- `{spacing.*}` oltre `space-6`/`space-8` (`header-height` 76px, `section-padding-desktop` 80px/48px, `hero-padding-desktop` 64px/48px/88px, `card-gap` 28px, e la progressione `spacing.10/12/16/20` 40/48/64/80px) — nessun custom property equivalente esiste (`globals.css` si ferma a `--space-8: 32px`, nessun `--space-7`) — tutti letterali.

Tutti i token di tipografia (`display-hero`, `display-section`, `display-card`, `nav-item`, `wordmark`, `eyebrow`, `button-label`, `button-label-secondary`, `label-tag`, `label-heading`, `body`, `body-lead`, `meta`, `countdown-value`, `countdown-label`) non hanno alcun equivalente in `globals.css` (che non definisce token tipografici) — sempre letterali, valori esatti in `DESIGN.md` (frontmatter YAML, sezione `typography`).

### Mappa componente `DESIGN.md` → file esistente

| Componente `DESIGN.md` | File(s) | Task |
|---|---|---|
| `header-nav` | `app/HeaderPubblico.tsx`/`.module.css`, `app/NavPubblica.tsx`/`.module.css` | 1 |
| `hero` + `button-primary` (CTA) + `placeholder-foto` (foto hero) | `app/page.tsx`, `app/home-pubblica.module.css` | 2 |
| `sponsor-strip` | `app/page.tsx` (sezione Sponsor), `app/SponsorPubblicoCard.tsx`, `app/home-pubblica.module.css` | 3 |
| `next-match-strip`/`match-card` | `app/page.tsx` (sezione Partite), `app/home-pubblica.module.css` | 4 |
| `team-card` (galleria parziale, nessun placeholder) | `app/page.tsx` (sezione Foto squadra), `app/home-pubblica.module.css` | 5 |
| `social-embed` | `app/page.tsx` (sezione Post social), `app/home-pubblica.module.css` | 6 |
| `team-card` (elenco completo, con placeholder) + `placeholder-foto` (foto squadra) | `app/squadre/page.tsx`, `app/squadre/squadre.module.css` | 7 |
| `footer` + icona social | `app/FooterPubblico.tsx`/`.module.css` | 8 |
| `cookie-banner` | `app/CookieBanner.tsx`/`.module.css` | 9 |
| `staff-list`, `contact-block` | `/staff`, `/contatti` (Story 18.10/18.11, backlog) — **fuori scope**, non toccare | — |

### `AGENTS.md` — Next.js non standard

Questo progetto usa una versione di Next.js con differenze rispetto al training data (`AGENTS.md`, root del repo): prima di scrivere codice di routing/parametri, consultare `node_modules/next/dist/docs/`. **Non sostanzialmente applicabile a questa storia**: nessuna modifica a routing, `params`/`searchParams`, o Server Action — è restyling CSS/markup su pagine già esistenti. L'unica modifica di logica server è il riuso di `leggiUrlPaginaFacebook()` già esistente (Task 8), stesso pattern già usato in `app/page.tsx`.

### Pattern da riusare (non reinventare)

- **`leggiUrlPaginaFacebook()`** (`@/lib/configurazione-applicazione`) — già letta in `app/page.tsx` (Story 18.5), riusare identica in `FooterPubblico.tsx` per l'icona social (Task 8), con lo stesso `.catch()` fail-soft.
- **`urlPubblicoFotoSquadra`/`elencaGruppiConFoto`** (`@/lib/storage/foto-squadra`) — già usate in entrambe le pagine, nessuna modifica di firma necessaria per il Task 7 (solo il ramo di rendering quando l'entry non esiste cambia, non la funzione).
- **`costruisciLinkNaviga`** (`@/lib/link-naviga-palestra`) — invariato, solo lo stile del link "Naviga" cambia (Task 4).
- **Micro-animazioni CSS-only** (`transition`, `::after` per la sottolineatura nav, `transform: translateY()` per hover di pulsanti/card) — nessuna libreria di animazione, coerente con `EXPERIENCE.md` → Primitive di Interazione ("nessuna animazione di ingresso pagina, nessun parallax, nessun carosello automatico"). Aggiungere `@media (prefers-reduced-motion: reduce)` per ridurre le transizioni a un cambio di stato quasi istantaneo (esplicitamente richiesto in `EXPERIENCE.md` → Primitive di Interazione, aggiunto in accessibility review).
- **Un CSS module per pagina/componente condiviso** (nessun cambio a questa convenzione): `home-pubblica.module.css` per il contenuto specifico della home, `squadre.module.css` per `/squadre`, `HeaderPubblico.module.css`/`FooterPubblico.module.css`/`NavPubblica.module.css`/`CookieBanner.module.css` per i componenti condivisi (eccezioni già documentate nei rispettivi file).

### Project Structure Notes

- Nessun file nuovo creato — questa storia modifica solo file esistenti: `app/HeaderPubblico.tsx` (piccola aggiunta, nessuna nuova prop), `app/HeaderPubblico.module.css`, `app/NavPubblica.module.css`, `app/page.tsx` (import `Link`, markup CTA hero + placeholder foto hero, nessun cambio alle query/condizioni), `app/home-pubblica.module.css` (riscrittura estesa dei valori visivi, struttura CSS invariata), `app/squadre/page.tsx` (ramo `else` per il placeholder foto), `app/squadre/squadre.module.css`, `app/FooterPubblico.tsx` (nuova lettura `leggiUrlPaginaFacebook()` + rendering condizionale icona), `app/FooterPubblico.module.css`, `app/CookieBanner.module.css` (nessuna modifica a `CookieBanner.tsx`, solo CSS).
- Nessuna migrazione DB, nessuna nuova Server Action, nessuna modifica a `lib/auth/route-guard.ts` (nessuna nuova rotta), nessuna modifica a `prisma/schema.prisma`.
- Allineamento con la struttura di progetto: nessuna deviazione — questa storia rispetta esattamente la stessa convenzione "un modulo CSS per pagina/componente condiviso" già stabilita da Story 18.2-18.11.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Epic 18, Story 18.12] — testo originale di User Story e AC (verbatim in questo file), nota "Restyling puro" e vincolo esplicito sul comportamento nav mobile.
- [Source: _bmad-output/planning-artifacts/epics.md#Epic 18, Story 18.2/18.3/18.4/18.5/18.6/18.7/18.8] — AC già validati da non regredire (visibilità condizionale, wrap nav, touch target, placeholder home).
- [Source: app/page.tsx] — markup/condizioni attuali della home (hero riga 242-250, sezione Sponsor 255-290, sezione Partite 292-347, sezione Foto squadra 349-374, sezione Social 376-407), letture dati (Promise.all righe 105-200), montaggio Header/Footer/CookieBanner (righe 240, 413, 418).
- [Source: app/squadre/page.tsx] — markup/condizioni attuali di `/squadre` (query righe 40-72, rendering card righe 88-120, ramo `fotoInfo !== undefined` riga 97 da estendere con placeholder).
- [Source: app/HeaderPubblico.tsx, app/HeaderPubblico.module.css] — markup/stile attuale header condiviso, `.accedi` da ricolorare (non è button-primary).
- [Source: app/NavPubblica.tsx, app/NavPubblica.module.css] — markup/stile attuale nav condivisa, commento sorgente sulla lezione touch-target 44px (righe 24-27), review fix specificità `.voceAttiva:hover` (righe 60-68) da preservare.
- [Source: app/FooterPubblico.tsx, app/FooterPubblico.module.css] — markup/stile attuale footer condiviso, prop `conSpazioCookieBanner`.
- [Source: app/CookieBanner.tsx, app/CookieBanner.module.css] — markup/stile/logica attuale banner cookie, scoping solo-home (righe 12-19 commento).
- [Source: app/SponsorPubblicoCard.tsx] — markup card sponsor attuale, nessun stato "sponsor senza logo" gestito.
- [Source: app/home-pubblica.module.css, app/squadre/squadre.module.css] — CSS Modules attuali da riscrivere, entrambi oggi basati sui token del portale interno (`var(--color-*)`, `var(--space-*)`).
- [Source: app/globals.css, righe 7-40] — elenco completo dei custom property CSS esistenti (`--color-*`, `--radius-*`, `--space-*`), confrontato riga-per-riga con `DESIGN.md → Colori` per determinare le quattro eccezioni riusabili e i "falsi amici".
- [Source: prisma/schema.prisma, riga 180, model AnnoAgonistico] — nessun campo `nome`/etichetta stagione, rilevante per la nota su eyebrow badge opzionale nell'hero.
- [Source: lib/auth/route-decision.test.ts, righe 64-70] — unico test esistente che menziona le rotte pubbliche coinvolte, non dipendente da markup/CSS.
- [Source: ux-designs/ux-societa-manager-2026-08-13/DESIGN.md, frontmatter `colors`/`typography`/`rounded`/`spacing`/`components`, e sezioni "Colori", "Tipografia", "Layout e Spaziatura", "Forme", "Componenti", "Cose da fare e da evitare"] — fonte di ogni token citato in questa storia (header-nav, hero, next-match-strip, team-card, match-card, sponsor-strip, footer, button-primary, cookie-banner, social-embed, placeholder-foto), contrasti calcolati.
- [Source: ux-designs/ux-societa-manager-2026-08-13/EXPERIENCE.md#Architettura dell'Informazione, #Pattern dei Componenti, #Fotografia Placeholder, #Pattern di Stato, #Primitive di Interazione, #Soglia di Accessibilità, #Responsive & Piattaforma, #Key Flows] — comportamento atteso di ogni componente, nota critica nav mobile (mockup vs decisione reale), le due regole del placeholder-foto (hero permanente vs team-card solo fase di lancio, home galleria parziale vs squadre elenco completo), flow Chiara/Davide come riferimento end-to-end da non rompere.
- [Source: ux-designs/ux-societa-manager-2026-08-13/mockups/home-poster-sportivo.html, righe 46-420] — valori CSS letterali di riferimento (clip-path, gradient, font-size) per header/hero/next-match/team-card/match-card/sponsor/footer, versione corretta post accessibility-review — contenuto illustrativo (nomi atleta/numero maglia in team-card) esplicitamente da **non** portare nel codice reale (vedi `DESIGN.md` → Marchio e Stile, nota `[NOTA UX APERTA]`, e → Cose da fare e da evitare).
- [Source: _bmad-output/implementation-artifacts/18-9-pagina-calendario.md, Dev Notes "Token esatti da usare"/"Convenzione CSS"] — precedente diretto della stessa convenzione hex-letterale-commentato con le tre eccezioni azzurro/navy/magenta, qui estesa con la quarta eccezione (bianco) e i "falsi amici" aggiuntivi verificati per lo scope più ampio di questa storia.
- [Source: _bmad-output/implementation-artifacts/18-10-pagina-staff.md, 18-11-pagina-contatti.md] — stessa convenzione applicata a `staff-list`/`contact-block`, citate qui solo per continuità di convenzione, componenti fuori scope di questa storia.
- [Source: AGENTS.md, root del repo] — nota su Next.js non standard, non sostanzialmente applicabile (nessun routing/parametri toccato).
- [Source: lib/configurazione-applicazione.ts] — `leggiUrlPaginaFacebook()`, funzione già esistente riusata per l'icona social del footer (Task 8), nessuna nuova Server Action.

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
