## Deferred from: code review of 1-1-registrazione-e-login-per-ruolo (2026-07-15)

- ~~`Utente.attivo` non viene mai controllato al login~~ — **risolto in Story 1.2** (con un bug residuo poi corretto in code review, vedi sotto).
- Nessuna validazione centralizzata delle variabili d'ambiente richieste — asserzioni non-null sparse (`process.env.X!`) in più file, un valore mancante emerge come errore criptico a runtime invece che al boot. Cross-cutting, non blocca nessun AC di questa storia. [lib/prisma.ts, lib/supabase/client.ts, lib/supabase/server.ts, lib/auth-admin/client.ts, proxy.ts]
- Il matcher del Proxy esclude solo `_next/static`, `_next/image` e `favicon.ico`: altri eventuali asset in `/public` verrebbero comunque fatti passare dal redirect di autenticazione. Nessun asset del genere esiste ancora, impatto attuale nullo. [proxy.ts:52]
- Finestra di autorizzazione stantia: se i Ruoli di un utente vengono revocati, il JWT già emesso mantiene i vecchi Ruoli fino al refresh/scadenza (`jwt_expiry` = 1h). Trade-off intrinseco della scelta architetturale AD-11 di leggere i Ruoli dal JWT invece che da una query Prisma ad ogni richiesta. [proxy.ts]
- Nessuna funzionalità di logout in nessuna pagina di questo diff. Fuori scope per gli AC di questa storia (login/registrazione/route guard). [app/page.tsx]

## Deferred from: code review of 9-15-assegnazione-atlete-da-allenatore (2026-07-30)

- Doppia chiamata a `supabase.auth.getUser()` per invocazione (`requireRuolo` + `risolviPossessoGruppo`) — stesso pattern già presente e accettato in `app/(partite-campionati)/campionati/actions.ts` (Epic 10), non specifico di questa storia. [app/(gruppi-allenatori)/gruppi/actions.ts]
- Enumerazione minima: un Allenatore può distinguere "Gruppo inesistente" (VALIDATION) da "Gruppo non tuo" (FORBIDDEN) in base all'ordine dei controlli — rischio basso (id UUID non enumerabili, utenti già autenticati, dato non sensibile). [app/(gruppi-allenatori)/gruppi/actions.ts]
- Selettore Atlete in `MioGruppoCard.tsx` senza ricerca/filtro né indicazione di quali siano già assegnate altrove — usabilità peggiorata su un elenco fino a ~200 righe, richiederebbe una feature di ricerca non specificata negli AC. [app/(gruppi-allenatori)/i-miei-gruppi/page.tsx]
- `elencaAtlete(supabase)` eseguita incondizionatamente anche quando l'Allenatore non gestisce alcun Gruppo (a differenza della query `gruppoAtleteRows`, correttamente gated) — inefficienza minima su una tabella di ~200 righe. [app/(gruppi-allenatori)/i-miei-gruppi/page.tsx]
- `prisma.allenatore.findFirst` assume un solo Allenatore per Utente, senza asserzione esplicita — stesso identico pattern già usato in `presenze/page.tsx`, `campionati/page.tsx`, `dati-fisici/page.tsx`. [app/(gruppi-allenatori)/i-miei-gruppi/page.tsx]
- Nessun `try/catch` attorno alle letture in `Promise.all` di `page.tsx` — stesso identico pattern non-guardato già usato in `presenze/page.tsx` per le letture GET. [app/(gruppi-allenatori)/i-miei-gruppi/page.tsx]

## Deferred from: code review of 9-16-parametri-standard-dati-fisici (2026-07-31)

- `PARAMETRI_STANDARD` confronta `tipo` per uguaglianza esatta nel form, mentre `raggruppaPerTipo` normalizza (trim/lowercase) per il raggruppamento — un tipo "Altro" digitato con maiuscole/spazi diversi da uno standard (es. " Peso") si raggruppa con quello standard nel grafico/tabella ma non verrebbe mai riconosciuto come parametro standard dal selettore. Impatto pratico nullo oggi (nessun flusso di modifica/ripresentazione esiste). [lib/misurazioni/parametri-standard.ts]
- Nessun annuncio per screen reader quando i campi tipo/unità diventano read-only dopo la scelta di un parametro standard — nessun'altra pagina di questo progetto usa `aria-live` per scenari simili, stesso livello di accessibilità di base già esistente. [app/(dati-atleta)/dati-fisici/MisurazioneForm.tsx]
- L'errore di validazione sui 3 tentativi è un unico messaggio generico, non indica quale dei tre campi è invalido — coerente con la convenzione già stabilita in tutto il progetto (un solo messaggio di errore per form). [app/(dati-atleta)/dati-fisici/actions.ts]

## Deferred from: code review of 9-17-griglia-mensile-presenze-per-gruppo (2026-07-31)

- `<input type="month">` ha supporto browser non universale e nessun `min`/`max` — mitigato in buona parte dal fallback al mese corrente per input malformato, un `min`/`max` resta un miglioramento cosmetico. [app/(presenze)/storico-presenze/page.tsx]
- Il ramo di autorizzazione `gruppoValido` (AC #4) non ha test dedicati — nessuna pagina di questo progetto ha mai avuto test per la propria logica di autorizzazione lato GET (stesso limite di `presenze/page.tsx`, `dati-fisici/page.tsx`). [app/(presenze)/storico-presenze/page.tsx]
- Stesso messaggio "Gruppo non trovato tra i tuoi" mostrato sia per un Gruppo manomesso sia per un link obsoleto quando non esiste un Anno Agonistico corrente — casistica rara. [app/(presenze)/storico-presenze/page.tsx]
- `leggiPresenzeGriglia` riceve l'intero array `giorni` (28-31 elementi) ma usa solo il primo/ultimo — un parametro `{ inizio, fine }` sarebbe più esplicito, refactor di forma dell'API senza impatto funzionale. [lib/db-rls/presenza.ts]
- `prisma.slot.findMany` legge tutti gli Slot mai creati per il Gruppo, non filtrati per il mese selezionato, solo per costruire `slotIds` — inefficienza minima, scala ridotta del progetto. [app/(presenze)/storico-presenze/page.tsx]
- Nessun `<caption>`/legenda sulla tabella della griglia per screen reader — stesso livello di accessibilità di base già esistente in ogni altra tabella del progetto. [app/(presenze)/storico-presenze/page.tsx]
- Due righe Presenza per la stessa Atleta+data da due Slot diversi dello stesso Gruppo lo stesso giorno di calendario — la `Map` tiene silenziosamente solo l'ultima incontrata; casistica rara, la rappresentazione corretta richiederebbe una decisione di prodotto non coperta da alcun AC. [app/(presenze)/storico-presenze/page.tsx]

## Deferred from: code review of 1-2-gestione-utenti-e-ruoli-admin (2026-07-16)

- Nessuna normalizzazione case dell'email prima delle chiamate Supabase/Prisma — stesso pattern non normalizzato già presente in `registrati/actions.ts` di Story 1.1. [app/(amministrazione)/admin/actions.ts]
- `signInWithPassword` mappa qualunque codice di errore (rate limit, email non confermata, ecc.) sullo stesso messaggio generico "Credenziali non valide" — comportamento invariato di Story 1.1, non toccato dal Task 3 di questa storia. [app/(auth)/accedi/actions.ts]
- `AdminPage` legge tutti gli Utenti senza paginazione — prematuro per la scala attuale hobby/piccola società. [app/(amministrazione)/admin/page.tsx]
- `AdminPage` non ha un error boundary se la query Prisma fallisce — nessuna pagina dell'app ha `error.tsx`, non specifico di questa storia. [app/(amministrazione)/admin/page.tsx]
- L'array `RUOLI` (valore/etichetta) è duplicato in `NuovoUtenteForm.tsx`, `UtenteRow.tsx` e `registrati/page.tsx` (Story 1.1) — pulizia DRY a basso rischio, tocca anche un file di Story 1.1. [app/(amministrazione)/admin/NuovoUtenteForm.tsx, app/(amministrazione)/admin/UtenteRow.tsx]
- Nessun flusso di invito/password temporanea/reset forzato per gli utenti creati dall'Admin — esplicitamente fuori scope (nessun sistema di inviti/email esiste in questo progetto). [app/(amministrazione)/admin/actions.ts]

## Deferred from: code review of 1-3-import-archivio-atlete-da-export-federale (2026-07-16)

- Import sequenziale riga-per-riga (1 SELECT + 1 INSERT/UPDATE per riga, nessun batching/transazione/upsert) — rischio concreto di timeout su Cloudflare Workers per file grandi, ma NFR5 limita la scala a ~200 Atlete; risolverlo richiederebbe ridisegnare il conteggio create/aggiornate (AC #5) per un batch sicuro. [app/(onboarding-import)/import-atlete/actions.ts]
- Nessun test automatico verifica dal vivo che la policy RLS su `atlete` blocchi davvero i Ruoli non autorizzati — verificato manualmente dal vivo durante lo sviluppo, non con un test ripetibile; un'infrastruttura di test di integrazione contro Postgres reale è fuori dallo standard di test (Vitest + mock) di questo progetto. [prisma/migrations/20260716063714_add_atleta/migration.sql]
- Finestra di autorizzazione stantia sulla revoca di un Ruolo (fino a 1h) — stesso trade-off architetturale già accettato in AD-11/Story 1.1.
- Il route guard permette per difetto ("fail-open") qualunque rotta autenticata non esplicitamente elencata in `PROTECTED_ROUTES` — comportamento preesistente dalla Story 1.1. [lib/auth/route-guard.ts]
- Nessun limite di dimensione/tipo file lato server sull'upload Excel oltre all'`accept=".xlsx"` lato client — rischio basso per uno strumento interno già filtrato dal route guard. [app/(onboarding-import)/import-atlete/actions.ts]
- `trovaPerCodiceFiscale` non distingue "Atleta inesistente" da "Atleta esistente ma nascosta dalla RLS" — nessuna conseguenza oggi (solo Admin/Dirigente lo richiamano), diventerà rilevante con le policy scoped di Story 1.5/Epic 2. [lib/matching-codice-fiscale/trova-per-codice-fiscale.ts]
- Il Codice Fiscale può comparire senza redazione nei log (`console.error`) — stesso pattern già adottato ed esplicitamente approvato in Story 1.1/1.2; una redazione andrebbe applicata trasversalmente. [app/(onboarding-import)/import-atlete/actions.ts]

## Deferred from: code review of 1-4-precaricamento-allenatori (2026-07-16)

- L'aggancio Allenatore usa la sola conoscenza del Codice Fiscale come prova d'identità, senza verifica aggiuntiva — **deciso con l'utente: accettabile per ora** (piccola società, singolo Admin). **Da riconsiderare (conferma manuale dell'Admin) se l'app venisse estesa a tutta la polisportiva** — vedi PRD §8 "Estensione pluri-settore". [app/(onboarding-import)/registrati/actions.ts]
- Race condition (check-then-create) su Codice Fiscale duplicato, sia in `precaricaAllenatore` sia nell'aggancio in registrazione — bassa probabilità reale (singolo Admin, piccola società), stessa classe di problema già accettata come Defer in Story 1.3. [app/(onboarding-import)/precaricamento-allenatori/actions.ts, app/(onboarding-import)/registrati/actions.ts]
- Nessun percorso di riconciliazione se il precaricamento avviene dopo l'autoregistrazione — esplicitamente fuori scope (compito di una storia futura). [app/(onboarding-import)/registrati/actions.ts]
- Nessuna UI di modifica/eliminazione/elenco per i precaricamenti — fuori scope, coerente con NFR6. [app/(onboarding-import)/precaricamento-allenatori/page.tsx]
- Nessuna validazione di lunghezza massima sul campo `nome` in `precaricaAllenatore` — valore basso per uno strumento interno riservato all'Admin. [app/(onboarding-import)/precaricamento-allenatori/actions.ts]

## Deferred from: code review of 1-5-aggancio-genitore-atleta-in-registrazione (2026-07-16)

- Verifica d'identità basata solo sul Codice Fiscale + possibile enumerazione di Atlete tramite il messaggio d'errore distinto ("Nessuna Atleta trovata con questo Codice Fiscale", pre-`signUp`, nessuna autenticazione/rate limiting) — **deciso con l'utente: accettabile per ora**, stessa logica già accettata in Story 1.4 per l'Allenatore (piccola società, Admin singolo che conosce le famiglie). **Da riconsiderare se l'app venisse estesa a tutta la polisportiva** — vedi PRD §8 "Estensione pluri-settore". [app/(onboarding-import)/registrati/actions.ts]
- Nessuna transazione avvolge le scritture post-`signUp` (`Utente`, `sincronizzaRuoliAppMetadata`, aggancio Allenatore, `GenitoreAtleta`) — stessa decisione "nessun rollback automatico" già presa in Story 1.1 e riapplicata identica in Story 1.4; include il caso limite (bassa probabilità, nessuna funzionalità di eliminazione Atleta esiste oggi) in cui l'Atleta venga eliminata/modificata tra il lookup pre-`signUp` e la `create` post-`signUp`. [app/(onboarding-import)/registrati/actions.ts]
- `trovaPerCodiceFiscale` fa `select("*")`, portando in memoria l'intero record Atleta (inclusi campi PII) quando questa storia usa solo `atleta.id` — non risolvibile localmente: funzione condivisa (AD-5), Story 1.3 ha bisogno del record completo; richiederebbe un parametro opzionale di selezione colonne. [lib/matching-codice-fiscale/trova-per-codice-fiscale.ts]
- `GRANT SELECT ON "atlete" TO service_role` è una concessione permanente e ampia per un caso d'uso ristretto (verifica di esistenza per CF) — un'alternativa scoped (funzione Postgres `SECURITY DEFINER`) sarebbe più difensiva ma è un redesign. [prisma/migrations/20260716120000_grant_atlete_service_role/migration.sql]
- `genitori_atlete` non ha RLS/policy deny-all esplicite come difesa in profondità — oggi protetta solo dall'assenza di GRANT per `authenticated`/`anon` (fail-closed di default), ma priva di una policy che la protegga se in futuro venisse concesso un accesso generico. [prisma/schema.prisma]
- Nessun test di integrazione automatico verifica dal vivo GRANT/RLS/client service-role contro Postgres reale — fuori dallo standard di test (Vitest + mock) di questo progetto, già accettato come Defer in Story 1.3; verificato manualmente dal vivo in fase di dev-story. [prisma/migrations/20260716120000_grant_atlete_service_role/migration.sql]
- Il test per l'AC #4 (due Genitori sulla stessa Atleta) verifica solo gli argomenti della chiamata mockata, non il vincolo reale `@@unique([utenteId, atletaId])` — coperto dalla verifica dal vivo eseguita in fase di dev-story, non da un test automatico ripetibile. [app/(onboarding-import)/registrati/actions.test.ts]
- Il Ruolo Atleta non ha alcun meccanismo di auto-aggancio al proprio record Atleta tramite Codice Fiscale (a differenza di Genitore e Allenatore) — nessuna FR lo richiede oggi, possibile lacuna di prodotto da valutare in futuro. [app/(onboarding-import)/registrati/page.tsx]
- L'email non viene normalizzata (minuscolo) prima del confronto di unicità Prisma, a differenza del Codice Fiscale — comportamento preesistente da Story 1.1, non toccato da questa storia. [app/(onboarding-import)/registrati/actions.ts]

## Deferred from: dev-story of 1-6-conferma-iscrizione (2026-07-16)

- **Staleness del JWT subito dopo l'auto-registrazione**: un utente appena registrato (`registrati/actions.ts`) ottiene una sessione il cui JWT è stato emesso da `signUp` prima che `sincronizzaRuoliAppMetadata` scrivesse i Ruoli su `app_metadata` — una lettura RLS-protetta con la propria sessione, effettuata subito dopo, vede `ruoli` ancora vuoto e restituisce zero righe (nessun errore, solo un elenco vuoto), finché l'utente non effettua un nuovo login (JWT fresco). Scoperto dal vivo durante la verifica di Story 1.6 (Segreteria auto-registrata che visita subito `/conferma-iscrizioni`). Stesso trade-off già esplicitamente accettato in AD-11 ("la staleness del JWT... è accettata, non richiede invalidazione forzata della sessione") — qui solo osservato in un caso concreto e nuovo (auto-registrazione + Ruolo Segreteria-gated nella stessa sessione). Risolverlo richiederebbe un refresh esplicito della sessione dopo la sincronizzazione di `app_metadata`, un cambiamento trasversale a tutte le storie di registrazione, non specifico di questa. [app/(onboarding-import)/registrati/actions.ts]

## Deferred from: code review of 1-6-conferma-iscrizione (2026-07-16)

- `calcolaIntervalloStagioneCorrente` calcola i confini della stagione in UTC, non in orario locale Europe/Rome — margine di errore di 1-2 ore attorno alla mezzanotte UTC del 31 luglio/1 agosto. **Deciso con l'utente: accettabile per ora** (margine di poche ore, una volta l'anno). Da riconsiderare con una gestione esplicita del fuso orario se il margine si rivelasse un problema reale. [lib/anno-agonistico/calcola-intervallo-stagione-corrente.ts]
- Nessuna paginazione su `elencaAtlete`/`elencaIscrizioniPerAnno` — coerente con NFR5 (~200 Atlete per il v1), stesso trattamento di ogni altra pagina-elenco del progetto. [lib/db-rls/atleta.ts, lib/db-rls/iscrizione.ts]
- L'errore generico `INTERNAL` nasconde la causa reale (RLS, FK, race, timeout) al client — coerente con la convenzione già stabilita in tutto il progetto. [app/(iscrizioni)/conferma-iscrizioni/actions.ts]
- Nessun test automatico verifica dal vivo che le policy RLS di `iscrizioni` blocchino davvero un Ruolo non autorizzato — stessa categoria già accettata come Defer in Story 1.3, verificato manualmente dal vivo. [prisma/migrations/20260716210000_add_iscrizione/migration.sql]
- Nessun messaggio di stato vuoto né attributi di accessibilità di base nella tabella di `page.tsx` — stesso livello di rifinitura UI di ogni altra pagina-elenco del progetto. [app/(iscrizioni)/conferma-iscrizioni/page.tsx]
- Eliminazione concorrente di un'Atleta con una riga aperta lato Segreteria → errore generico riprovabile all'infinito — puramente teorico, nessuna funzionalità di eliminazione Atleta esiste oggi. [app/(iscrizioni)/conferma-iscrizioni/actions.ts]
- Nessun vincolo di controllo DB su `AnnoAgonistico` (`dataFine > dataInizio`, non sovrapposizione) — idea di hardening, nessuno script di scrittura diretta esiste oggi. [prisma/schema.prisma]
- `calcolaIntervalloStagioneCorrente` non ha una guardia per una Data non valida — nessun punto di chiamata reale le passa altro che una data valida oggi. [lib/anno-agonistico/calcola-intervallo-stagione-corrente.ts]
- Il `Promise.all` in `page.tsx` non è avvolto in un try/catch, nessun `error.tsx` in tutta l'app — gap preesistente e trasversale, già loggato in Story 1.2. [app/(iscrizioni)/conferma-iscrizioni/page.tsx]

## Deferred from: code review of 1-7-merge-certificato-in-import (2026-07-17)

- Race condition (read-then-write) in `unisciCertificato` sotto import concorrenti sulla stessa Atleta — stessa classe già accettata come Defer in Story 1.3/1.4/1.5/1.6 (singolo Admin, piccola società). Un fix analogo a quello di Story 1.6 (cattura del vincolo univoco + retry) è possibile ma non banale qui, da rivalutare se si manifestasse realmente. [lib/matching-codice-fiscale/unisci-certificato.ts]
- Se `esistente.dataFineValidita` fosse mai un valore non parsabile, il confronto risulterebbe sempre falso (NaN) e l'aggiornamento salterebbe silenziosamente — nessun percorso di scrittura reale produce oggi un valore non-ISO per questa colonna. [lib/matching-codice-fiscale/unisci-certificato.ts]
- Il messaggio d'errore generico dell'import non distingue un fallimento sull'Atleta da un fallimento sul solo merge del certificato — coerente con la convenzione di errore generico già stabilita in tutto il progetto. [app/(onboarding-import)/import-atlete/actions.ts]
- Una riga con altri campi certificato popolati ma senza una data di fine validità parsabile viene scartata silenziosamente, senza segnale — aggiungere un avviso richiederebbe cambiare la forma del riepilogo dell'import, esplicitamente fuori scope per questa storia. [app/(onboarding-import)/import-atlete/parser.ts]
- Nessun vincolo di controllo DB che imponga `dataInizioValidita <= dataFineValidita` — idea di hardening, nessuna conseguenza grave oggi (Epic 4 non esiste ancora). [prisma/migrations/20260717090000_add_certificato_medico/migration.sql]
- Nessuno storico dei Certificati Medici (un solo record "corrente" per Atleta) — decisione architetturale già esplicita e motivata nelle Dev Notes della storia. [prisma/schema.prisma]
- La policy RLS `SELECT` su `certificati_medici` è scoped solo per Ruolo, non per relazione Genitore/Atleta — dati sanitari più sensibili dei campi identitari di `Atleta`, ma è esattamente lo scope di FR-27/Epic 5 ("Permessi granulari su dati sanitari"), già pianificato come storia futura dedicata. [prisma/migrations/20260717090000_add_certificato_medico/migration.sql]
- Nessun limite di plausibilità sulle date di certificato (una data palesemente errata "vincerebbe" sempre il confronto "più recente") — nessuna UI espone oggi questi dati per una verifica umana; l'Epic 4 costruirà i flussi di revisione. [app/(onboarding-import)/import-atlete/parser.ts]
- Il ciclo di import esegue ora fino a 4 round-trip Supabase sequenziali per riga (erano 2) — stesso Defer già accettato in Story 1.3, la scala resta entro NFR5. [app/(onboarding-import)/import-atlete/actions.ts]
- Nessun confine transazionale attorno al ciclo per-riga — recupero implicito via idempotenza reale (non dichiarata esplicitamente prima d'ora) sia dell'upsert Atleta sia del merge certificato, stessa decisione "nessun rollback automatico" di Story 1.1. [app/(onboarding-import)/import-atlete/actions.ts]

## Deferred from: code review of 1-8-riporto-under-13-nel-rollover (2026-07-17)

- Policy RLS `UPDATE` su `iscrizioni` non è column-scoped — un Ruolo autorizzato potrebbe riscrivere `atletaId`/`annoAgonisticoId` via una chiamata Supabase diretta, non solo `attiva`. Pattern preesistente identico su tutte le tabelle RLS del progetto (`atlete`, `certificati_medici`), non una regressione di questa storia — richiederebbe un intervento di hardening RLS a livello di intero progetto. [prisma/migrations/20260717130000_iscrizioni_restore_update_for_esclusione/migration.sql]
- Nessuna paginazione su `elencaAtlete`/`elencaIscrizioniPerAnno` — pattern preesistente da Story 1.6, rischio basso data la scala del progetto (NFR5, ~200 Atlete). [lib/db-rls/atleta.ts, lib/db-rls/iscrizione.ts]
- Nessuna transazione attorno al ciclo di import + riporto — un fallimento a metà non riporta i conteggi parziali (`create`/`aggiornate`/`riportate`), stesso pattern preesistente identico dal ciclo principale (Story 1.3/1.7). [app/(onboarding-import)/import-atlete/actions.ts]
- Euristica di riporto strutturalmente imprecisa: usa la `categoria` corrente dell'Atleta (non storicizzata per stagione — un cambio di categoria fuori-import la altererebbe retroattivamente), non esclude le righe scartate (`scartate`) da `codiciFiscaliImportati` (un'Under 13 respinta per un errore di formato verrebbe comunque "salvata" col dato dell'anno precedente invece di far emergere il problema), e guarda solo alla stagione immediatamente precedente per data. Limiti intrinseci al design scelto, già impliciti nell'approssimazione accettata nelle Dev Notes della storia. [app/(onboarding-import)/import-atlete/actions.ts]
- `AnnoAgonistico` senza vincolo CHECK a livello DB su `dataInizio < dataFine` — preesistente da Story 1.6, nessuno scrittore diretto esiste oggi al di fuori di `risolviAnnoAgonisticoCorrente` (che rispetta sempre l'invariante). [prisma/schema.prisma]

## Deferred from: code review of 2-1-anagrafica-palestre-e-campi (2026-07-17)

- Nessun controllo che i valori di `FormData` siano effettivamente stringhe (un campo `File` verrebbe stringificato e persistito come nome) — pattern identico in ogni Server Action del progetto fin da Story 1.1, non introdotto da questa storia. [app/(orari-palestre)/palestre/actions.ts]
- Nessuna gestione di scritture concorrenti su `aggiornaPalestra`/`aggiornaCampo` (ultima modifica vince silenziosamente, nessun controllo `updatedAt`) — stessa categoria di rischio a bassa probabilità già accettata in Story 1.3/1.4 (pochi Admin/Dirigente attivi contemporaneamente). [app/(orari-palestre)/palestre/actions.ts]
- L'elenco Ruoli `["ADMIN", "DIRIGENTE"]` è duplicato letteralmente in 4 Server Action più una volta in `route-guard.ts`, senza una costante condivisa — stesso pattern in ogni Server Action del progetto fin da Story 1.3; un'estrazione in una costante condivisa (es. `lib/auth/ruoli-gestione.ts`) sarebbe un refactor trasversale a più storie, non specifico di questa. [app/(orari-palestre)/palestre/actions.ts, lib/auth/route-guard.ts]
- Nessun messaggio di stato vuoto quando non esistono ancora Palestre o quando una Palestra non ha ancora Campi — stesso livello di rifinitura UI già accettato per ogni altra pagina-elenco del progetto (vedi Story 1.6). [app/(orari-palestre)/palestre/page.tsx, app/(orari-palestre)/palestre/PalestraRow.tsx]

## Deferred from: code review of 2-2-creazione-gruppi (2026-07-17)

- Nessun indice sulla colonna FK `annoAgonisticoId` su `gruppi` — stesso gap preesistente già presente su `campi.palestraId` (Story 2.1), scala ridotta (poche decine di Gruppi al massimo per una polisportiva). [prisma/migrations/20260717150000_add_gruppo/migration.sql]
- Il messaggio di successo dopo la creazione di un Gruppo non conferma a quale Anno Agonistico è stato collegato — un Dirigente che crea un Gruppo esattamente al confine 31 luglio/1 agosto potrebbe non accorgersi che `risolviAnnoAgonisticoCorrente()` ha appena attraversato il cambio di stagione. Stessa categoria di rischio a bassa probabilità già accettata per i confini di data dell'Anno Agonistico (vedi Story 1.6, margine UTC). [app/(gruppi-allenatori)/gruppi/NuovoGruppoForm.tsx]
- Nessun controllo che i valori di `FormData` siano effettivamente stringhe (un campo `File` verrebbe stringificato e persistito) — pattern identico in ogni Server Action del progetto fin da Story 1.1, non introdotto da questa storia (già loggato per Story 2.1). [app/(gruppi-allenatori)/gruppi/actions.ts]

## Deferred from: code review of 2-3-assegnazione-allenatori-a-gruppo (2026-07-17)

- Il percorso di scrittura (`assegnaAllenatore`) non verifica che il `gruppoId` appartenga all'Anno Agonistico corrente — solo la lettura (`page.tsx`) è filtrata; una tab del browser rimasta aperta a cavallo del cambio stagione potrebbe assegnare un Allenatore a un Gruppo di una stagione passata. Stessa categoria di rischio a bassa probabilità già accettata per i confini di data dell'Anno Agonistico (vedi Story 1.6, margine UTC). [app/(gruppi-allenatori)/gruppi/actions.ts]
- Nessun indice sulla sola colonna `allenatoreId` (solo l'indice univoco composito `(gruppoId, allenatoreId)`) — stesso gap ricorrente già presente su `campi.palestraId` (Story 2.1) e `gruppi.annoAgonisticoId` (Story 2.2), scala ridotta (poche decine di Gruppi/Allenatori al massimo). [prisma/migrations/20260717160000_add_gruppo_allenatore/migration.sql]

## Deferred from: code review of 2-4-assegnazione-atlete-a-gruppo (2026-07-17)

- Stessa categoria già deferita per `assegnaAllenatore` (Story 2.3): `assegnaAtleta` non verifica che il `gruppoId` appartenga all'Anno Agonistico corrente — usa qualunque stagione porti con sé il Gruppo indicato dal client. Una tab rimasta aperta a cavallo del cambio stagione potrebbe assegnare un'Atleta a un Gruppo di una stagione passata. Stessa categoria di rischio a bassa probabilità già accettata per i confini di data dell'Anno Agonistico (vedi Story 1.6, margine UTC). [app/(gruppi-allenatori)/gruppi/actions.ts]
- Il blocco catch-all di `assegnaAtleta` restituisce lo stesso messaggio generico ("Impossibile assegnare l'Atleta. Riprova.") sia per un `atletaId` inesistente (violazione FK, mai risolvibile con un retry) sia per un errore transitorio (timeout, deadlock) — stessa asimmetria già presente e mai corretta in `assegnaAllenatore` (Story 2.3) rispetto al percorso "Gruppo non trovato", che invece ha un messaggio dedicato. [app/(gruppi-allenatori)/gruppi/actions.ts]
- Nessuna validazione che la `categoria` dell'Atleta corrisponda alla `categoria` del Gruppo (es. un'Under 13 assegnabile a un Gruppo Under 18) — nessun AC di questa storia lo richiede, possibile regola di prodotto da valutare in futuro. [app/(gruppi-allenatori)/gruppi/actions.ts]
- Nessun controllo che l'Atleta abbia un'Iscrizione attiva per l'Anno Agonistico corrente prima di assegnarla a un Gruppo — nessun AC lo richiede oggi; un'Atleta mai rinnovata potrebbe finire nel roster di un Gruppo attivo. [app/(gruppi-allenatori)/gruppi/actions.ts]
- Nessun indice su `gruppo_atlete.annoAgonisticoId` da solo (solo l'indice univoco composito `(atletaId, annoAgonisticoId)`, che Postgres non può usare efficientemente per un filtro sulla sola seconda colonna) — stesso gap ricorrente già presente su `campi.palestraId` (Story 2.1), `gruppi.annoAgonisticoId` (Story 2.2) e `gruppo_allenatori.allenatoreId` (Story 2.3), scala ridotta (~200 Atlete al massimo). [prisma/migrations/20260717170000_add_gruppo_atleta/migration.sql]
- Il `Promise.all` in `page.tsx` non è avvolto in un try/catch (se una qualunque delle 4 query fallisse, l'intera pagina `/gruppi` andrebbe in errore) — gap preesistente e trasversale già presente da Story 2.2/2.3, non introdotto da questa storia. [app/(gruppi-allenatori)/gruppi/page.tsx]
- Nessuna indicazione nella UI del Gruppo attuale di un'Atleta prima di riassegnarla a un Gruppo diverso — AC #2 richiede esplicitamente una sostituzione silenziosa (nessun passo di conferma), ma un Dirigente potrebbe spostare per errore un'Atleta senza accorgersene. Miglioramento di prodotto/UX, non un difetto rispetto agli AC. [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx]

## Deferred from: code review of 2-5-caricamento-slot (2026-07-17)

- Stessa categoria già deferita per `assegnaAllenatore`/`assegnaAtleta` (Story 2.3/2.4), terza occorrenza: `creaSlot` non rivalida che il `gruppoId` appartenga all'Anno Agonistico corrente — una tab rimasta aperta a cavallo del cambio stagione potrebbe creare uno Slot pinnato a un Gruppo di una stagione passata, che poi sparirebbe silenziosamente dall'elenco `/slot` corrente (filtrato transitivamente via Gruppo). [app/(orari-palestre)/slot/actions.ts]
- Il `<select>` Gruppo nel form va vuoto senza spiegazione se non esiste ancora un Anno Agonistico corrente o se la stagione corrente non ha ancora Gruppi — stessa categoria di gap di stato-vuoto già accettata in Story 2.1 (Palestre/Campi). [app/(orari-palestre)/slot/NuovoSlotForm.tsx]
- Nessun vincolo di unicità/protezione da duplicati esatti su `Slot` (a differenza di `GruppoAllenatore`/`GruppoAtleta`, che hanno `@@unique` + trattamento idempotente) — un doppio submit produce due righe identiche indistinguibili in "Elenco Slot", senza vincoli DB a impedirlo. Probabilità bassa (il bottone è già `disabled={pending}`), nessun AC lo richiede. [prisma/schema.prisma]
- Nessuna Server Action di modifica o rimozione per Slot, né un passo di conferma prima della creazione — un errore di inserimento (giorno/orario/campo/gruppo sbagliato) resta per l'intera stagione senza alcun rimedio in-app. Decisione di scope deliberata (AC #1: "resta fisso per l'intero Anno Agonistico", nessun AC richiede modifica/rimozione) — da riconsiderare se il rischio di errore reale si manifestasse. [app/(orari-palestre)/slot/actions.ts]
- Nessun messaggio di stato vuoto in "Elenco Slot" quando non esistono ancora Slot per la stagione corrente — stesso livello di rifinitura UI già accettato per ogni altra pagina-elenco del progetto (Story 1.6, 2.1). [app/(orari-palestre)/slot/page.tsx]

## Deferred from: code review of 2-7-vista-orario-personale-atleta (2026-07-17)

- Nessuna esclusività sull'auto-aggancio di un'Atleta a se stessa: `GenitoreAtleta` ammette (per design, stesso comportamento già accettato per più Genitori sulla stessa Atleta, Story 1.5 AC #4) che più account diversi si colleghino alla stessa Atleta fornendo lo stesso Codice Fiscale — a differenza del flusso Allenatore (`!allenatore.utenteId` prima del link). Impatto limitato: la vista collegata è di sola lettura e mostra solo dati di orario (giorno/ora/campo/gruppo), non dati sanitari o identitari. Da riconsiderare se emergesse un caso reale di doppio aggancio. [app/(onboarding-import)/registrati/actions.ts]
- Se un Utente selezionasse sia GENITORE sia ATLETA e inserisse lo stesso Codice Fiscale in entrambi i campi (per errore), la seconda `genitoreAtleta.create` violerebbe `@@unique([utenteId, atletaId])` (P2002 non gestito esplicitamente) — stesso pattern "nessun rollback automatico" già accettato in tutto `registrati/actions.ts` fin da Story 1.1. [app/(onboarding-import)/registrati/actions.ts]
- Ordine dei campi nel form (`codiceFiscaleAtleta` mostrato sopra `codiceFiscaleFiglio`) non allineato all'ordine di validazione in `actions.ts` (blocco GENITORE validato per primo) — con entrambi i Ruoli selezionati e entrambi i campi vuoti, l'utente vede prima l'errore sul campo più in basso nel form. Cosmetico, richiede comunque più tentativi con o senza il riordino. [app/(onboarding-import)/registrati/page.tsx, actions.ts]
- Se lo stesso Utente ha sia un aggancio "a se stessa" (Ruolo Atleta) sia ad altre Atlete come Genitore, `/mio-orario` unisce tutti gli Slot in un'unica lista senza etichettare a quale Atleta appartiene ciascuna riga — decisione già esplicitamente accettata e documentata nelle Dev Notes della storia stessa, non un'omissione. [app/(orari-palestre)/mio-orario/page.tsx]
- `orderBy` a livello Prisma mantenuto su entrambe le query Slot (ramo Allenatore e ramo Atleta) pur essendo di fatto ridondante, dato che il risultato unito viene comunque riordinato lato JS da `unisciESordinaSlot` — innocuo, nessun impatto sulla correttezza. [app/(orari-palestre)/mio-orario/page.tsx]

## Deferred from: code review of 2-6-vista-orario-personale-allenatore (2026-07-17)

- Nessun `try/catch` attorno alle chiamate Supabase/Prisma di `mio-orario/page.tsx` — gap preesistente e trasversale a tutta l'app (nessun `error.tsx`), già loggato in Story 1.2. [app/(orari-palestre)/mio-orario/page.tsx]
- Se `user` è `null` (sessione scaduta nella finestra tra `proxy.ts` e la chiamata `getUser()` di questa pagina) o se esiste un `user` Supabase senza una riga `Utente` corrispondente, la pagina mostra lo stesso messaggio "non ancora collegato a un profilo Allenatore" pensato per un caso diverso (AC #2) — probabilità bassa, nessun crash, solo un messaggio impreciso per uno stato dati patologico. [app/(orari-palestre)/mio-orario/page.tsx]
- Se `trovaAnnoAgonisticoCorrente()` restituisce `null` (nessuna stagione corrente configurata), la pagina mostra lo stesso messaggio "Nessuno Slot ancora assegnato" di un elenco genuinamente vuoto — stessa categoria di imprecisione di stato-vuoto già accettata in Story 2.1/2.5. [app/(orari-palestre)/mio-orario/page.tsx]
- `Utente.attivo = false` è verificato solo al login, non ad ogni richiesta — un Allenatore disattivato a metà sessione può continuare a vedere il proprio orario fino a un nuovo login. Decisione già esplicita altrove nella codebase (AD-11, staleness del JWT accettata). [app/(orari-palestre)/mio-orario/page.tsx]
- Query Prisma senza `select` esplicito (`allenatore`, `campo`/`palestra`/`gruppo` via `include`) — recuperano colonne non usate in JSX (es. `codiceFiscale`, `email`, `createdAt`). Nessun rischio di esposizione al client (Server Component, i dati non attraversano mai il confine server/client come props), solo un'inefficienza di query trascurabile alla scala del progetto (~200 utenti). [app/(orari-palestre)/mio-orario/page.tsx]
- Markup tabellare e forma della query Slot duplicati (non estratti in un componente/helper condiviso) tra `mio-orario/page.tsx` e `slot/page.tsx` — solo la seconda occorrenza di un pattern simile, prematuro estrarre un'astrazione ora; da riconsiderare se una terza vista analoga comparisse (es. Story 2.7/2.8). [app/(orari-palestre)/mio-orario/page.tsx, app/(orari-palestre)/slot/page.tsx]
- Il messaggio di stato vuoto ("Nessuno Slot ancora assegnato ai tuoi Gruppi") non distingue "nessuna assegnazione a un Gruppo", "assegnazione solo a Gruppi di stagioni passate" e "assegnazione corrente ma nessuno Slot ancora caricato da un Dirigente" — miglioramento di prodotto/UX, nessun AC lo richiede. [app/(orari-palestre)/mio-orario/page.tsx]
- `trovaAnnoAgonisticoCorrente()` viene risolta in sequenza dopo la catena identità→Allenatore invece che in parallelo (non dipende da `allenatore`) — micro-ottimizzazione, query singola e leggera, nessun impatto misurabile alla scala del progetto. [app/(orari-palestre)/mio-orario/page.tsx]
- `oraInizio`/`oraFine` renderizzati come stringhe grezze senza validazione/fallback in lettura — il solo percorso di scrittura esistente (`creaSlot`) valida già il formato; nessuno scenario reale produce oggi un valore malformato. [app/(orari-palestre)/mio-orario/page.tsx]

## Deferred from: code review of 2-8-vista-orari-trasversale-segreteria (2026-07-17)

- Il `<select>` Gruppo non viene ristretto ai soli Gruppi che hanno effettivamente Slot nella Palestra selezionata — combinazioni Palestra+Gruppo che non coesistono mai portano a "Nessuno Slot trovato" senza indicare quale filtro sia la causa. Costruire una narrowing dinamica richiederebbe JS lato client o una modellazione aggiuntiva (Gruppo non ha una relazione diretta con Palestra); sproporzionato per una feature Should/differibile a v1.1. [app/(orari-palestre)/orari/page.tsx]
- "Nessuno Slot trovato." è identico sia per un filtro troppo restrittivo sia per l'assenza di un Anno Agonistico corrente — stessa categoria di imprecisione di stato-vuoto già accettata più volte (Story 2.1, 2.5, 2.6, 2.7). [app/(orari-palestre)/orari/page.tsx]
- Nessun indicatore in pagina di quale Anno Agonistico sia mostrato — gap sistemico e trasversale a `/slot`, `/mio-orario`, `/orari` (nessuna di queste pagine lo mostra), non introdotto da questa storia. [app/(orari-palestre)/orari/page.tsx]
- `palestraId`/`gruppoId` da `searchParams` usati come filtro Prisma senza validare che l'id esista ancora nell'elenco corrente — un link salvato/manomesso con un id non più valido degrada silenziosamente a un risultato vuoto invece di segnalare "filtro non più valido". Rischio pressoché teorico: oggi non esiste alcuna funzionalità di eliminazione per Palestra/Campo/Gruppo, quindi un id "stale" è raggiungibile solo manomettendo l'URL a mano. [app/(orari-palestre)/orari/page.tsx]
- Nessuna voce di navigazione/menu in tutta l'app collega a `/slot`, `/mio-orario` o `/orari` — le pagine sono raggiungibili solo digitando l'URL. Gap sistemico dell'intera applicazione (nessuna storia ha mai costruito una navigazione), non specifico di questa storia. [intera applicazione]

## Deferred from: code review of 3-1-registrazione-presenze (2026-07-18)

- `ON DELETE CASCADE` da `presenze` verso `atlete`/`slot` cancella silenziosamente lo storico presenze se l'Atleta o lo Slot vengono eliminati — stesso pattern già usato per `Iscrizione`/`CertificatoMedico`, non una deviazione introdotta da questa storia, ma un rischio da tenere presente per Story 3.2/3.3 (storico/trend, costruite su questo storico). [prisma/migrations/20260717190000_add_presenza/migration.sql]
- Nessun indice con `slotId`+`data` come colonne guida (l'unico indice è l'UNIQUE su `atletaId, slotId, data`) — `leggiPresenzePerSlotEData` non può sfruttarlo appieno; non rilevante alla scala attuale (NFR PRD §8, un Gruppo ha tipicamente 10-20 Atlete). [prisma/migrations/20260717190000_add_presenza/migration.sql]
- La nuova policy SELECT `allenatore_proprie_atlete_select` su `atlete` espone tutte le colonne (non solo id/nome) a un Allenatore per le proprie Atlete — stesso livello di esposizione già presente nella policy ADMIN/DIRIGENTE/SEGRETERIA esistente su `atlete`, non un pattern nuovo introdotto da questa storia. Da riconsiderare se emergesse la necessità di una restrizione a livello di colonna. [prisma/migrations/20260717200000_atlete_allenatore_select/migration.sql]
- Le policy RLS di scrittura per ADMIN/DIRIGENTE/SEGRETERIA su `presenze` non sono mai state esercitate nella verifica dal vivo (solo il rifiuto di route-guard è stato verificato, nessuna UI per questi Ruoli in questa storia) — stesso pattern boilerplate già collaudato su `atlete`/`iscrizioni`/`certificati_medici`. [prisma/migrations/20260717190000_add_presenza/migration.sql]
- Nessuna validazione di validità calendariale oltre al formato regex per `data` (es. "2026-02-30" viene normalizzata silenziosamente da `Date`) — stessa scelta deliberata già presa per `oraInizio`/`oraFine` (Story 2.5); il confronto con `slot.giorno` intercetta comunque la quasi totalità dei casi reali. [app/(presenze)/presenze/actions.ts]
- I test di `route-guard.test.ts` per `/presenze` non coprono esplicitamente il rifiuto per DIRIGENTE/SEGRETERIA (solo ATLETA e ADMIN) — copertura minima già accettata altrove nel progetto. [lib/auth/route-guard.test.ts]

## Deferred from: code review of 3-2-storico-presenze-per-atleta (2026-07-18)

- Il ramo Allenatore di `/storico-presenze` risolve il roster solo per l'Anno Agonistico corrente — un coach perde l'accesso UI allo storico di un'Atleta passata a un altro Gruppo/Allenatore in una stagione successiva, anche se lo storico esiste ancora. Stessa scelta di scoping già applicata al selettore Slot di `/presenze` (Story 3.1). [app/(presenze)/storico-presenze/page.tsx]
- La colonna "Giorno" dello storico è derivata dal `Slot.giorno` corrente, non dal giorno della settimana calcolato dalla `data` storica della Presenza — se in futuro esistesse una funzionalità di modifica dello Slot, lo storico potrebbe retroattivamente mostrare un giorno errato per righe passate. Nessuna funzionalità di modifica Slot esiste oggi. [app/(presenze)/storico-presenze/page.tsx]
- Nessuna paginazione su `leggiStoricoPresenzePerAtleta` — alla scala attuale (una stagione = poche decine di Slot per Atleta) non rilevante; da riconsiderare se lo storico dovesse estendersi su più stagioni. [lib/db-rls/presenza.ts]
- Query duplicata se lo stesso Utente compare in entrambe le sezioni della pagina (es. un Allenatore che allena anche se stessa) — nessuna memoizzazione tra la sezione "Il mio storico" e "Storico delle mie Atlete". Caso raro, nessun impatto di correttezza. [app/(presenze)/storico-presenze/page.tsx]

## Deferred from: code review of 4-1-upload-certificato-medico (2026-07-18)

- Se `caricaFileCertificato` riesce ma il successivo `collegaFileCertificato` fallisce, il file resta orfano nel bucket senza alcuna riga DB che lo referenzi e senza pulizia automatica — caso raro (fallimento tra due chiamate consecutive), nessun rollback implementato. Bucket privato, nessun costo di esposizione, solo di storage. [app/(certificati-medici)/certificato-medico/actions.ts]
- `collegaFileCertificato` rigenera un nuovo `id` ad ogni upsert (incluso un ri-caricamento) — stesso "churn" già accettato per `Presenza` (Story 3.1), ma Story 4.4 (validazione Segreteria) potrebbe in futuro aver bisogno di riferirsi a "quale caricamento specifico è stato validato" — da rivalutare esplicitamente quando quella storia verrà scritta. [lib/db-rls/certificato-medico.ts]
- Gli oggetti nel bucket Storage non vengono rimossi automaticamente quando la riga `CertificatoMedico`/`Atleta` corrispondente viene eliminata — nessuna relazione FK tra lo schema `public` di Postgres e `storage.objects`. Un file caricato può restare orfano nel bucket anche dopo la cancellazione dei dati collegati. [lib/storage/certificati.ts]

## Deferred from: code review of 4-2-notifica-automatica-upload (2026-07-18)

- Nessun indice esplicito su `notifiche.createdAt` per il carico di `ORDER BY` su una tabella che cresce senza limite (nessuna deduplicazione/stato "letta", scelta deliberata). Nessuna migrazione di questo progetto ha mai aggiunto un indice non-unique esplicito — aggiungerne uno qui sarebbe una deviazione isolata dalla convenzione del progetto, non richiesta da alcun AC. Da rivalutare se/quando la tabella raggiungerà una scala reale (NFR PRD §8: nessuna preoccupazione di volume alla scala attuale di un singolo club). [prisma/migrations/20260718050000_add_notifica/migration.sql]
- Nessun campo che registri **chi** (Genitore vs Atleta stessa) ha innescato l'upload che ha generato la notifica — informazione non recuperabile retroattivamente se una storia futura volesse distinguerlo nel messaggio mostrato. Speculativo, nessun AC attuale lo richiede. [lib/db-rls/notifica.ts, prisma/schema.prisma]

## Deferred from: code review of 7-1-configurazione-smtp (2026-07-18)

- Nessuna coercizione difensiva se un campo del `FormData` atteso come testo arriva come `File` (`String(File)` produce `"[object File]"`, salvato/usato silenziosamente) — rischio reale ma basso: solo un Admin autenticato potrebbe auto-infliggerselo con una richiesta manomessa, nessun bypass di sicurezza, solo dati corrotti auto-inflitti. [app/(configurazione)/smtp/actions.ts]
- Nessun audit trail (chi ha modificato la configurazione SMTP, quando, valore precedente) — speculativo, nessun AC lo richiede, da valutare se una storia futura di compliance/audit lo richiedesse esplicitamente. [prisma/schema.prisma]
- Nessun rate limiting su `inviaEmailDiProva` — Admin-only, rischio speculativo di abuso di un account già fidato (uso ripetuto potrebbe far scattare anti-abuso lato provider SMTP, es. Aruba). [app/(configurazione)/smtp/actions.ts]

## Deferred from: code review of 7-2-configurazione-logo-applicazione (2026-07-18)

- Nessuna pre-validazione lato client in `LogoForm.tsx` oltre il filtro nativo `accept` dell'`<input type="file">` (banalmente aggirabile) — miglioramento UX (feedback immediato invece di un round-trip al server), non richiesto da alcun AC, la validazione server-side (bucket + applicativa) resta comunque l'unica autorità. [app/(configurazione)/logo/LogoForm.tsx]

## Deferred from: code review of 4-3-mail-automatica-alla-segreteria (2026-07-20)

- Nessuna verifica che la cifratura SMTP (`sicura`) corrisponda realmente a host/porta configurati (un Admin potrebbe impostare la porta 25 con `sicura: false`, trasmettendo il Certificato in chiaro) — superficie di configurazione preesistente da Story 7.1, non introdotta da questa storia. [lib/db-rls/configurazione-smtp.ts]
- `nodemailer.createTransport` viene ricreato ad ogni chiamata a `inviaEmail`, senza pooling di connessione — pattern preesistente da Story 7.1 (questa storia lo consuma per la prima volta con un vero allegato), diventerà più rilevante con gli invii multipli di Story 4.6 (promemoria scadenza a più Ruoli). [lib/email/invia-email.ts]

## Deferred from: code review of 4-5-alert-scadenza-non-bloccante (2026-07-22)

- Nessun test automatico end-to-end verifica dal vivo che la nuova policy RLS su `certificati_medici` isoli davvero un Allenatore dalle Atlete di un Gruppo non proprio (AC #5) — stessa categoria già accettata più volte in questo progetto (nessuna infrastruttura di test di integrazione contro Postgres reale), rischio basso perché la stessa funzione `allenatore_possiede_atleta` è già in produzione senza problemi di isolamento dalla Story 3.1. [prisma/migrations/20260722000000_certificati_allenatore_select/migration.sql]
- Nessun indicatore distinto per un Certificato `IN_ATTESA` (ri-caricato, in attesa di riconferma della Segreteria, Story 4.4) rispetto a uno realmente `CONFERMATO` — l'Allenatore vede lo stesso "non scaduto" in entrambi i casi se la vecchia data non è ancora passata. Scelta architetturale deliberata di questa storia (separazione di responsabilità da Story 4.4, vedi Dev Notes), non un difetto — possibile idea di prodotto per una storia futura se si rivelasse un problema reale in pratica. [app/(presenze)/presenze/certificato-scaduto.ts]

## Deferred from: code review of 4-4-conferma-validazione-certificato (2026-07-22)

- Nessuna rimozione del vecchio file dal bucket quando la Segreteria sostituisce lo scan allegato durante una conferma (a differenza del ri-caricamento Genitore/Atleta, Story 4.1 review fix, che rimuove esplicitamente il vecchio file) — decisione già esplicita nei Dev Notes della storia stessa, nessun AC la richiede. [app/(certificati-medici)/conferma-certificati/actions.ts]
- Il default `CONFERMATO` della migrazione dello `stato` marca retroattivamente come confermate eventuali righe create dal solo upload (Story 4.1) e mai davvero verificate da un umano, prima che questa storia esistesse — accettabile perché nessun dato di produzione reale esiste ancora (progetto non ancora lanciato), decisione già esplicita nel Prerequisito architetturale #1 della storia. [prisma/migrations/20260721000000_add_stato_certificato_medico]
- Race TOCTOU tra un ri-caricamento (Genitore/Atleta, che riporta lo stato a `IN_ATTESA`) e una conferma in corso basata su dati ormai stantii (Segreteria) — stessa categoria di rischio a bassa probabilità già accettata più volte in questo progetto (Story 1.4/1.5, singolo Admin/piccola società). [app/(certificati-medici)/conferma-certificati/actions.ts, lib/db-rls/certificato-medico.ts]
- Nessun rollback/pulizia del file appena caricato se la successiva scrittura su `certificati_medici` fallisce nel flusso di conferma manuale — stesso gap già accettato per l'upload di Story 4.1 (vedi sopra, "Deferred from: code review of 4-1-upload-certificato-medico"), qui duplicato in un secondo punto d'ingresso. [app/(certificati-medici)/conferma-certificati/actions.ts]

## Deferred from: code review of 3-3-storico-presenze-con-trend-percentuale (2026-07-18)

- Il trend può cambiare a seguito di un semplice ri-invio (correzione) di una Presenza sulla stessa data, non di un cambio reale di presenza: `leggiStoricoPresenzePerAtleta` usa `id` come spareggio d'ordinamento per righe con la stessa `data`, ma `Presenza.id` viene rigenerato ad ogni upsert (Story 3.1) — se due Slot cadono nello stesso giorno per la stessa Atleta e una correzione tardiva sposta la posizione relativa di una riga vicino al punto di taglio a metà, il trend visualizzato può cambiare senza che sia cambiato nulla di sostanza. Scenario raro, fix richiederebbe allargare la firma di `calcolaStatistichePresenza` oltre `{ presente: boolean }[]` per ordinare esplicitamente per data — fuori dal footprint minimo di questa storia. [app/(presenze)/storico-presenze/calcola-statistiche-presenza.ts]
- Nessuna soglia minima di campione oltre il caso singola riga: con uno storico di 2-3 Presenze, un solo cambio può spostare la percentuale di una metà di 50-100 punti, superando ampiamente la soglia di 10 punti e producendo "in-crescita"/"in-calo" da un campione statisticamente irrilevante — comportamento consentito dall'algoritmo così come specificato, già esplicitamente inquadrato come "regolabile in futuro se si rivelasse poco utile in pratica". [app/(presenze)/storico-presenze/calcola-statistiche-presenza.ts]

## Deferred from: code review of 8-6-gruppi-dati-atleta-e-iscrizioni (2026-07-24)

- ~70 righe di CSS quasi duplicate tra i 4 nuovi CSS module della storia (`.sezione`/`.campo`/`.bottone`/`.errore`/`.successo`/`.tabella`) — coerente con la scelta architetturale dell'Epic 8 di un CSS module indipendente per pagina senza condivisione tra moduli, stesso pattern già accettato in Story 8.5. [app/(gruppi-allenatori)/gruppi/gruppi.module.css, app/(dati-atleta)/dati-fisici/dati-fisici.module.css, app/(iscrizioni)/conferma-iscrizioni/conferma-iscrizioni.module.css]
- `align-self: flex-start` su `.bottone` è CSS morto in ogni suo utilizzo (il genitore `<form>` non è mai un contenitore flex in questa storia) — pattern copiato identico in ogni CSS module dell'Epic 8 dalla Story 8.2 in poi, zero impatto visivo, non una deviazione introdotta da questa storia. [app/(gruppi-allenatori)/gruppi/gruppi.module.css, app/(dati-atleta)/dati-fisici/dati-fisici.module.css, app/(gruppi-allenatori)/wizard-nuova-stagione/wizard-nuova-stagione.module.css]

## Deferred from: code review of 8-7-amministrazione-configurazione-e-pagine-condivise (2026-07-24)

- `logo.module.css` non applica il fix `::file-selector-button` per `input[type="file"]` già stabilito in `import-atlete.module.css` (Story 8.2) — pattern applicato una sola volta nell'intero progetto, non riproposto neanche in `certificato-medico.module.css` (Story 8.5) o `conferma-certificati.module.css`, incoerenza pre-esistente non introdotta da questa storia. [app/(configurazione)/logo/logo.module.css]
- Ulteriore duplicazione di `.campo`/`.bottone`/`.errore`/`.successo` in 3 nuovi CSS module — stessa categoria già accettata come defer in Story 8.5/8.6, coerente con la scelta architetturale dell'Epic 8 di un CSS module indipendente per pagina. [app/(amministrazione)/admin/admin.module.css, app/(configurazione)/smtp/smtp.module.css, app/(configurazione)/logo/logo.module.css]
- `.anteprimaLogo` non riserva spazio (`width`/`height`/`aspect-ratio`) per l'`<img>` del logo, possibile layout shift al caricamento — nessun precedente nel progetto per dimensionamento intrinseco immagini. [app/(configurazione)/logo/logo.module.css]
- Nessuno stato `:hover` sui pulsanti nuovi di questa storia — gap sistemico su tutti i pulsanti dell'intero Epic 8 dalla Story 8.2 in poi, non introdotto da questa storia. [app/(amministrazione)/admin/admin.module.css, app/(configurazione)/smtp/smtp.module.css, app/(configurazione)/logo/logo.module.css]

## Deferred from: code review of 8-5-certificati-medici (2026-07-24)

- `CONFERMATO` con `dataFineValidita` null verrebbe mostrato come badge success "Certificato in regola" senza data di validità — non raggiungibile dai path di scrittura reali (`confermaCertificato` richiede sempre una data, validata in `actions.ts` con regex+parsing prima del salvataggio), unico caso teorico è il default retroattivo della migrazione di Story 4.4 (già deferred, nessun dato reale di produzione esiste ancora). [app/(certificati-medici)/certificato-medico/stato-certificato-visualizzato.ts]
- Una `dataFineValidita` malformata produrrebbe il testo "Invalid Date" in UI — nessun path di scrittura reale la produce (stessa validazione di cui sopra), stessa categoria di rischio teorico già accettata più volte nel progetto (dato ipoteticamente corrotto ma nessuno scrittore reale lo produce). [app/(certificati-medici)/certificato-medico/stato-certificato-visualizzato.ts, page.tsx]
- Il `.dropzone` con bordo tratteggiato suggerisce visivamente drag-and-drop ma non è cablato (nessun `onDrop`/`onDragOver`) — markup della zona upload deliberatamente invariato per vincolo esplicito della storia (nessuna nuova logica interattiva in questa eccezione al vincolo "solo className"), l'input file nativo resta pienamente funzionante al click. [app/(certificati-medici)/certificato-medico/CaricaCertificatoForm.tsx]
- `.testoStato` (classe pensata per la riga di stato del certificato) viene riusata anche per il messaggio istruzionale "Seleziona un'Atleta per gestire il suo Certificato" — accoppiamento semantico a basso rischio tra un nome di classe di dominio e un uso generico, nessun impatto visivo oggi ma un futuro restyle di uno dei due usi potrebbe rompere silenziosamente l'altro. [app/(certificati-medici)/certificato-medico/page.tsx]
- `.bottone`/`.bottoneCarica` condividono 8 dichiarazioni CSS su 9 nello stesso file, pattern quasi identico ripetuto una terza volta in `conferma-certificati.module.css` — coerente con la scelta architetturale dell'Epic 8 di un CSS module indipendente per pagina senza condivisione tra moduli, non una deviazione introdotta da questa storia. [app/(certificati-medici)/certificato-medico/certificato-medico.module.css, app/(certificati-medici)/conferma-certificati/conferma-certificati.module.css]
- Il nuovo `<p className={styles.uploadLabel}>Carica un nuovo certificato</p>` non è un heading semantico, invisibile alla navigazione per heading di uno screen reader — fedele al mockup approvato (`key-certificato-medico.html`, che usa anch'esso `<p class="upload-label">`), non una regressione introdotta da questa storia. [app/(certificati-medici)/certificato-medico/CaricaCertificatoForm.tsx]
- Nessuna indicazione del numero di Presenze (N) su cui si basano percentuale/trend — un Allenatore non può distinguere a colpo d'occhio "100% su 1 sola Presenza" da "100% su 50", rilevante per l'uso dichiarato (supporto rapido alle scelte di formazione, FR-10) ma nessun AC lo richiede. [app/(presenze)/storico-presenze/page.tsx]
- Nessuna distinzione visiva (colore/icona) tra le tre direzioni del trend, solo testo semplice — un Allenatore che scorre più Atlete deve leggere la parola per ciascuna riga invece di riconoscere un colore/icona, in tensione con il "rapido" della user story ma nessun AC lo richiede esplicitamente. [app/(presenze)/storico-presenze/page.tsx]

## Deferred from: code review of story-4-6-promemoria-scadenza (2026-07-22)

- Il corpo dell'email di promemoria identifica l'Atleta solo per `nome` (nessun cognome/matricola per disambiguare atlete omonime) — stesso identico pattern già usato in `caricaCertificato` (Story 4.3, `app/(certificati-medici)/certificato-medico/actions.ts`), non una regressione introdotta da questa storia; una correzione isolata qui lascerebbe l'altra email inconsistente, meglio affrontarlo trasversalmente in una storia futura. [app/api/cron/promemoria-certificati/route.ts]

## Deferred from: code review of story-5-1-vista-d-insieme-dirigente (2026-07-23)

- Nessuna posizione condivisa (`lib/`) per `calcolaGiorniAScadenza`, riusata da una cartella di un Route Handler (`api/cron/promemoria-certificati/`) — deliberato e documentato nei Prerequisiti della Story 5.1, ma un futuro refactor/rimozione del Cron potrebbe rompere silenziosamente `vista-dirigente`; da consolidare in un modulo condiviso (es. `lib/certificati-medici/`) in una storia trasversale futura. [app/(amministrazione)/vista-dirigente/categorizza-stato-certificato.ts]
- Nessun `try/catch`/`error.tsx` attorno alle letture `Promise.all` di `vista-dirigente/page.tsx` — stesso pattern (assente) già presente in ogni altra pagina di questa codebase (`gruppi/page.tsx`, `orari/page.tsx`, `admin/page.tsx`), non una regressione specifica di questa storia; se mai affrontato, andrebbe risolto trasversalmente per tutte le pagine insieme. [app/(amministrazione)/vista-dirigente/page.tsx]
- Il quarto bucket `SENZA_CERTIFICATO`/"da verificare" (Story 5.1) non ha un token dedicato in `DESIGN.md` — disclosurato esplicitamente nei Dev Notes della storia come scelta necessaria (i tre bucket originali dell'AC non coprono tutte le Atlete), non un'omissione silenziosa. [app/(amministrazione)/vista-dirigente/vista-dirigente.module.css]
- Cast di tipo non sicuro (`as string | null`) su dati non tipizzati restituiti da `elencaCertificati`/`elencaAtlete` — pattern preesistente in tutta la codebase (nessuna tipizzazione esplicita del client Supabase su queste query), non introdotto da Story 5.1. [app/(amministrazione)/vista-dirigente/page.tsx]
- Residui dello scaffold `create-next-app` non toccati (`<title>Create Next App</title>`, font Geist caricati ma mai applicati in `app/layout.tsx`) — esplicitamente fuori perimetro di Story 5.1 (nav-bar/layout globale, retrofit futuro separato dopo Epic 5/6). [app/layout.tsx]

## Deferred from: code review of story-5-2-permessi-granulari-su-dati-sanitari (2026-07-23)

- `/conferma-certificati` (Story 4.4) mostra un'Atleta con Certificato già `CONFERMATO` ma fuori dallo scope del Dirigente come "Da confermare" (falso, non solo mancante) — pagina preesistente non progettata per il concetto di scoping introdotto da Story 5.2; richiederebbe un ripensamento della pagina stessa (distinguere "nessun certificato" da "certificato nascosto dal permesso"). [app/(certificati-medici)/conferma-certificati/page.tsx]
- `confermaCertificato`/`aggiornaCertificato` (`lib/db-rls/certificato-medico.ts`, condivisa da tutti i Ruoli) possono restituire successo senza aver scritto nulla se la RLS nega silenziosamente l'upsert (controllano solo `error`, mai le righe modificate) — gap preesistente da prima di Story 5.2, reso raggiungibile in pratica solo ora che un Dirigente può essere scoped; il fix riguarda una funzione condivisa usata da ogni Ruolo. [lib/db-rls/certificato-medico.ts]
- Le policy su `storage.objects` per il file del certificato (PDF/immagine) restano non scoped per il Dirigente — solo la riga di metadata in `certificati_medici` è limitata da Story 5.2, non il documento reale. Rilevante perché la storia riguarda esplicitamente "permessi granulari sui dati sanitari". [prisma/migrations/20260718020000_certificati_storage_e_rls/migration.sql]
- Le email di promemoria scadenza (Story 4.6, Cron) notificano ogni Dirigente attivo di ogni certificato in scadenza, ignorando la restrizione configurata da Story 5.2 — bypass della granularità per il canale email. [app/api/cron/promemoria-certificati/route.ts]
- Un Utente con Ruolo Dirigente **e anche** Segreteria o Admin non è mai limitato dalla restrizione (le policy RLS si combinano in OR) — comportamento corretto e voluto (Segreteria/Admin restano sempre ad accesso pieno per decisione esplicita), ma non ovvio leggendo solo il codice; da documentare più esplicitamente se mai desse adito a confusione in pratica. [prisma/migrations/20260724000000_dirigente_certificati_scoped_select/migration.sql]
- Le checkbox del form `/permessi-certificati` non si risincronizzano dopo `revalidatePath` in scenari di modifica concorrente (componente non controllato) — rischio basso, stessa assunzione "singolo Admin" già accettata in storie precedenti. [app/(amministrazione)/permessi-certificati/PermessiCertificatiForm.tsx]

## Deferred from: code review of 6-1-dati-antropometrici-e-test-fisici (2026-07-23)

- `allenatore_possiede_atleta` non è scoped per stagione — un Allenatore assegnato a un'Atleta solo nella stagione corrente eredita comunque l'accesso in lettura all'intero storico misurazioni (incluse righe di stagioni/allenatori precedenti). Funzione condivisa e preesistente (Story 3.1/4.5): lo stesso comportamento è già presente per Presenza e Certificato Medico, non introdotto da questa storia — un fix andrebbe fatto a livello di funzione condivisa, non per una singola tabella. [prisma/migrations/20260725000000_add_misurazione_atleta/migration.sql]
- `FORMATO_DATA` (`/^\d{4}-\d{2}-\d{2}$/`) valida solo la forma della data, non la validità calendariale (es. "2026-13-45" verrebbe salvata così com'è) — stesso pattern già in uso identico in `registraPresenze` (Story 3.1), non specifico di questa storia. [app/(dati-atleta)/dati-fisici/actions.ts]
- Nessun `try/catch` attorno alle query di lettura in `page.tsx` (un errore RLS/DB fa crashare l'intera pagina, inclusa la sezione già renderizzata) — replica esatta dello stesso pattern non gestito in `storico-presenze/page.tsx` (Story 3.2), mai risolto lì. [app/(dati-atleta)/dati-fisici/page.tsx]
- `atletaIds[0]` risolto da `genitoreAtleta.findMany` senza `orderBy` — spareggio non deterministico se mai venisse violata l'integrità dei dati (due righe `autoAggancio` per lo stesso Utente) — replica esatta dello stesso pattern di `storico-presenze/page.tsx`. [app/(dati-atleta)/dati-fisici/page.tsx]
- Nessuna protezione da doppio invio del form (nessun reset dei campi dopo il submit, nessun vincolo di unicità a livello DB) — coerente con la convenzione dell'intero progetto: nessun form di questa codebase ha una protezione simile. [app/(dati-atleta)/dati-fisici/MisurazioneForm.tsx]
- `leggiMisurazioniPerAtleta` interroga senza `LIMIT`/paginazione — stessa convenzione già accettata per `leggiStoricoPresenzePerAtleta`; prematuro per la scala attuale hobby/piccola società. [lib/db-rls/misurazione-atleta.ts]
- Un Allenatore senza Atlete nella stagione corrente vede solo "Seleziona..." nel `<select>`, senza un messaggio esplicito — stesso gap già presente in `storico-presenze/page.tsx`, non introdotto da questa storia. [app/(dati-atleta)/dati-fisici/page.tsx]

## Deferred from: code review of 6-2-grafico-progresso-test-fisici (2026-07-23)

- `calcolaCoordinateGrafico` non valida che `padding * 2 < larghezza/altezza` — se un chiamante futuro passasse dimensioni sbagliate lo scaling andrebbe negativo (geometria invertita/collassata). L'unico chiamante reale oggi (`GraficoMisurazione.tsx`) usa valori fissi e sicuri (280/120/24); rischio solo per un ipotetico riuso futuro della funzione, esportata dal barrel `lib/misurazioni`. [lib/misurazioni/calcola-coordinate-grafico.ts]
- `calcolaCoordinateGrafico` non filtra valori non finiti (NaN/Infinity) — produrrebbe coordinate NaN se mai ricevesse un valore non valido. Non raggiungibile oggi: `app/(dati-atleta)/dati-fisici/actions.ts` (Story 6.1) già valida `Number.isFinite(valore)` prima che qualunque dato raggiunga il database. [lib/misurazioni/calcola-coordinate-grafico.ts]
- Nessun limite/decimazione sul numero di punti per tipo in un grafico — uno storico lungo (anni di misurazioni mensili) affollerebbe un grafico a dimensione fissa 280×120 in un'unica polyline illeggibile. Stesso principio già accettato per l'assenza di `LIMIT`/paginazione nello storico testuale (Story 6.1, deferred identico) — scala NFR del progetto (~200 atlete) rende il caso poco plausibile nel breve termine. [lib/misurazioni/raggruppa-per-tipo.ts, app/(dati-atleta)/dati-fisici/GraficoMisurazione.tsx]

## Deferred from: code review of 6-3-wizard-nuova-stagione (2026-07-23)

- Il controllo "la stagione ha già Gruppi" in `confermaWizardNuovaStagione` è un check-then-insert senza vincolo unico a livello DB — due submit concorrenti (doppio click, due tab) potrebbero entrambi superare il controllo e copiare due volte. Stessa classe di race a bassa probabilità già accettata più volte in questo progetto (Story 1.3/1.4/1.5), contesto singolo Admin/Dirigente attivo. [app/(gruppi-allenatori)/wizard-nuova-stagione/actions.ts]
- `page.tsx` e `confermaWizardNuovaStagione` calcolano l'intervallo di stagione indipendentemente (`calcolaIntervalloStagioneCorrente(new Date())` chiamato due volte) — se il confine del 1° agosto viene attraversato tra il caricamento della pagina e il click su "Conferma", ciò che viene copiato potrebbe differire silenziosamente da quanto mostrato nell'anteprima. Finestra estremamente stretta (esattamente la mezzanotte UTC del 1° agosto). [app/(gruppi-allenatori)/wizard-nuova-stagione/page.tsx, actions.ts]
- Nessuna funzionalità per rinominare/eliminare un Gruppo esiste in nessuna pagina dell'app — `/gruppi` (Story 2.2) mostra `nome`/`categoria` come testo statico, non editabile. Un Gruppo copiato dal wizard con una `categoria` non più corretta (es. una fascia d'età che cambia da una stagione all'altra) non ha oggi alcun percorso di correzione. Gap preesistente da Story 2.2 (non introdotto dalla Story 6.3), diventa solo più visibile perché il wizard crea più Gruppi automaticamente invece che uno alla volta digitato a mano. [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx]

## Deferred from: code review of 8-1-layout-globale-e-barra-di-navigazione (2026-07-23)

- `x-pathname` è impostato su `response.headers` in `proxy.ts`, non su `request.headers` come da pattern Next.js documentato per rendere un valore leggibile via `headers()` in un Server Component — staticamente "non dovrebbe funzionare" per convenzione, ma verificato dal vivo 3 volte contro il dev server reale (nascondimento barra su `/non-autorizzato` e stato attivo della voce corrente, inclusa un'ispezione diretta dell'HTML) e confermato funzionante in questa versione di Next.js 16/Turbopack. Nota di manutenzione futura: potrebbe comportarsi diversamente con un aggiornamento di Next.js o un runtime/adapter diverso (es. `@opennextjs/cloudflare` in produzione) — ri-verificare se il comportamento della barra su `/non-autorizzato`/lo stato attivo smettesse di funzionare dopo un aggiornamento. [proxy.ts, app/NavBar.tsx]
- Nessun `aria-current="page"` sulla voce di navigazione attiva (solo una classe CSS per lo stile) — miglioramento di accessibilità reale (screen reader non hanno indicazione programmatica della pagina corrente), nessun AC lo richiede esplicitamente. [app/NavBar.tsx]
- Nessuna funzionalità di logout/account in nessuna pagina dell'app — gap di prodotto reale, mai esistita in nessuna storia precedente (nemmeno Story 1.1). Esplicitamente fuori perimetro di questa storia; da considerare come storia a parte se diventa un problema pratico. [app/NavBar.tsx]
- Un Utente già autenticato che visita `/accedi`/`/registrati` vede comunque la barra di navigazione sopra il form — coerente col comportamento preesistente del route guard (un Utente autenticato non viene mai reindirizzato via da una rotta pubblica, invariato da Story 1.1). [app/NavBar.tsx]
- Riuso 1:1 di `PROTECTED_ROUTES` come fonte delle voci di navigazione conflagra "autorizzato ad accedere" con "merita una voce di primo livello" (es. `/wizard-nuova-stagione`, un wizard una-tantum, accanto a voci quotidiane come `/gruppi`) — decisione esplicita e approvata in fase di creazione di questa storia, da non riaprire senza nuova elicitazione con l'utente. [lib/auth/voci-navigazione.ts]
- Elenco voci della barra piatto, non raggruppato per "Nav → Configurazione"/"Nav → Amministrazione" come annotato in `EXPERIENCE.md` (righe 54-57) — semplificazione deliberata coerente col vincolo "nessuna barra laterale o drawer complesso" dello stesso documento; nessun meccanismo di raggruppamento/sottomenu è mai stato specificato in dettaglio. [lib/auth/voci-navigazione.ts]
- Il confronto prefisso-pathname per lo stato attivo in `NavBar.tsx` duplica (non riusa) la logica equivalente di `matchProtectedRoute` in `lib/auth/route-guard.ts` (funzione privata, non esportata) — basso rischio di divergenza nel breve termine, ma andrebbe unificato se `route-guard.ts` cambiasse la propria logica di match. [app/NavBar.tsx, lib/auth/route-guard.ts]
- Due chiamate di rete sequenziali non parallelizzate (sessione Supabase Auth + lookup Storage del logo) su ogni pagina autenticata dell'app, nessuna cache — coerente con NFR4 (nessun requisito di performance/uptime formale in questo progetto personale), ma da riconsiderare se la latenza percepita diventasse un problema reale. [app/NavBar.tsx]
- `overflow-x: auto` impostato sia su `.navBar` sia su `.voci` in `NavBar.module.css` — ridondante, rischio di doppia scrollbar orizzontale annidata su viewport stretti (innocuo, solo qualità del CSS). [app/NavBar.module.css]

## Deferred from: code review of 8-2-onboarding-e-autenticazione (2026-07-24)

- Valori tipografici (`font-size`/`font-weight`/`letter-spacing`) scritti come numeri letterali invece di custom property, a differenza di colore/spaziatura/radius — pattern preesistente e identico in ogni CSS module di questa codebase (`permessi-certificati.module.css`, `vista-dirigente.module.css`): `app/globals.css` non definisce alcuna custom property tipografica, solo colore/spaziatura/radius. Non introdotto da questa storia; risolverlo richiederebbe introdurre token tipografici a livello globale, trasversale a tutte le storie 5.1-8.x. [app/(auth)/accedi/accedi.module.css e gli altri 3 CSS module di questa storia]
- Nessun valore di fallback nelle funzioni `var(--color-*)` (es. `var(--color-border)` invece di `var(--color-border, #bfe4f5)`) — stesso pattern identico in ogni CSS module esistente nel progetto fin da Story 5.1, non introdotto da questa storia. [app/(auth)/accedi/accedi.module.css e gli altri 3 CSS module di questa storia]
- Nessuna rete di sicurezza sui nomi di classe CSS module: `import styles from "./x.module.css"` è tipizzato genericamente da Next.js come `{ [key: string]: string }`, quindi un futuro refuso in `styles.qualcosaDiSbagliato` compilerebbe comunque restituendo `string` invece di un errore — limite strutturale di TypeScript/Next.js su ogni CSS module del progetto, non specifico di questa storia. Nessun test di rendering esiste per intercettarlo (vedi punto successivo). [tutti i CSS module del progetto]
- Nessun test automatico protegge `role="alert"`/`role="status"` (o qualunque markup JSX) dalle 4 pagine di questa storia da regressioni future — coerente con la convenzione già stabilita dal progetto (solo funzioni pure hanno test Vitest, nessun Client/Server Component di pagina è mai stato testato con un test-runner React, esplicitato nelle Dev Notes di Story 8.1). [app/(auth)/accedi/page.tsx e gli altri 3 page.tsx di questa storia]

## Deferred from: code review of 8-3-orari-e-palestre (2026-07-24)

- CSS duplicato quasi verbatim (`.campo`/`.bottone`/`.errore`/`.successo`/`.sezione`) fra `palestre.module.css`, `slot.module.css`, `orari.module.css` e i CSS module di Story 8.2 — nessun meccanismo `composes: ... from` è mai stato usato in questa codebase; ogni route ha sempre avuto il proprio CSS module indipendente (convenzione esplicita, non un difetto di questa storia). Un refactor verso un file di pattern condivisi è possibile ma trasversale a tutte le storie 8.x, non specifico di questa. [app/(orari-palestre)/palestre/palestre.module.css, slot/slot.module.css, orari/orari.module.css]
- `.errore`/`.successo` usano solo colore testo (`--color-danger`/`--color-success`), non le varianti `-bg` definite in `DESIGN.md` — stesso pattern identico già introdotto in Story 8.2, non una regressione di questa storia. [tutti i nuovi CSS module di questa storia]
- `raggruppaPerGiorno` (`mio-orario/page.tsx`) si fida che l'array `slot` arrivi già ordinato e contiguo per giorno (da `unisciESordinaSlot`) e che ogni valore `giorno` sia una delle 7 chiavi note di `ETICHETTA_GIORNO`, senza guardia difensiva propria (nessun fallback se un giorno non mappato producesse un'etichetta vuota, nessuna protezione contro una `key={gruppo.giorno}` duplicata se la contiguità si rompesse) — garantito oggi dal tipo `GiornoSettimana` di Prisma e dalla convenzione "unica fonte di verità" di `GIORNI_SETTIMANA`/`ORDINE_GIORNO`, stessa classe di fiducia in un invariante già estesa altrove in questa codebase. [app/(orari-palestre)/mio-orario/page.tsx]
- `raggruppaPerGiorno` non ha una copertura di test dedicata (funzione pura colocata in `page.tsx`, non estratta in `lib/` come la sua "sorella" `unisciESordinaSlot`) — divergenza minore dalla convenzione stabilita da Story 2.7; da valutare l'estrazione se una logica simile servisse altrove in futuro. [app/(orari-palestre)/mio-orario/page.tsx]
- La conversione da tabella (`SlotTable`, intestazioni `<th>` Giorno/Orario/Palestra-Campo/Gruppo) a card raggruppate per giorno in `/mio-orario` perde l'associazione semantica header-colonna per screen reader (`span`/`div` senza etichetta accessibile equivalente) — la tabella originale non aveva comunque `scope="col"` sui propri `<th>`, quindi non era pienamente accessibile nemmeno prima; un miglioramento reale richiede una scelta di formulazione/UX non banale, da affrontare come refinement dedicato se necessario. [app/(orari-palestre)/mio-orario/page.tsx]
- Il rendering a card di `/mio-orario` e il rendering a tabella di `SlotTable` non condividono markup/classi pur rappresentando la stessa forma di Slot — conseguenza diretta e accettata della "Decisione" esplicita di questa storia (mockup a card vs tabella condivisa): un futuro cambio del vocabolario visivo "riga Slot" richiederebbe un aggiornamento manuale in entrambi i punti. [app/(orari-palestre)/mio-orario/page.tsx, app/(orari-palestre)/SlotTable.tsx]

## Deferred from: code review of 8-4-presenze (2026-07-24)

- CSS duplicato quasi verbatim (`.campo`/`.bottone*`/`.errore`/`.successo`/`.sezione`) fra `presenze.module.css`, `storico-presenze.module.css` e i CSS module di Story 8.2/8.3 — stessa convenzione già accettata più volte (nessun `composes` mai usato in questa codebase), non un difetto di questa storia. [app/(presenze)/presenze/presenze.module.css, storico-presenze/storico-presenze.module.css]
- Naming inconsistente per lo stesso ruolo UI: il pulsante "Carica" del form di filtro è `.bottoneCarica` in `presenze.module.css` ma `.bottone` in `storico-presenze.module.css`, pur essendo lo stesso ruolo (submit di un form GET) introdotto nello stesso diff — cosmetico, nessun impatto funzionale, conseguenza dell'assenza di un CSS condiviso fra route (vedi punto sopra). [app/(presenze)/presenze/presenze.module.css, storico-presenze/storico-presenze.module.css]
- `.scrollWrapper` (riusato da `SlotTable.module.css`, Story 8.3, ora anche in `storico-presenze.module.css`) non ha `tabindex`/`role` per permettere lo scroll orizzontale da tastiera su viewport stretto, né un'indicazione visiva (fade/ombra) che la tabella è scorrevole — stesso gap preesistente già presente nella sua prima introduzione (Story 8.3), qui copiato una seconda volta senza correggerlo. Da affrontare trasversalmente su tutte le occorrenze insieme, non una alla volta. [app/(orari-palestre)/SlotTable.module.css, app/(presenze)/storico-presenze/storico-presenze.module.css]
- L'intestazione Slot di `/presenze` mostra sempre "Slot selezionato", mentre il mockup approvato (`key-presenze.html`, scenario UJ-2 specifico) mostra "Slot passato" — generalizzazione deliberata e ragionevole (nessuno slot passato/futuro/odierno viene distinto oggi, e distinguerlo richiederebbe nuova logica di confronto date, fuori scope per un restyle), non un difetto. [app/(presenze)/presenze/page.tsx]
- Un valore di `data` sintatticamente valido (`/^\d{4}-\d{2}-\d{2}$/`) ma calendarialmente impossibile (es. "2026-02-30", che `Date` normalizza a "2 marzo") e che per coincidenza superasse il controllo giorno-della-settimana a monte produrrebbe un'intestazione Slot con una data di calendario inesistente mostrata alla lettera — stesso limite già accettato in tutto il progetto (la validazione di `data` è sempre solo di forma/regex, mai di validità calendariale reale, es. `dati-fisici/actions.ts`), non introdotto né aggravato da questa storia. [app/(presenze)/presenze/page.tsx]

## Deferred from: code review of 9-1-pulsante-di-logoff (2026-07-25)

- Nessuno stato pending/disabled sul pulsante "Esci" durante l'esecuzione della Server Action (niente `useFormStatus`/wrapper client) — un doppio click concorrente invoca `esci()` due volte, ma l'effetto è innocuo (`signOut()` è idempotente, `redirect()` interrompe comunque l'esecuzione): non vale la complessità di un wrapper client solo per questo. [app/NavBar.tsx]
- Il pulsante back del browser dopo il logoff (AC #3) non è verificato contro il back-forward cache (bfcache) dei browser moderni, che in teoria potrebbe ripristinare una pagina protetta dalla memoria senza richiesta di rete - probabilmente già mitigato dagli header Cache-Control che Next.js imposta di default sulle pagine dinamiche (molte pagine di questo progetto usano già `dynamic = "force-dynamic"`), ma non verificato dal vivo in un browser reale; riguarda l'intera app (ogni pagina protetta), non specifico di questa storia. [middleware.ts, tutte le pagine protette]

## Deferred from: code review of 9-2-navigazione-responsive-hamburger-e-barra-laterale (2026-07-25)

- La navigazione su mobile richiede JavaScript (il drawer e' permanentemente `translateX(-100%)`, solo lo stato client-side `aperto` lo porta a video) - a differenza della vecchia barra orizzontale (Story 8.1), un Server Component puro raggiungibile senza JS. Nessun requisito formale "nessun JS" esiste in questo progetto (il resto dell'app usa gia' `useActionState`/form interattivi ovunque), accettato come compromesso ragionevole. [app/NavBarClient.tsx, app/NavBar.module.css]
- Nessuna gestione del focus quando il drawer si apre/chiude: il focus non si sposta sulla prima voce all'apertura, ne' torna sul pulsante hamburger alla chiusura (Esc, overlay, selezione voce) - `inert` (patch applicata in questa stessa review) gia' impedisce che il focus finisca su elementi invisibili, ma non offre l'esperienza completa di un drawer accessibile. [app/NavBarClient.tsx]
- Nessun `role="dialog"`/`aria-modal="true"` sul drawer aperto - trattato come landmark di navigazione (`<nav aria-label="Navigazione principale">`) + `inert` quando chiuso, non come un vero dialog modale. Scelta ragionevole per un drawer di navigazione (non un vero modale), ma non l'unica valida. [app/NavBarClient.tsx]
- `z-index: 20`/`21` (overlay/drawer) introdotti senza alcuna scala di z-index documentata - le uniche due dichiarazioni `z-index` in tutto `app/**/*.module.css`. Nessun conflitto oggi (nessun altro overlay/modale esiste nell'app), da riconsiderare se in futuro si aggiungesse un altro elemento in overlay. [app/NavBar.module.css]
- Ridimensionare la finestra oltre gli 880px con il drawer mobile rimasto aperto, poi tornare sotto 880px senza toccare l'hamburger, fa ricomparire drawer/overlay alla prossima visualizzazione stretta - interazione rara (richiede di aver aperto il drawer su mobile e poi ridimensionare live la finestra), non gestita con un listener di reset dedicato. [app/NavBarClient.tsx]

## Deferred from: code review of 9-3-riquadro-larghezza-massima-pagine-form (2026-07-25)

- `.pagina`/`.riquadro` (locale, `accedi.module.css`, Story 8.2, max-width 360px) e `.pagina-form`/`.riquadro-form` (globale, `app/globals.css`, questa storia, max-width 480px) sono due copie a mano dello stesso pattern visivo, differenti solo nel max-width - `/accedi` non è stata rifattorizzata per riusare le classi globali (decisione deliberata della storia, zero rischio di regressione sul login), ma questo lascia il pattern duplicato senza alcun meccanismo che lo tenga sincronizzato se in futuro cambiano i token di base (colore, radius, ombra). Da riconsiderare se si introduce un terzo punto con lo stesso pattern. [app/globals.css, app/(auth)/accedi/accedi.module.css]

## Deferred from: implementation of 9-4-menu-profilo-con-logoff-e-modifica-password (2026-07-26)

- Il form di modifica password non richiede la password attuale come conferma d'identità aggiuntiva — `supabase.auth.updateUser({password})` si fida della sola sessione già autenticata, stessa fiducia già riposta ovunque nel progetto in una sessione valida. Un dispositivo condiviso lasciato con una sessione sbloccata (es. un tablet in palestra, scenario già citato in `NavBar.actions.ts` a proposito dello scope globale del logoff) permetterebbe a chiunque vi acceda di cambiare la password altrui senza conoscerla. Nessun AC di questa storia lo richiede; da riconsiderare se emergesse un caso reale (richiederebbe un secondo campo "password attuale" + una chiamata di verifica prima di `updateUser`). [app/modifica-password/actions.ts]
- Nessuna gestione del focus quando il menu profilo si apre/chiude (il focus non si sposta sulla prima voce all'apertura né torna sul trigger alla chiusura) — stesso gap già accettato e deferito per il drawer mobile in Story 9.2, qui riproposto identico per il nuovo dropdown. [app/NavBarClient.tsx]
- Il click-fuori del menu profilo ascolta solo `mousedown`, non `touchstart` — copre comunque il tap su touch screen nella maggior parte dei browser mobili moderni (un tap sintetizza comunque un evento `mousedown`), ma non è stato verificato dal vivo su un dispositivo touch reale in questa storia. [app/NavBarClient.tsx]
- Nessun test automatico protegge il markup `role="menu"`/`role="menuitem"` di `NavBarClient.tsx` da regressioni future — coerente con la convenzione già stabilita dal progetto (solo funzioni pure hanno test Vitest, nessun Client/Server Component è mai stato testato con un test-runner React, vedi Defer Story 8.2). [app/NavBarClient.tsx]
- `modificaPassword()` non termina le altre sessioni/dispositivi dopo un cambio password riuscito — a differenza di `esci()` (Story 9.1, scope globale deliberato "coerente con l'aspettativa che 'uscire' chiuda la sessione ovunque"), qui una sessione già attiva altrove (es. rubata) resta valida anche dopo che l'Utente ha cambiato la password credendo di essersi messo al sicuro. Nessun AC di questa storia lo richiede; da riconsiderare insieme al punto sopra sulla verifica della password attuale (stesso scenario di minaccia: sessione compromessa). [app/modifica-password/actions.ts]

## Deferred from: code review of 9-5-campo-cognome-per-allenatore (2026-07-27)

- Messaggio di validazione impreciso quando manca un solo campo su tre ("Nome, Cognome e Codice Fiscale sono obbligatori." anche se manca solo Cognome) — stesso pattern già presente per Nome/Codice Fiscale prima di questa storia, solo esteso a un terzo campo. [app/(onboarding-import)/precaricamento-allenatori/actions.ts]
- Race TOCTOU su Codice Fiscale duplicato: `trovaAllenatorePerCodiceFiscale` (check) e `prisma.allenatore.create` (create) non sono transazionali — due submit concorrenti con lo stesso CF possono entrambi superare il check e uno fallire sul vincolo unique, ricadendo nel catch generico ("INTERNAL") invece del messaggio duplicato dedicato. Gap pre-esistente, non introdotto da questa storia. [app/(onboarding-import)/precaricamento-allenatori/actions.ts]
- Nessun vincolo di formato/lunghezza su `cognome` (maxlength, normalizzazione spazi multipli) — stesso gap già presente su `nome`, non specifico di questa storia. [app/(onboarding-import)/precaricamento-allenatori/actions.ts]
- Nessun attributo `autoComplete` sul nuovo campo Cognome — nessun campo nome/form del progetto lo usa, convenzione pre-esistente. [app/(onboarding-import)/precaricamento-allenatori/page.tsx]
- Nessun test che verifichi il rendering del campo Cognome in UI (solo la Server Action è testata) — coerente con la convenzione già stabilita dal progetto (nessuna pagina ha mai avuto test di rendering component, solo Server Action). [app/(onboarding-import)/precaricamento-allenatori/page.tsx]

## Deferred from: code review of 9-6-geolocalizzazione-palestre (2026-07-27)

- Nessun test verifica il rendering condizionale del link "Naviga" nei tre punti di utilizzo — coerente con la convenzione gia' stabilita dal progetto (nessuna pagina/componente ha mai avuto test di rendering, solo funzioni pure e Server Action). [app/(orari-palestre)/SlotTable.tsx, app/(orari-palestre)/mio-orario/page.tsx, app/(orari-palestre)/palestre/PalestraRow.tsx]
- Nessun avviso "si apre in una nuova scheda" sui link `target="_blank"` — nessun precedente di questo pattern esiste altrove nel progetto (nessun altro link esterno), da valutare come convenzione trasversale futura. [app/(orari-palestre)/SlotTable.tsx, app/(orari-palestre)/mio-orario/page.tsx, app/(orari-palestre)/palestre/PalestraRow.tsx]
- Il link "Naviga" in `/palestre` riflette l'ultimo indirizzo salvato, non il valore live dell'input durante la modifica — comportamento intrinseco del pattern Server Component + `revalidatePath` gia' usato in tutto il progetto, non specifico di questa storia. [app/(orari-palestre)/palestre/PalestraRow.tsx]

## Deferred from: code review of 9-6-geolocalizzazione-palestre estensione (2026-07-27)

- Se il pattern con priorità più alta matcha ma produce coordinate fuori range, `estraiCoordinateDaLinkMaps` ritorna `null` invece di ritentare i pattern successivi — teorico, un vero link Google Maps ha sempre coordinate internamente coerenti tra i suoi pattern. [lib/estrai-coordinate-maps.ts]
- `(0,0)` ("Null Island") è accettato come coordinata valida da `coordinateValide` — richiederebbe la cifra letterale "0,0" nella posizione esatta di un pattern reale, probabilità trascurabile. [lib/estrai-coordinate-maps.ts]
- Il pattern del centro vista richiede la virgola finale (`@lat,lng,`) — un link `@lat,lngz` senza quella virgola non matcherebbe; i link Google Maps reali includono sempre quella virgola prima dello zoom. [lib/estrai-coordinate-maps.ts]
- Messaggio di errore generico per il link Maps non valido — non distingue "testo non riconoscibile" da "la fetch di risoluzione del link breve è fallita/andata in timeout". [app/(orari-palestre)/palestre/actions.ts]
- Nessun attributo `sandbox` sull'iframe della mappa incorporata — rischierebbe di rompere l'interattività della mappa (che richiede script) senza verifica dal vivo; il controllo di dominio già applicato al link riduce comunque il rischio di contenuto arbitrario incorporato. [app/(orari-palestre)/palestre/PalestraRow.tsx]

## Deferred from: code review of 9-9-gestione-allenatori-precaricati (2026-07-27)

- Race TOCTOU sul Codice Fiscale duplicato in `aggiornaAllenatore` [app/(onboarding-import)/precaricamento-allenatori/actions.ts] — stesso pattern pre-esistente già deferito per `precaricaAllenatore` (code review Story 9.5).
- Messaggio "Riprova" fuorviante quando l'Allenatore non esiste più al momento della cancellazione (pagina non aggiornata, cancellato da un altro Admin nel frattempo) [app/(onboarding-import)/precaricamento-allenatori/actions.ts] — percorso raro, impatto basso.
- Nessuna restrizione sulla modifica del Codice Fiscale di un Allenatore già "Registrato" — rischio speculativo di disallineamento dell'identità usata per il matching, nessun AC lo vieta esplicitamente. [app/(onboarding-import)/precaricamento-allenatori/actions.ts]
- Nessuna paginazione sull'elenco Allenatori — coerente con la stessa scelta già deliberata altrove nel progetto (`/palestre`, `/admin`) per la scala ridotta di NFR5.

## Deferred from: code review of 9-10-voce-navigazione-attiva-non-aggiornata (2026-07-28)

- Il controllo `NON_AUTORIZZATO_PATH` in `NavBar.tsx` (righe 50-53) legge ancora il pathname calcolato una sola volta lato server (header `x-pathname`) — stessa identica causa di staleness (Client Cache del layout radice) appena risolta per la voce attiva in questa storia, ma esplicitamente escluso dallo scope nei Dev Notes originali. Fuori scope esplicito di questa storia; stesso pattern che ha già generato la Story 9.10 a partire dalla 9.7, potrebbe meritare una story propria se osservato dal vivo. [app/NavBar.tsx]
- L'elenco `voci` (filtrato per Ruolo in `NavBar.tsx`) soffre della stessa staleness di navigazione se i Ruoli di un Utente cambiano durante una sessione attiva — non toccato da questa storia. Collegato al trade-off già accettato in AD-11 (finestra di autorizzazione stantia, deferred-work.md Story 1.1). [app/NavBar.tsx]

## Deferred from: code review of 9-11-recupero-password (2026-07-28)

- Il link di recupero password costruito in `richiediRecuperoPassword` deriva l'origine da `headers().get("host")`/`x-forwarded-proto` senza validarli contro un valore noto — un Host non attendibile porterebbe l'email di recupero a puntare a un dominio diverso da quello reale (password-reset poisoning). Rischio accettato per ora dall'utente: l'hosting Cloudflare Pages instrada solo le richieste per il dominio configurato, riducendo il rischio pratico rispetto a un reverse proxy generico; da riconsiderare se osservato dal vivo o se cambia l'hosting. [app/(auth)/recupera-password/actions.ts:54-57]
- Nessun rate limiting/protezione da abuso su `/recupera-password` (email-bombing, esaurimento quota SMTP) — pre-esistente: nessuna infrastruttura di rate limiting esiste in nessun punto del progetto (incluse `/accedi`/`/registrati`, già pubbliche da 9 epic), richiederebbe nuova infrastruttura (KV/Durable Object) fuori perimetro di questa storia. [app/(auth)/recupera-password/actions.ts]
- Il `token_hash` viaggia come query param in chiaro nell'URL (cronologia browser, log server) — caratteristica intrinseca del pattern Supabase scelto (stesso schema del suo `action_link`), mitigata da mono-uso (`verifyOtp`) e scadenza breve; nessuna risorsa di terze parti caricata da quella pagina, quindi nessuna fuga via `Referrer-Policy` in pratica. [app/(auth)/reimposta-password/page.tsx]
- Il dialogo `window.confirm()` del reset Admin non mostra mai il valore fisso `Volley@Mogliano` — l'Admin deve già conoscerlo a memoria/da documentazione (ora in README); richiederebbe una costante condivisa client/server non ancora presente. [app/(amministrazione)/admin/UtenteRow.tsx]

## Deferred from: code review of 9-12-upload-foto-profilo (2026-07-28)

- Le policy `foto_profilo_*_ampia_select` danno a qualunque Allenatore visibilità su tutte le foto di tutte le Atlete (nessuno scoping per Gruppo), a differenza di ogni altro percorso dati rivolto all'Allenatore nel progetto — confermato esplicitamente dall'utente come scelta voluta (non un oversight), nessuna modifica alla migrazione. [prisma/migrations/20260728000000_add_foto_profilo/migration.sql]
- `requireRuolo()` non è avvolta in un `try/catch` in `caricaFotoProfilo` — pattern pre-esistente e identico in ogni Server Action del progetto, non specifico di questa storia. [app/il-mio-profilo/actions.ts]
- Nessuna validazione lato client di formato/dimensione oltre al filtro `accept` dell'input file — miglioramento UX a bassa priorità, nessun AC lo richiede. [app/il-mio-profilo/FotoProfiloForm.tsx]
- `utente_possiede_allenatore` non verifica `Utente.attivo` — stesso trade-off già accettato a livello di intero progetto (Story 1.2), tutte le funzioni RLS sorelle hanno la stessa caratteristica. [prisma/migrations/20260728000000_add_foto_profilo/migration.sql]
- Doppio round-trip a `supabase.auth.getUser()` per invio (uno dentro `requireRuolo`, uno esplicito nell'azione) — inefficienza minore coerente con lo stesso pattern già presente in ogni altra Server Action del progetto. [app/il-mio-profilo/actions.ts]

## Deferred from: code review of 10-1-creazione-campionato-per-un-gruppo (2026-07-28)

- Un errore di `supabase.auth.getUser()` in `page.tsx` viene solo loggato, l'esecuzione prosegue con `user` potenzialmente `undefined` — un Admin/Dirigente colpito da un problema transitorio di autenticazione vedrebbe il messaggio "account non collegato a un profilo Allenatore" invece di un'indicazione di errore reale. Stesso pattern di gestione errori già presente in molte pagine del progetto, severità bassa. [app/(partite-campionati)/campionati/page.tsx]
- `Utente.attivo` non viene mai verificato nella risoluzione dell'Allenatore chiamante in `verificaPossessoGruppoSeAllenatore` — stesso trade-off già accettato a livello di intero progetto (Story 1.2/AD-11). [app/(partite-campionati)/campionati/actions.ts]
- Doppio round-trip a `supabase.auth.getUser()` per invio (uno dentro `requireRuolo`, uno dentro `verificaPossessoGruppoSeAllenatore`) — stessa inefficienza minore già accettata altrove nel progetto (Story 9.12). [app/(partite-campionati)/campionati/actions.ts]
- ~~`gruppo_campionati` non ha un indice secondario su `campionatoId` da solo~~ — **non più applicabile**: la tabella `gruppo_campionati` è stata rimossa interamente in Story 10.7 (`Campionato.gruppoId` è ora una FK diretta, non più una tabella di giunzione).
- La corrispondenza tra l'`onDelete` implicito di Prisma per `Campionato → AnnoAgonistico` e `ON DELETE RESTRICT` scritto a mano nella migrazione non è stata verificata con `prisma migrate diff`, solo per analogia visiva con la migrazione già in produzione di `Gruppo` — rischio basso per analogia diretta con un pattern già validato dal vivo.

## Deferred from: code review of 10-2-import-excel-partite-campionato (2026-07-29)

- Nessuna transazione attorno al ciclo di scrittura per riga: un fallimento a metà lascia scritture già committate senza rollback, e i conteggi `create`/`aggiornate` accumulati in memoria vengono scartati nel messaggio di errore generico — scelta di design già documentata e giustificata nei Dev Notes/Task 5 della storia (ogni Partita è indipendente, upsert idempotente per costruzione, un reimport dello stesso file si autocorregge), stesso principio già accettato in `import-atlete/actions.ts`. [app/(partite-campionati)/campionati/importa-gare-actions.ts]
- `parseDataItaliana`/`analizzaFileGare` non gestiscono un seriale data/ora nativo di Excel (`XLSX.read` senza `cellDates: true`) — il file reale di riferimento (`_bmad/resources/Gare.xls`) è stato verificato manualmente (xlrd) e usa celle testo per entrambe le colonne; un fix sicuro per "Ora" richiederebbe una gestione dedicata del seriale tempo, fuori scope di questa storia. [lib/data-italiana.ts, lib/importa-gare/parser.ts]
- Nessun limite massimo di dimensione sul file caricato prima di bufferizzarlo interamente in memoria — stesso gap già presente nel pattern di riferimento esplicitamente citato da questa storia (`import-atlete/actions.ts`), non introdotto qui. [app/(partite-campionati)/campionati/importa-gare-actions.ts]
- Messaggio di errore del parser propagato verbatim all'utente: se `XLSX.read` lanciasse un'eccezione propria (file realmente corrotto), il testo (potenzialmente inglese/tecnico) apparirebbe così com'è nel banner di errore italiano — un messaggio generico fisso risolverebbe questo caso ma regredirebbe i messaggi specifici già pensati per l'AC #3 (colonne mancanti), lanciati dallo stesso blocco try/catch; nessun modo pulito di distinguere i due casi senza classi di errore dedicate. [app/(partite-campionati)/campionati/importa-gare-actions.ts]
- Scritture sequenziali riga per riga (un `findUnique` + `create`/`update` per riga, nessun batching) — solo un problema di performance su file molto grandi, stesso pattern già accettato in `import-atlete/actions.ts`. [app/(partite-campionati)/campionati/importa-gare-actions.ts]
- Messaggio fuorviante su un file con intestazioni valide ma zero righe dati ("Intestazioni non riconosciute" invece di "nessuna riga da importare") — caso raro, solo un problema di formulazione del messaggio. [lib/importa-gare/parser.ts]

## Deferred from: code review of 10-3-vista-partite-settimana-per-settimana (2026-07-29)

- Blocco di risoluzione Ruoli/Gruppo (utente, `eGestionale`, `Allenatore`, messaggio di errore) duplicato verbatim tra `campionati/page.tsx` e `partite/page.tsx`, nessun helper condiviso — stesso pattern di duplicazione già tollerato in almeno tre pagine del progetto (`dati-fisici/page.tsx` incluso), non una regressione introdotta qui. [app/(partite-campionati)/partite/page.tsx]
- Nessun test automatico copre direttamente `filtroAllenatore` (il filtro Prisma che soddisfa l'AC #3 di questa storia) — coerente con la convenzione già stabilita in tutto il progetto: nessuna pagina Server Component ha mai un test delle proprie query Prisma interne, solo le Server Action esportate vengono testate. [app/(partite-campionati)/partite/page.tsx]
- Un errore di `supabase.auth.getUser()` viene solo loggato, l'esecuzione prosegue con un messaggio fuorviante ("account non collegato a un profilo Allenatore" invece di un'indicazione di errore reale) — stesso identico gap già deferito per la pagina gemella `campionati/page.tsx` in Story 10.1. [app/(partite-campionati)/partite/page.tsx]
- Cella "Luogo" vuota se `impianto` è assente ma `indirizzoImpianto` è presente (il link "Naviga" compare comunque senza alcun nome visibile del luogo) — bassa probabilità, l'export federale reale popola sempre entrambi i campi insieme (verificato sul file reale, Story 10.2). [app/(partite-campionati)/partite/page.tsx]

## Deferred from: code review of 9-13-modifica-e-cancellazione-slot (2026-07-29)

- Nessun controllo di concorrenza ottimistica in `aggiornaSlot` (nessun campo versione/`updatedAt` confrontato) — un form rimasto aperto con valori ormai stantii può sovrascrivere silenziosamente una modifica concorrente di un altro Admin. Stesso identico rischio già presente e accettato in `aggiornaAllenatore` (Story 9.9), non introdotto qui. [app/(orari-palestre)/slot/actions.ts]
- Messaggio generico "Riprova" fuorviante quando `cancellaSlot`/`aggiornaSlot` operano su uno Slot già cancellato da un'altra sessione concorrente — stesso identico limite già presente e accettato in `cancellaAllenatore` (Story 9.9). [app/(orari-palestre)/slot/actions.ts]
- Nessuna validazione che `campoId`/`gruppoId` corrispondano a righe realmente esistenti in `validaCampiSlot` — pre-esistente in `creaSlot` (Story 2.5), non introdotto qui. [app/(orari-palestre)/slot/actions.ts]
- Nessun controllo di sovrapposizione oraria per lo stesso Campo raggiungibile dalla modifica — stesso limite pre-esistente di `creaSlot`. [app/(orari-palestre)/slot/actions.ts]
- Nessun test dedicato per `aggiornaSlot` su uno Slot cancellato concorrentemente (Prisma P2025) — il `catch` generico gestisce già il caso in sicurezza, solo non testato esplicitamente. [app/(orari-palestre)/slot/actions.test.ts]
- Margine superiore raddoppiato nel form di modifica (`.form` e il primo `.campo` hanno entrambi `margin-top: var(--space-4)`) — stesso identico pattern già presente in `precaricamento-allenatori.module.css` (Story 9.9). [app/(orari-palestre)/slot/slot.module.css]

## Deferred from: code review of 9-18-creazione-nuova-atleta-da-allenatore (2026-07-31)

- Race TOCTOU sul controllo Codice Fiscale duplicato in `creaEAssegnaAtleta` (check-then-insert, non atomico) — stessa classe di rischio a bassa probabilità già accettata in Story 1.4/1.5/9.5/9.9 (singolo Admin/piccola società). Un submit perdente in una race concorrente riceve il messaggio generico INTERNAL invece di quello specifico "Esiste già un'Atleta con questo Codice Fiscale.". [app/(gruppi-allenatori)/gruppi/actions.ts]
- Nessun rollback se l'upsert di assegnazione al Gruppo fallisce dopo la creazione riuscita dell'Atleta in `creaEAssegnaAtleta` — mitigato: l'Atleta orfana (creata ma non assegnata) ricompare comunque nel form "Assegna Atleta" esistente per un tentativo successivo. Stesso principio "nessun rollback automatico" già accettato ovunque nel progetto fin da Story 1.1. [app/(gruppi-allenatori)/gruppi/actions.ts]
- `isCodiceFiscaleValido` non fa un controllo del checksum, solo del formato (16 caratteri alfanumerici) — limite preesistente da Story 1.4, riusato senza modifiche da `creaEAssegnaAtleta` per gating della creazione di una nuova identità Atleta. [lib/matching-codice-fiscale/valida-codice-fiscale.ts]

## Deferred from: code review of 9-14-rimozione-atleta-da-gruppo (2026-07-29)

- `deleteMany` ritorna comunque `{ success: true }` anche quando `count` è 0 perché l'Atleta è stata riassegnata a un altro Gruppo tra il caricamento della pagina e il click — stessa classe di ambiguità già accettata in Story 9.13 per `cancellaSlot`/`aggiornaSlot` su una riga cancellata concorrentemente. [app/(gruppi-allenatori)/gruppi/actions.ts]
- Nessun coordinamento tra "Assegna Atleta" e "Rimuovi" per la stessa Atleta — `atleteDisponibili` non filtrata (pre-esistente da Story 2.4, l'upsert lo rende comunque sicuro), i due `pending` non sono condivisi come in `AllenatoreRow.tsx`/`SlotRow.tsx` — stessa classe di race a bassa probabilità già tollerata altrove. [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx, AtletaAssegnata.tsx]
- `console.error(err)` senza identificativi (`gruppoId`/`atletaId`) per correlare un errore in produzione — stessa convenzione identica in ogni altra Server Action del progetto. [app/(gruppi-allenatori)/gruppi/actions.ts]
- Nessun `aria-busy`/testo "in corso" oltre a `disabled` durante la rimozione — stessa convenzione già presente in ogni altro Client Component del progetto. [app/(gruppi-allenatori)/gruppi/AtletaAssegnata.tsx]
- `revalidatePath("/gruppi")` fuori dal blocco `try/catch` — stessa identica collocazione già usata in ogni altra Server Action del progetto. [app/(gruppi-allenatori)/gruppi/actions.ts]
- `window.confirm` bypassabile da una sottomissione del form non innescata dall'evento React `onSubmit` — stesso limite già presente nel pattern replicato (`AllenatoreRow.tsx`/`SlotRow.tsx`, Story 9.9/9.13). [app/(gruppi-allenatori)/gruppi/AtletaAssegnata.tsx]
- Il messaggio di errore precedente resta visibile mentre un nuovo tentativo è `pending` — comportamento intrinseco dello stesso pattern `useActionState` già usato in ogni altro Client Component del progetto. [app/(gruppi-allenatori)/gruppi/AtletaAssegnata.tsx]
- `String(formData.get(...) ?? "")` non verifica che il valore non sia un `File` — stessa identica conversione già usata in ogni altra Server Action del progetto. [app/(gruppi-allenatori)/gruppi/actions.ts]
- `aria-label` identico per due Atlete con lo stesso `nome` nello stesso Gruppo — l'elenco mostra solo `nome` (mai `cognome`) fin da Story 2.4, stessa ambiguità di visualizzazione pre-esistente. [app/(gruppi-allenatori)/gruppi/AtletaAssegnata.tsx]

## Deferred from: code review of 9-19-badge-certificato-in-scadenza (2026-08-01)

- Cast non verificati su `certificato.dataFineValidita`/`stato` da `elencaCertificati` (client Supabase non tipizzato) — pre-esistente (stesso pattern già presente in `vista-dirigente/page.tsx` prima di questa storia), ora replicato in 2 nuovi punti; un cambiamento di forma della riga fallirebbe silenziosamente a runtime. [app/(gruppi-allenatori)/gruppi/page.tsx:79-80, app/(gruppi-allenatori)/i-miei-gruppi/page.tsx:93-94]
- `aria-controls` punta a un id che non esiste nel DOM quando il drill-down è chiuso — pre-esistente sul tile "scaduto" (Story 5.1), ora duplicato anche sul nuovo tile "in scadenza" seguendo lo stesso pattern. [app/(amministrazione)/vista-dirigente/GruppoCard.tsx]
- Nessuno stile `:disabled` (opacità/dimming) su `.statTile` — gap pre-esistente, ora presente anche sul nuovo pulsante "in scadenza" quando il conteggio è 0. [app/(amministrazione)/vista-dirigente/vista-dirigente.module.css]
- Fallback "Atleta sconosciuta" silenzioso (solo `console.warn`, nessun monitoraggio) esteso a un secondo bucket — pre-esistente per `atleteScadute`, ora replicato per `atleteInScadenza`. [app/(amministrazione)/vista-dirigente/page.tsx]

## Deferred from: code review of 9-20-data-certificato-in-fase-di-caricamento (2026-08-01)

- Nessun limite di plausibilità (min/max) sulle due date del certificato, né client né server — pre-esistente, coerente con `confermaCertificato` (stessa mancanza sulla funzione gemella), nessun AC di questa storia lo richiede. [app/(certificati-medici)/certificato-medico/actions.ts, CaricaCertificatoForm.tsx]
- Messaggio di validazione generico non identifica quale dei due campi data manca — coerente con la convenzione a un solo messaggio per form già stabilita in tutto il progetto. [app/(certificati-medici)/certificato-medico/actions.ts]
- Round-trip multipli se la validazione HTML5 lato client viene bypassata (nessun controllo aggregato di tutti gli errori in un colpo solo) — stesso pattern di validazione sequenziale già usato in ogni Server Action del progetto. [app/(certificati-medici)/certificato-medico/actions.ts]

## Deferred from: code review of 9-26-vista-insieme-allenatore (2026-08-02)

- `supabase.auth.getUser()` in errore viene solo loggato, poi trattato come "nessuna sessione" — un Utente con una sessione realmente autenticata ma un errore transitorio vedrebbe il messaggio "account non collegato a un profilo Allenatore" invece di un errore distinto. Pattern preesistente identico già in `i-miei-gruppi/page.tsx` (Story 9.15), replicato deliberatamente. [app/(gruppi-allenatori)/vista-allenatore/page.tsx]
- Nessun test dedicato alla logica di scoping/aggregazione della pagina (solo `route-guard` è testato) — coerente con la convenzione "nessun test di rendering" già stabilita, `vista-dirigente/page.tsx` non ne ha mai avuto uno. [app/(gruppi-allenatori)/vista-allenatore/page.tsx]
- `.filter()` dentro `.map()` per associare Atlete a Gruppo (O(gruppi × atlete)) — stesso pattern identico già presente invariato in `vista-dirigente/page.tsx`, scala ridotta. [app/(gruppi-allenatori)/vista-allenatore/page.tsx]
- Cast `as string | null`/`as StatoCertificato | null` sui campi di `certificato` — stesso pattern identico già presente in `vista-dirigente/page.tsx`. [app/(gruppi-allenatori)/vista-allenatore/page.tsx]

## Deferred from: code review of 9-25-ordinamento-stato-confermati (2026-08-02)

- Nessun indicatore visivo di direzione sull'etichetta "Stato" (solo `aria-pressed`) — un Utente vedente non ha modo di sapere se l'ordinamento è attivo senza cliccare e osservare il riordino. [app/(certificati-medici)/conferma-certificati/ListaConfermati.tsx]
- Nessun `aria-controls`/relazione esplicita tra il bottone "Stato" e la lista che governa — nessun precedente di questo pattern nel progetto. [app/(certificati-medici)/conferma-certificati/ListaConfermati.tsx]
- Nessun test di interazione per `ListaConfermati.tsx` (click sul bottone, verifica riordino renderizzato) — coerente con la convenzione "nessun test di rendering" già stabilita per i Client Component, solo la funzione pura di ordinamento è testata.
- Il bottone "Stato" compare anche con una sola riga confermata, dove l'ordinamento non ha alcun effetto osservabile — cosmetico.
- `localeCompare(nome, "it")` senza opzioni esplicite di sensitivity, copertura di test minima sugli accenti — coerente col livello già accettato altrove nel progetto.
- `StatoCertificatoAggregato` enumerato in tre punti indipendenti (mappa priorità + mappa classe badge + mappa etichetta badge) — l'esaustività di `Record<...>` di TypeScript previene chiavi dimenticate, ma restano tre punti di manutenzione per lo stesso concetto. [lib/ordina-certificati-per-stato.ts, app/(certificati-medici)/conferma-certificati/ListaConfermati.tsx]

## Deferred from: code review of 9-24-menu-impostazioni-smtp-logo (2026-08-02)

- Nessun link/breadcrumb di ritorno da `/smtp`/`/logo` verso `/impostazioni` — richiederebbe toccare quelle due pagine, esplicitamente fuori scope della Story 9.24; mitigato dal pulsante "indietro" del browser. [app/(configurazione)/smtp/page.tsx, app/(configurazione)/logo/page.tsx]
- `<ul>` con `list-style: none` senza `role="list"` in `/impostazioni` (VoiceOver/Safari possono perdere il ruolo implicito di lista) — stesso pattern identico replicato in ogni altra lista di questo progetto, non specifico di questa storia. [app/(configurazione)/impostazioni/impostazioni.module.css]
- Il tipo inline di `PROTECTED_ROUTES` (4 campi, commento multi-riga) inizia a essere poco leggibile nel punto di dichiarazione — refactor di forma (estrarre un `type RouteDefinition` nominato), da valutare se un quinto campo venisse aggiunto. [lib/auth/route-guard.ts]
- Nessun test per `/impostazioni` con un Utente ADMIN+DIRIGENTE combinati — la logica di intersezione `.some()` è generica e già coperta da test analoghi su altre rotte, duplicazione di basso valore. [lib/auth/route-guard.test.ts]
- L'ordine preservato di `/impostazioni` in `PROTECTED_ROUTES` è verificato solo in prosa, mai da un test (`arrayContaining` è intenzionalmente order-insensitive) — nessun impatto comportamentale osservabile oggi. [lib/auth/voci-navigazione.test.ts]

## Deferred from: code review of 9-23-colore-semantico-certificati-confermati (2026-08-02)

- Lo stesso stato `SCADUTO` viene mostrato in tono warning (giallo) altrove nell'app (badge "Certificato in scadenza", vista-dirigente) e in tono danger (rosso) in `/conferma-certificati`, senza alcun segnale che la scala colore sia dipendente dal contesto — osservazione UX legittima, nessuna correzione di codice non ambigua possibile senza una decisione di prodotto. [app/(certificati-medici)/conferma-certificati/page.tsx]

## Deferred from: code review of 9-22-precaricamento-allenatori-solo-admin (2026-08-02)

- Nessun test copre `getRouteDecision` con un array di Ruoli vuoto (Utente autenticato ma senza alcun Ruolo) su `/precaricamento-allenatori` — gap pre-esistente e trasversale a ogni voce di `PROTECTED_ROUTES`, non introdotto da questa storia. [lib/auth/route-guard.test.ts]

## Deferred from: code review of 10-7-campionato-un-solo-gruppo (2026-08-02)

- Il nuovo `onDelete: Cascade` da `Campionato` a `Gruppo` fa sì che cancellare un Gruppo cancellerebbe ora a cascata anche i suoi Campionati (prima, con la relazione molti-a-molti, un Campionato condiviso sarebbe sopravvissuto) — nessuna funzionalità di cancellazione Gruppo esiste oggi, rischio dormiente rilevante solo se una storia futura la introducesse. Le Partite venivano già cancellate a cascata direttamente tramite `Partita.gruppoId` indipendentemente da questo cambiamento (Story 10.2) - l'aumento reale del raggio d'azione è limitato alle sole righe `Campionato`. [prisma/schema.prisma]
- Nessun vincolo univoco a livello DB dietro al controllo duplicati di `creaCampionato` (AC #3/#4 di questa storia) — due chiamate concorrenti per lo stesso Gruppo+nome+stagione potrebbero entrambe superare il controllo e creare due Campionati duplicati (race TOCTOU). Stessa classe di rischio già accettata ripetutamente in questo progetto (Story 1.3/1.4/9.9/10.1), bassa probabilità reale (piccola società, pochi Admin/Allenatori attivi contemporaneamente). [app/(partite-campionati)/campionati/actions.ts]
- `creaCampionato` risolve l'Anno Agonistico corrente due volte (una dentro `risolviAutorizzazioneGruppo` per il controllo duplicati, una tramite `risolviAnnoAgonisticoCorrente()` per la creazione) — pattern preesistente dalla Story 10.1, non introdotto né toccato da questa storia. [app/(partite-campionati)/campionati/actions.ts]
- Nessun indice secondario sulla nuova colonna `Campionato.gruppoId` (l'indice univoco di `gruppo_campionati`, ora rimosso, lo forniva implicitamente) — stesso gap ricorrente già accettato ripetutamente in questo progetto (Story 2.1/2.2/2.3/2.4/10.1), scala ridotta. [prisma/schema.prisma]

## Deferred from: code review of 10-4-modifica-singola-partita (2026-08-03)

- `findUnique`/`risolviAutorizzazioneGruppo` non avvolti in try/catch in `aggiornaPartita` — stesso gap identico preesistente in `cancellaPartita`, stesso file, non introdotto da questa storia. [app/(partite-campionati)/partite/actions.ts]
- Nessuna concorrenza ottimistica (`Partita.updatedAt` non verificato, last-write-wins silenzioso su modifiche concorrenti) — pattern sistemico assente in tutte le Server Action di modifica del progetto (`aggiornaSlot`, `aggiornaAllenatore`, `confermaCertificato`...), nessun precedente di optimistic locking da cui derogare qui. [app/(partite-campionati)/partite/actions.ts]
- `impianto`/`indirizzoImpianto` senza limite di lunghezza lato server — coerente con la convenzione già in uso su ogni altro campo testo libero del progetto, nessun precedente di `maxLength` applicativo. [app/(partite-campionati)/partite/actions.ts]
- Doppio round-trip per risolvere il Gruppo (`partita.findUnique` per `gruppoId` + `risolviAutorizzazioneGruppo` che rifà il proprio `gruppo.findUnique`) — rispecchia lo stesso pattern già presente in `cancellaPartita`, non peggiorato da questa storia. [app/(partite-campionati)/partite/actions.ts]

## Deferred from: code review of 9-28-aggiunta-atleta-anche-da-admin (2026-08-03)

- Nessun `maxLength`/`pattern`/`inputMode` sul Codice Fiscale lato client nel form "Nuova Atleta" — stesso gap identico già presente in `MioGruppoCard.tsx` (Story 9.18), duplicato non introdotto da questa storia. [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx]
- Nessun `max` sulla data di nascita (`<input type="date">` accetta date future) — stesso gap identico già presente in `MioGruppoCard.tsx` (Story 9.18). [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx]
- Doppio submit rapido prima che `disabled={nuovaAtletaPending}` si applichi può far collidere due chiamate concorrenti su Codice Fiscale duplicato (errore INTERNAL generico invece del messaggio duplicato chiaro) — stesso identico pattern del bottone già presente in `MioGruppoCard.tsx` (Story 9.18). [app/(gruppi-allenatori)/gruppi/GruppoRow.tsx]

## Deferred from: code review of 9-27-modifica-date-certificato-confermato (2026-08-03)

- Nessuna rimozione del vecchio file quando se ne carica uno nuovo su un Certificato già confermato (`rimuoviFileCertificato` mai chiamata in questo modulo) — file orfani nel bucket storage. Gap preesistente in `confermaCertificato` (il modulo gemello `certificato-medico/actions.ts`, Story 4.1, applica correttamente questo pattern) — questa storia lo rende raggiungibile anche per Certificati CONFERMATO, ma non lo introduce. Richiede una story dedicata per sistemare entrambe le azioni in modo coerente. [app/(certificati-medici)/conferma-certificati/actions.ts]
- Nessuna concorrenza ottimistica: due Admin che modificano lo stesso Certificato concorrentemente si sovrascrivono silenziosamente — pattern sistemico assente in tutte le Server Action di modifica del progetto, stesso identico defer già accettato per Story 10.4. [app/(certificati-medici)/conferma-certificati/actions.ts]
- Nessun controllo di coerenza incrociata tra `dataFineValidita`/`dataInizioValidita`/`mesiValidita` — preesistente in `validaCampiCertificato`/`confermaCertificato`, non introdotto da questa storia. [app/(certificati-medici)/conferma-certificati/actions.ts]
- `mesiValidita` senza limite superiore — preesistente in `validaCampiCertificato`/`confermaCertificato`. [app/(certificati-medici)/conferma-certificati/actions.ts]
- Nessun modo di rimuovere (solo sostituire) un file già allegato — stessa limitazione già presente nel flusso `confermaCertificato`/"Da confermare". [app/(certificati-medici)/conferma-certificati/actions.ts]
- `revalidatePath` non avvolta in try/catch dopo il commit del salvataggio — rispecchia la stessa identica struttura di `confermaCertificato`. [app/(certificati-medici)/conferma-certificati/actions.ts]
- Campi FormData mancanti sovrascrivono i valori esistenti invece di preservarli — rispecchia lo stesso comportamento "sovrascrivi sempre, mai un merge per-campo" già di `confermaCertificato`; raggiungibile solo manomettendo l'API direttamente, la UI precompila sempre i valori esistenti. [app/(certificati-medici)/conferma-certificati/actions.ts]

## Deferred from: code review of 10-5-vista-partite-atleta-e-genitore (2026-08-04)

- Se una riga `GenitoreAtleta` esiste ma l'Atleta corrispondente non è leggibile via RLS (dato inconsistente/accesso revocato), l'utente supera il controllo iniziale (`atletaIds.length > 0`) ma `proprieAtlete` risulta vuoto — vede "Nessuna partita programmata" invece di un messaggio che spieghi la situazione. Stessa identica caratteristica già presente nel pattern mirror `certificato-medico/page.tsx`, non introdotta da questa storia. [app/(partite-campionati)/partite/page.tsx]

## Deferred from: code review of 12-1-entita-permessi-e-pagina-admin (2026-08-04)

- Nessun lock di concorrenza ottimistica sul salvataggio dei permessi (due Admin che salvano quasi contemporaneamente: l'ultimo vince silenziosamente) — stesso identico pattern delete-all+insert senza lock già in uso in `permessi-certificati/actions.ts`, non introdotto da questa storia. [app/(amministrazione)/permessi-accesso/actions.ts]
- Nessun meccanismo automatico che tenga sincronizzati nel tempo il seed della migrazione `20260804000000_add_permesso_rotta` e `PROTECTED_ROUTES`: una futura rotta protetta aggiunta a `route-guard.ts` comparirà nella matrice `/permessi-accesso` con tutte le caselle deselezionate finché un Admin non se ne accorge e la configura manualmente (fail-closed silenzioso). Correzione appropriata architetturale, appartiene a Story 12.2/12.3 (un helper/processo di sincronizzazione), non a questa story — un test statico che confronti la migrazione storica con `PROTECTED_ROUTES` si romperebbe a ogni futura rotta aggiunta da una story diversa. [prisma/migrations/20260804000000_add_permesso_rotta/migration.sql]
- Il messaggio "Configurazione salvata." non si resetta se l'Admin modifica ulteriormente le checkbox dopo un salvataggio riuscito senza ri-sottomettere — potrebbe essere letto come riferito allo stato attualmente visualizzato in pagina. Rifinitura UX minore, nessun precedente di fix per questo esatto pattern (`useActionState` che non resetta lo stato) altrove nel progetto. [app/(amministrazione)/permessi-accesso/PermessiAccessoForm.tsx]

## Deferred from: code review of 12-3-integrazione-route-guard (2026-08-04)

- `isAutorizzato` lancia `Promise.all(ruoli.map(ruolo => rottaAbilitataPerRuolo(...)))`: per un utente con più Ruoli su una rotta migrata, più chiamate concorrenti possono trovare simultaneamente la cache di `permessi-configurabili.ts` fredda/scaduta e innescare più query Prisma in parallelo invece di una sola (nessun coalescing delle richieste in-flight). Compone lo stesso gap già deferito nella review di Story 12.2 — zero impatto oggi (nessuna rotta reale migrata), da riconsiderare insieme a Story 12.4. [lib/auth/route-decision.ts]
- Non verificato se Prisma/`pg` si inizializzi correttamente sotto il runtime `experimental-edge` specifico di `middleware.ts` (diverso dal contesto Worker principale in cui girano le Server Action) — `npm run build` prova solo che i moduli si risolvano, non che le API Node (`net`/`tls`) siano davvero disponibili a runtime in quel contesto. Il percorso di codice è oggi completamente morto in produzione (nessuna rotta reale migrata). Story 12.4 sarà la prima a esercitarlo davvero (flippando il flag su `/precaricamento-allenatori`) — va verificato dal vivo prima di considerare quella PoC conclusa, non assumere che funzioni solo perché la build passa. [lib/auth/route-decision.ts, middleware.ts]

## Deferred from: code review of 12-2-helper-lettura-permessi-con-cache (2026-08-04)

- Nessun coalescing delle richieste concorrenti quando la cache è assente/scaduta ("thundering herd"): più chiamate simultanee innescherebbero ciascuna una `findMany` separata prima che la prima possa popolare la cache per le altre — il costo è solo query ridondanti (nessun risultato scorretto), non una violazione di AC. Nessun consumer esiste ancora per osservare il pattern di carico reale; da rivalutare quando Story 12.3/12.4 collegheranno un chiamante reale. [lib/auth/permessi-configurabili.ts]
- Una `invalidaCachePermessi()` chiamata mentre un refresh precedente è ancora in volo potrebbe veder quel refresh completarsi dopo l'invalidazione, ripopolando silenziosamente la cache appena azzerata con dati pre-invalidazione — stessa causa radice del punto precedente (nessun meccanismo di coalescing/lock). [lib/auth/permessi-configurabili.ts]
- `chiave()` non normalizza `rotta` (maiuscole/minuscole, spazi, slash finale) — un futuro consumer che passasse un `pathname` grezzo invece del `prefix` esatto di `PROTECTED_ROUTES` fallirebbe chiuso silenziosamente. La correzione corretta dipende da come Story 12.3 deciderà di chiamare questo helper (pathname grezzo vs. `route.prefix` già risolto) — prematuro indovinare ora quale normalizzazione serva davvero. [lib/auth/permessi-configurabili.ts]

## Deferred from: code review of 12-4-poc-precaricamento-allenatori (2026-08-04)

- La cache di lettura invalidata da `invalidaCachePermessi()` non è letteralmente condivisa tra la pagina `/permessi-accesso` (che salva) e un eventuale altro isolate Cloudflare Workers che serva `/precaricamento-allenatori` in quel momento: l'invalidazione azzera solo la cache dell'isolate che gestisce la richiesta di salvataggio, non dell'intera flotta — un limite superiore probabilistico più basso del TTL pieno (90s), non una garanzia immediata a livello di flotta. Limite architetturale intrinseco della cache in-memory per-isolate già accettato in Story 12.2. [lib/auth/permessi-configurabili.ts]
- Nessun coalescing delle richieste concorrenti nel `Promise.all` di `isAutorizzato` quando la cache è assente/scaduta e un utente ha più Ruoli sulla stessa rotta migrata — ri-afferma lo stesso gap già deferito in Story 12.2/12.3, ora con un primo consumer reale (`/precaricamento-allenatori`) ma senza segnale di carico reale che ne giustifichi la correzione. [lib/auth/route-decision.ts]
- Gap di concorrenza su `salvaPermessiRotte` (due Admin che salvano quasi contemporaneamente: l'ultimo vince silenziosamente) — ri-afferma lo stesso Defer già accettato in Story 12.1/12.2, non introdotto da questa storia. [app/(amministrazione)/permessi-accesso/actions.ts]
